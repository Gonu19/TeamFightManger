> **답하는 질문:** 이 값을 내가 직접 바꾸려면 어디를 어떻게 고치나
> **읽을 때:** 임계값·프롬프트를 조정할 때. 명령 자체는 `RUNBOOK.md` 다
> **줄 번호는 힌트다.** 이름(상수명·키명)으로 찾는다 — 줄은 밀린다

# 손으로 고치는 값

## 한눈에

세 가지의 성격이 다르다. **①만 지금 수동으로 된다.** ②·③은 코드에 박혀 있어
고치면 재빌드(= 앱 재시작)가 따른다.

| 무엇 | 어디 | 재시작 | 그다음 |
|---|---|---|---|
| ① 패치 감쇠 반감기 | DB `analysis_config` | 불필요 | **집계 다시 돌리기** |
| ① 축소 강도 `k0` | DB `analysis_config` | 불필요 | **집계 다시 돌리기** |
| ② 쌍 채택 문턱 `MIN_OBSERVATIONS` | `PairEffectCalculator` | 필요 | **집계 다시 돌리기** |
| ② 묶음 문턱 `SIGNAL` | `web/view/PairRow` | 필요 | 새로고침 |
| ② 묶음당 줄 수 `PAIRS_SHOWN` | `web/StatsController` | 필요 | 새로고침 |
| ② 채우는 줄 수 `MIN_SHOWN` | `web/view/PairBucket` | 필요 | 새로고침 |
| ③ 기사·댓글 프롬프트 | `story/StoryPrompts` | 필요 | **기사를 새로 뽑아야** 보인다 |
| ③ 갤러리 프롬프트 | `story/gallery/GalleryPrompts` | 필요 | **갤을 새로 뽑아야** 보인다 |

"집계 다시 돌리기" = 연대기 화면의 **② 「다시 돌리기」** 버튼, 또는 `.\run.ps1 -Aggregate`.
재시작 = `.\run.ps1` (스크립트가 컴파일까지 한다. devtools 가 없어 자바는 안 갈아끼워진다).

---

## ① 패치별 가중치

### 무엇을 정하는 값인가

경기 하나가 **지금** 얼마나 유효한지를 정한다. 오래된 경기를 버리는 대신 무게를 깎는다.

```
w = 0.5^(자기변경 ÷ self_decay_half_life) × 0.5^(경과패치 ÷ meta_decay_half_life)
```

두 축을 나눈 이유가 있다 (D15a). 패치가 늑대인간을 너프하면 **늑대인간을 안 건드렸어도**
전사의 승률이 바뀐다 — 상대 풀이 달라지기 때문이다. 그래서 안 바뀐 챔피언도 감쇠를
받아야 하는데, 한 축으로 하드 컷을 하면 남는 데이터가 없다.
**자기 변경은 세게(반감기 2), 메타 변화는 약하게** 깎는 것이 그 답이다.

지수의 두 인자는 **"경기 이후 기준 시점까지 흐른 양"** 이지 경기 시점의 누적값이 아니다.
이걸 뒤집으면 최신 경기가 가장 세게 깎이고 옛 경기가 살아남는데, 값은 여전히 (0,1] 안이라
**눈으로는 못 잡는다.**

### 살아 있는 키는 셋뿐이다

`analysis_config` 에 여섯 줄이 있지만 **셋은 아무 일도 안 한다.** 죽은 키를 고치고
재집계하면 "설정을 바꿨는데 결과가 안 바뀐다" 가 되는데, 로그만 보면 성공으로 보인다.

| 키 | 지금 값 | 쓰는 곳 | |
|---|---|---|---|
| `self_decay_half_life` | 2 | `DecayWeight.of` | **산다** |
| `meta_decay_half_life` | 2 | `DecayWeight.of` | **산다** (V5 가 12→2) |
| `prior_strength_k0` | 24 | `CounterCalculator` · `PerformanceCalculator` | **산다** |
| `prior_strength_k1` | 15 | 없음 | 죽었다 |
| `min_sample` | 10 | `agg_run` 에 기록만 된다 | 죽었다 — **필터가 아니다** |
| `synergy_max_size` | 3 | 없음 (시너지 계층 미구현, D60) | 죽었다 |

### 바꾸는 법

```bash
$env:PGPASSWORD='postgres'; & 'C:\Program Files\PostgreSQL\16\bin\psql.exe' -U postgres -d tfm -c "SELECT key, value, note, updated_at FROM analysis_config ORDER BY key;"
```

```bash
$env:PGPASSWORD='postgres'; & 'C:\Program Files\PostgreSQL\16\bin\psql.exe' -U postgres -d tfm -c "UPDATE analysis_config SET value = 4, note = note || ' [손으로 4 로 올림]', updated_at = now() WHERE key = 'meta_decay_half_life';"
```

그리고 연대기의 **② 「다시 돌리기」** 를 누른다. `AggregationService.run()` 이 매번
`configDao.load()` 로 새로 읽으므로 **재시작이 필요 없다.**

### 값을 고르는 감각

실측 패치 밀도가 시즌당 3.8개다 (V5 주석). 그래서 대략:

| 반감기 | 뜻 | 효과 |
|---|---|---|
| 2 | 약 0.5시즌 | 지금 값. 티어 순위가 최대 9칸 움직인다 |
| 4 | 약 1시즌 | 더 안정적, 대신 옛 메타가 더 남는다 |
| 12 | 약 3시즌 | **아무 일도 안 한다** — 한 커리어의 경기가 걸친 패치 폭이 1~3뿐이라 최소 가중치가 0.841 이고 티어가 평균 0.23%p 움직였다 (D53 이 이래서 12→2 로 내렸다) |

### 지뢰

- **0 이하를 넣으면 앱이 안 뜬다.** `AnalysisConfig` 가 던진다 — 0 이면 가중치가 전부
  0 이라 표본이 통째로 사라지고, 음수면 옛 데이터가 오히려 증폭된다. 죽는 편이 맞다
- **마이그레이션 파일(`V1`·`V5`)을 고쳐 값을 바꾸지 않는다.** 적용된 Flyway 파일은
  주석 한 글자도 체크섬을 깬다. 반드시 `UPDATE` 로 바꾼다
- **반감기를 바꾼 이력은 `agg_run` 에 안 남는다.** 그 표는 `min_sample` 과
  `prior_strength`(k0)만 찍는다. 흔적은 `analysis_config.updated_at` 과 `note` 뿐이니
  위 예시처럼 `note` 에 이유를 같이 적어 둔다
- 테스트 DB 는 `tfm_test` 라 **운영 값을 바꿔도 테스트는 안 깨진다.** 반대로
  시드를 고치면 `AnalysisConfigTest`·`AnalysisDaoTest` 가 깨진다

---

## ② 시너지·카운터 쌍의 출력 조건

### 어디서 잘리는지

값 하나가 화면에 뜨기까지 문턱을 **넷** 지난다. 어느 것을 만지느냐에 따라 재집계가
필요한지 아닌지가 갈린다.

```
관측
  │
  ├─ 릿지가 얇은 쌍을 0 쪽으로 누른다      PAIR_RIDGE 16 · TEAM_RIDGE 64
  │
  ├─ ① MIN_OBSERVATIONS 10 미만이면 버린다  ← 집계 시점. DB 에 안 들어간다
  │                                            ⇒ 바꾸면 재집계 필요
  ▼
champion_pair_effect (DB)
  │
  ├─ ② SIGNAL 0.10σ 를 못 넘으면 NEUTRAL    ← 표시 시점
  ├─ ③ PAIRS_SHOWN 10 줄에서 자른다          ← 표시 시점
  └─ ④ MIN_SHOWN 5 줄까지 흐리게 채운다      ← 표시 시점
                                              ⇒ 재시작만 하면 즉시
```

### ① `PairEffectCalculator.MIN_OBSERVATIONS` = 10

`analysis/pair/PairEffectCalculator.java:68`

**쌍을 아예 만들지 말지**를 정한다. 이 아래는 DB 에 행이 안 생기므로 화면에서
되살릴 방법이 없다. **바꾸면 반드시 재집계한다.**

내려도 되는 근거는 릿지에 있다. `PAIR_RIDGE` 가 16 이라 관측 10회짜리 쌍의 갱신 분모가
`10 + 16 = 26` — **계수의 6할 이상이 릿지에 눌린다.** 얇은 쌍이 큰 값으로 상위를
차지하는 일은 문턱이 아니라 릿지가 막는다. 문턱이 하는 일은 그 위에서
"이건 아예 말하지 않는다" 를 정하는 것뿐이다.

| 값 | 결과 |
|---|---|
| 20 (옛 값) | `DEATH` 쌍 314개 |
| **10 (지금)** | `DEATH` 쌍 1,194개 |
| 더 내리면 | 줄은 늘지만 전부 릿지에 눌려 0 근처다 — 목록이 아니라 잡음이 된다 |

화면의 값 옆에 관측 수가 그대로 붙으므로(D13·D60) 10회짜리도 "10경기" 라고 적힌 채 나간다.

### ② `PairRow.SIGNAL` = 0.10

`web/view/PairRow.java:73`

**어려움·쉬움·듀오 중 어디에 넣을지**를 가르는 σ 문턱이다. 못 넘으면 `NEUTRAL` 이라
세 묶음 어디에도 안 들어가고, 서명(「어그로 분산」 같은 말)도 이 문턱을 같이 본다.

- **올리면** 묶음이 비고 서명이 줄어든다 — 확신 있는 것만 말한다
- **내리면** 잡음이 묶음으로 올라온다

이 값은 **잰 것이 아니라 고른 값**이다 (`decisions/OPEN.md`). 정하려면 쌍마다 표준오차가
필요한데 릿지가 계수만 돌려준다.

### ③ `StatsController.PAIRS_SHOWN` = 10

`web/StatsController.java:58`

한 묶음에 그리는 최대 줄 수. 12 에서 10 으로 줄인 이유는 묶음이 셋이 되면서다 —
화면 하나에 30줄이면 세 묶음을 나란히 놓은 이점이 사라진다.

### ④ `PairBucket.MIN_SHOWN` = 5

`web/view/PairBucket.java:52`

문턱을 넘은 줄이 다섯에 못 미치면 **그 아래 줄로 다섯까지 채우되 흐리게** 그린다.
빈 묶음은 "없다" 로 읽히는데 실제로는 "확신이 없다" 이기 때문이다.
채운 줄과 넘은 줄을 **한 목록으로 합치지는 않는다** (D77).

### 바꾸는 절차

```bash
.\run.ps1 -Aggregate
```

`MIN_OBSERVATIONS` 를 건드렸다면 위처럼 재시작과 재집계를 한 번에. ②③④만 건드렸다면
`.\run.ps1` 로 재시작한 뒤 새로고침이면 끝난다.

---

## ③ 기사·갤러리 프롬프트

### 어디에 있나

프롬프트는 자바 텍스트 블록(`"""`)이다. 파일 둘뿐이다.

**`story/StoryPrompts.java`** (467줄)

| 블록 | 위치 | 무엇 |
|---|---|---|
| `article(...)` | 32 | 기사 본문. `system` 36~108 · `user` 113 |
| `comments(...)` | 143 | 경기 댓글. `system` 149~222 · `user` 228 |
| `roundSummary(...)` | 268 | 총평. `system` 271~297 |
| `ROUND_PARAGRAPHS` = 3 | 315 | 총평 문단 수 |
| `roundComments(...)` | 399 | 총평 댓글. `system` 402~421 |
| `ROUND_COMMENTS` = 8 | 436 | 총평 댓글 수. 매치 댓글보다 적다 |

**`story/gallery/GalleryPrompts.java`** (240줄)

| 블록 | 위치 | 무엇 |
|---|---|---|
| `IDENTITY` | 48~71 | 갤러리 화자의 정체성 |
| `SYSTEM_RULES` | 149~239 | 글쓰기 규칙 |
| `TOKENS_PER_POST` = 380 | 80 | 글 하나에 잡는 출력 토큰 |
| `MIN_TOKENS` = 2000 | 88 | 조각 하나의 출력 하한 |

### 고칠 때 반드시 지킬 것

**1. 챔피언은 한글로 부른다 (D80).** 프롬프트에서만 영어 코드로 되돌리면 기사는
영어로 나오는데 `FactCheck` 의 대조 어휘는 한글이라 **겹치는 낱말이 하나도 없다.**
그러면 검사가 조용히 죽는다 — 그리고 **지적 0건과 "깨끗하다" 는 화면에서 똑같이 보인다.**
어휘를 옮기려면 넷을 같이 옮긴다: `BriefRenderer` · 선수 표 · `FactCheck` · `StoryPrompts`.

**2. 출력 형식을 바꾸면 파서도 바꾼다.** 댓글은 `StoryPrompts.splitComments` 가
**줄 단위**로 자르고 번호·따옴표를 떼어낸다. 형식을 JSON 으로 바꾸는 식이면 그쪽도 같이.

**3. 토큰 예산을 넘기지 않는다.** Groq 무료 티어가 **분당 8,000 토큰**이다.
`TOKENS_PER_POST` 를 800 으로 되돌리면 페이지 하나가 20,000 을 쓰고 그 차이만큼 기다린다
(D74 가 380 으로 내린 이유). 반대로 `MIN_TOKENS` 를 조이면 추론 모델이 **생각으로만 다 쓰고
본문이 빈 채로** 돌아온다 — 실물에서 댓글 프롬프트가 그렇게 실패했다.

**4. 조사를 코드로 이어 붙이지 않는다.** `"기사" + " 를"` 은 띄어지고, 받침에 따라
을/를 이 갈리는 것도 못 맞춘다("총평를"). 종류가 몇 안 되므로 낱말마다 적어 둔다.

### 반영

```bash
.\run.ps1
```

**옛 기사·갤은 안 바뀐다.** `brief_text` 는 **생성 시점의 문자열**이라 일부러 안 고친다 —
그 기사가 무엇을 보고 쓰였는지가 기록이기 때문이다. 새 프롬프트를 보려면
연대기에서 **「기사 쓰기」·「반응 생성」** 을 다시 누른다.

값 없이 프롬프트만 보고 싶으면 실물 세이브로 찍어 볼 수 있다.

```bash
$env:TFM_PEEK_SAVE='C:\Users\altpa\AppData\LocalLow\samoyed\Teamfight Manager\slot_638683925954242004.tfm'; $env:TFM_DB_PASSWORD='postgres'; .\gradlew.bat test --tests '*PromptPeekTest' --console=plain
```

---

## 바꾼 뒤 확인하는 순서

1. **앱이 뜨는가** — `analysis_config` 에 못 쓸 값을 넣었으면 여기서 죽는다
2. **집계가 도는가** — 연대기 ②. 공지에 행 수가 찍힌다
3. **화면이 달라졌는가** — `/tier` 와 `/champion/{code}`
4. **테스트** — `$env:TFM_DB_PASSWORD='postgres'; .\gradlew.bat test`
   결과는 `build/reports/tests/test/index.html` **카운터로 센다**.
   `BUILD SUCCESSFUL` 만으로는 몇 개가 건너뛰어졌는지 모른다

상수를 바꿨을 때 **어느 테스트가 깨지는지는 상수마다 다르다.** 상수를 그대로 읽는
테스트는 안 깨지고, 숫자를 손으로 적어 둔 테스트만 깨진다.

| 바꾼 것 | 깨지나 | 왜 |
|---|---|---|
| `MIN_OBSERVATIONS` | 안 깨진다 | `PairEffectCalculatorTest` 가 상수를 그대로 읽는다 |
| `PAIRS_SHOWN` | 안 깨진다 | `PairBucketTest` 는 이 값을 인자로 받는다 |
| `MIN_SHOWN` | **깨진다** | `PairBucketTest` 가 `hasSize(5)` 로 적어 뒀다 |
| `SIGNAL` | 크게 바꾸면 깨진다 | 경계 검사는 `SIGNAL ± 0.01` 이라 따라오지만, `±0.10` 같은 고정 숫자를 쓰는 검사들이 문턱을 넘나든다 |

**깨졌다면 테스트가 틀린 것이 아니다 — 그 테스트가 지금 결정을 들고 있는 것이다.**
값을 바꾸기로 했으면 기댓값도 같이 옮기고, 왜 옮겼는지를 `decisions/` 에 새 번호로 남긴다.

---

## 아직 안 되는 것

②·③은 **화면에서 못 바꾼다.** 코드 상수와 텍스트 블록이라 편집기와 재빌드가 필요하다.

화면에서 바꾸고 싶다면 ①이 이미 그 모양이다 — `analysis_config` 처럼 표로 빼고
읽는 쪽이 매번 다시 읽으면 된다. 다만 그때 따라오는 것이 있다.

- **프롬프트를 DB 로 빼면 버전이 필요해진다.** 지금은 프롬프트가 git 에 있어서
  "이 기사는 어느 프롬프트로 썼나" 를 커밋으로 답할 수 있다. 표로 옮기면 그 답이 사라진다
- **상수를 표로 빼면 테스트가 값을 못 고정한다.** `PairRowTest` 가 지키는 것은 숫자가
  아니라 **규칙**이므로, 표에서 읽게 되면 그 규칙을 다른 방법으로 붙들어야 한다

그래서 이 문서는 **"지금 손으로 어떻게 하나"** 까지만 답한다. 화면에서 바꾸는 것은
위 둘을 어떻게 풀지 정한 다음의 일이고, 정하면 새 결정 문서로 남긴다.
