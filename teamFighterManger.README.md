---
title: 팀파이트 매니저 티어 분석
created: 2026-08-10
updated: 2026-08-25
para:
project:
areas: []
status:
tags: [teamfight-manager, database, data-analysis, spring-boot]
---

# 팀파이트 매니저 티어 분석

팀파이트 매니저 세이브 파일을 감시해서 경기 데이터를 자동 적재하고, **게임이 시즌마다 버리는 기록을 누적**해 카운터 상성·조합 시너지·챔피언 티어를 보여주는 웹.

## 문서

- [[project.md]] — 바이브 코딩용 요구사항. 범위·완료 기준·제약
- decisions/README.md — 설계 결정과 근거. 측정값과 과정 포함
- [[savefile.md]] — 세이브 파일 구조 레퍼런스 (리버싱 결과)
- [[banpick.md]] — 밴픽 시뮬레이터 설계. **다음 구현**
- [[db-review.md]] — 초기 OCR 전제 스키마 검토. **폐기됨** (OCR·라인 5종 전제).
  다만 밴 테이블 신설 · 집계 PK 에 patch_id · 비율 대신 카운트+생성열 · 순열 중복 차단
  네 결정은 현행 `V1__init.sql` 에 그대로 살아 있다. 그래서 기록용으로 남긴다.
  **`is_reliable = 20경기` 는 폐기된 값이다. 현행은 `min_sample = 10`.**
- `structure.md` — 파일 구조와 패키지 경계의 근거
- [[workflow.md]] — 작업 형식. 서브에이전트 배분과 문서 배분 규칙
- `src/main/resources/db/migration/V1__init.sql` — DDL 전문. **스키마의 유일한 원본**
- `db/migration/V3__seed_champions.sql` — 챔피언 40종 + 역할군 시드
- `tests/verify_schema.sql` — 스키마 제약 검증 (읽기 전용, ROLLBACK)
- `tools/nrbf.py` · `save_model.py` · `make_baseline.py` — 파서 레퍼런스 구현 (**동작 확인됨**)
- `tests/baseline/*.json` — 골든 파일 3슬롯. Java 파서의 정답지

## 결정 사항 요약

| 항목 | 선택 |
|---|---|
| 데이터 수집 | 세이브 파일(.tfm) 파싱. OCR·모드 안 씀 |
| 수집 방식 | 파일 쓰기 감지 → 자동 적재 |
| 경기 형식 | **4v4 고정.** 팀당 픽 4 / 밴 3. 라인 개념 없음 |
| 드래프트 순서 | 14스텝 고정. 2차 밴이 픽 4개 공개 뒤에 온다 |
| 역할군 | 전사 / 원거리 / 마법사 / 전투 보조 / 암살자 |
| 표본 기준 | 10경기 미만은 노출 안 함 |
| 순위 정렬 | 카운터·시너지는 챔피언 강도를 뺀 **상성 이득**으로 |
| 축소추정 | 2단 — 패치 추정 → 전체 누적 → 챔피언 강도 기대값 |
| 시너지 | 챔피언 2~3인. 4인은 역할군 조합으로 |
| 패치 | 이중 감쇠(자기변경 h=2, 메타 h=12). 사용자 입력 불필요 |
| 경기력(KDA·딜·탱) | 티어엔 미사용. 축소 목표값·조기 지표로만 |
| 이벤트전 | 게임이 데이터를 안 남김 → 처리 불필요 |
| 수집 주기 | 스크림 한 판마다 자동 저장. 파일 잠김 없음 |
| 밴픽 시뮬 | **다음 구현.** 표본 미달이면 추천을 거부하는 방식 |
| 스택 | **Spring Boot 4.1.1 / Java 21 / Thymeleaf + HTMX / Postgres 16 + Flyway** |
| 배포 | 로컬 `java -jar` 단일 실행. 워처가 세이브 파일을 봐야 해서 클라우드 불가 |
| 작업 방식 | 혼자 구현. 읽기 전용 검증만 `agy` 서브에이전트에 위임 |
| 환경 | **Windows 네이티브.** WSL·Docker 안 씀. 워처가 `WatchService` 로 네이티브 알림을 받는다 |

## 근거가 된 실측값

세이브 3개 = 공식 800경기 + 스크림 407경기, 챔피언 40종 기준.

- 카운터 쌍 1,560가지 중 1,554가지 관측 (99.6%)
- 진짜 카운터 효과의 표준편차 약 6%p — 대부분 44~56%, 꼬리는 75~84%
- 7경기를 넘기면 우연에 의한 100%/0% 승률이 사라짐
- 3인 조합은 3,914가지 관측, 최다 36회 / 4인 조합은 최다 9회

## 상태

설계 완료. 웹 코드 없음.

- **앱이 뜬다.** Spring Boot 4.1.1 부팅 확인, Flyway 가 `tfm` 에 v1 적용, 포트 8088
- **파서가 동작한다.** Java · Python 두 구현이 골든 파일에서 바이트 일치. 경기 805건
- **적재 계층의 토대가 섰다.** 엔티티 15 · 리포지토리 9. Hibernate 7 이 실제 스키마를 검증 통과,
  Postgres named ENUM 왕복 확인. 테스트 14개 통과(건너뜀 0)
- 스키마 Windows PG16.15 적용 검증 완료 — 오류 0, 제약 검증 **24/24 통과**
  (테이블 21 · 뷰 3 · ENUM 5 · 생성열 6 · 시드 `draft_step` 14행)
- `seed/champions.csv` 역할군 40행 완성 — 관측 40종과 정확히 일치
- 밴픽 시뮬 설계 완료 (`banpick.md`), 드래프트 14스텝 확정

**환경 준비 완료.** Java 21.0.10 · PostgreSQL 16.15 (Windows 네이티브) · DB `tfm`/`tfm_test` 생성됨.

**미확정:** 세이브 저장 주기(측정 대기), 티어 등급 컷라인,
`BluePick` 배열이 실제 픽 순서인지(D25), `Banpick.LineModel` 활용 가능 여부.

**아직 없는 것:** 워처, 적재, 집계, 화면, Java 파서. 측정 스크립트(`feasibility.py` 등).
