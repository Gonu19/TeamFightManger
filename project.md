---
title: 팀파이트 매니저 티어 분석 - 요구사항
created: 2026-08-10
updated: 2026-08-25
para:
project:
areas: []
status:
tags: [teamfight-manager, requirements, spring-boot, data-analysis]
---

# 팀파이트 매니저 티어 분석 웹

> 요구사항 문서입니다. **자유롭게 읽으세요.** 수정만 사용자 전용입니다(에이전트 편집은 설정으로 차단돼 있습니다).
> **★ 는 필수.** 나머지는 비워두면 팀장이 필요할 때 물어봅니다.

## ★ 무엇을 만드나

팀파이트 매니저 세이브 파일을 감시해서 경기 기록을 자동으로 DB에 쌓고, **게임이 시즌마다 버리는 데이터를 누적**해서 챔피언 티어표·카운터 상성·조합 시너지를 웹에서 볼 수 있게 한다. 사용자는 게임을 평소처럼 플레이하기만 하면 되고, 저장이 일어날 때마다 웹이 갱신된다.

세이브 파일 구조는 `savefile.md`, 왜 이렇게 정했는지는 `decision.md` 참고.
**결정을 뒤집고 싶으면 `decision.md` 의 해당 항목에 있는 "뒤집힐 조건"을 먼저 읽으세요.** 대부분의 결정은 실제 세이브 파일을 측정해서 나온 것이고, 근거 수치가 같이 적혀 있습니다.

## ★ 다음 구현 — 밴픽 시뮬레이터

1차(수집·집계·티어/카운터/시너지 화면)가 끝나면 바로 붙인다. 설계는 `banpick.md`.
표본이 부족한 문제는 기능을 미루는 대신 **추천을 거부하는 규칙**으로 푼다.
드래프트는 팀당 밴 3 · 픽 4 의 14스텝 고정이고, 순서는 `draft_step` 테이블이 원본이다.

## ★ 안 하는 것

- **OCR·스크린샷 수집** — 세이브 파일에 원본 데이터가 전부 있다
- **모드(Mod) 제작** — 1편에는 공식 SDK가 없고, 모드로 얻을 데이터가 이미 세이브에 있다
- **게임이 이미 보여주는 화면 재현** — 게임에 챔피언 통계 화면(승률·밴픽률, 현재패치/시즌전체 탭)이 이미 있다. 그걸 그대로 다시 만들지 않는다
- **라인별 티어표** — 이 게임에는 라인·포지션 개념이 없다
- **4인 챔피언 조합 통계** — 최다 9경기. 역할군 조합으로 대체한다
- **별도 SPA(React/Next)** — Thymeleaf 서버 렌더링으로 간다. 프로젝트·빌드·배포를 하나로 유지한다
- **클라우드 배포** — 워처가 세이브 파일을 봐야 해서 어차피 로컬 실행이다. `java -jar` 로 끝낸다
- 소셜 로그인, 권한 등급, 다국어, 모바일 앱, 실시간 게임 오버레이

## ★ 완료 기준

**직접 해보면 O/X가 갈리게** 쓰세요. "잘 된다"는 판정할 수 없습니다.
아래가 전부 통과하면 끝난 것으로 봅니다.

**수집**

- [ ] `slot_*.tfm` 파일 경로를 등록하면 공식 경기와 스크림이 전부 적재되고, 적재 건수가 파서가 세는 건수와 정확히 일치한다
- [ ] 같은 파일을 다시 적재하면 `ingest_run` 이 중복(file_hash)으로 걸러내고 경기가 늘지 않는다
- [ ] 게임에서 스크림을 한 판 돌리면(**저장을 누르지 않아도**) 웹을 새로고침하지 않아도 신규 경기 수가 올라간다
- [ ] 세이브 파일 쓰기 도중에 파싱이 걸려도 깨진 데이터가 저장되지 않는다(디바운스 후 재시도)
- [ ] 워처가 켜진 상태에서 들어온 스크림에는 `observed_day` 가 채워지고, 그 값으로 패치가 배정된다
- [ ] 슬롯을 2개 이상 등록하면 각각 별도 커리어로 잡히고, 슬롯 필터로 나눠 볼 수 있다
- [ ] 세이브 폴더를 통째로 등록해도 `*.tfm_backup` 이 슬롯으로 잡히지 않는다 — 슬롯 수가 3이지 6이 아니다
- [ ] 게임이 세이브를 쓰는 중에도 워처가 파일을 열 수 있다 (Windows 파일 잠김 — 공유 읽기로 연다)
- [ ] 시즌이 넘어가 게임에서 이전 시즌 기록이 사라져도, 웹에는 그 시즌 경기가 그대로 남아 있다

**패치**

- [ ] 패치 이력이 자동으로 적재된다. 사용자가 패치 날짜를 입력하는 화면이 없다
- [ ] 각 경기에 패치가 자동 배정되고, 패치를 고르면 그 시점의 축소추정값이 나온다 —
      해당 구간 경기만 세는 하드 필터가 아니다 (D24). 그 패치의 원시 경기 수가 함께 표시된다
- [ ] `GLOBAL` 스코프에서는 패치 선택이 뜨지 않는다 (슬롯을 합산하므로 패치 축이 없다)
- [ ] 챔피언 상세에서 "최근 N패치 동안 변경 없음" 또는 변경 내역(공격력 +12 등)이 표시된다

**분석**

- [ ] `/tier` 에서 챔피언 승률·픽률·밴률·티어가 한 표로 나오고, 역할군 필터가 동작한다
- [ ] 표본 10경기 미만인 조합은 어떤 화면에도 나오지 않는다 (승률 정렬 시. 픽률 정렬에서는 예외 — 아래 참고)
- [ ] 순위표가 원본 승률이 아니라 축소추정값으로 정렬되고, 원본 승률과 경기 수가 항상 함께 표시된다
- [ ] 10경기 3승 0패인 조합이 60경기 44승인 조합보다 위에 오지 않는다
- [ ] 정렬 기준을 픽률로 바꾸면 10경기 미만 챔피언도 목록에 나온다 — 정렬 축에 따라 표본 필터가 달라지고, 그 사실이 화면에 표시된다
- [ ] 챔피언 상세의 카운터 목록이 **실제 승률 / 기대 승률 / 상성 이득** 세 값을 함께 보여주고, 기본 정렬이 상성 이득이다
- [ ] 전체 승률이 낮은 챔피언(예: Fighter)이라도 상성 이득이 크면 카운터 목록 상위에 나온다 — 강한 챔피언이 모든 목록을 점령하지 않는다
- [ ] 2인·3인 시너지 조합이 조회되고, 4인 챔피언 조합 화면은 존재하지 않는다
- [ ] 역할군 4인 구성별 승률이 조회된다 (예: 전사2+마법사1+전투보조1)
- [ ] 밴률 집계에서 스크림이 제외된다 — 스크림 포함/제외를 토글하면 밴률만 달라지고 승률 표본은 늘어난다
- [ ] 표본이 부족한 매치업 목록을 볼 수 있고, 몇 경기가 더 필요한지 표시된다
- [ ] 챔피언 상세에 딜·탱·힐·KDA 가 표시되지만, 티어 점수를 바꾸지는 않는다 — 이 값이 달라져도 티어가 안 바뀐다
- [ ] 표본이 10~15경기인 매치업에 "상성 의심 · 표본 축적 중" 표시가 뜬다 (경기력 z가 기준을 넘을 때)

**적재 정확성**

- [ ] 파서가 각 참가자의 진영을 챔피언 이름으로 매칭한다 — `ChampStat` 인덱스 순서를 쓰지 않는다
- [ ] 워처가 표시하는 "현재 날짜"가 게임 화면의 날짜와 일치한다. 이벤트전만 치른 날에도 날짜가 멈추지 않는다
- [ ] 이벤트전을 치러도 경기 수가 늘지 않는다 (게임이 데이터를 남기지 않음)

**화면**

- [ ] 티어표에서 경기 수가 승률과 같은 크기·위계로 읽힌다. 눈을 가늘게 뜨지 않아도 보인다
- [ ] 10경기짜리 행과 60경기짜리 행이 시각적으로 구분된다 (같은 승률이어도)
- [ ] 티어 뱃지 옆에 항상 승률 숫자가 있다. 뱃지만 있는 화면이 없다
- [ ] 스크롤을 내려도 지금 보는 것이 `GLOBAL` 인지 `CAREER` 인지 알 수 있다
- [ ] 표 머리에 현재 정렬 축과 적용된 표본 필터가 문장으로 표시된다
- [ ] 1280px 창에서 티어표 40행이 가로 스크롤 없이 보인다

**공통**

- [ ] 로그인하지 않아도 통계를 볼 수 있고, 슬롯 등록·적재는 로그인해야 한다
- [ ] `.\gradlew.bat test` 가 전부 통과한다 (Windows 로컬 Postgres `tfm_test` 대상, 트랜잭션 롤백 격리)
- [ ] `tests/verify_schema.sql` 이 24/24 통과한다 — 스키마를 고칠 때마다 다시 돌린다
- [ ] **Java 파서가 `tests/baseline/*.json` 골든 파일을 그대로 통과한다** — Python 레퍼런스와 결과가 일치한다
- [ ] 바이트 수준 계약(부호 없는 readByte, 7-bit 길이, 버퍼 초과 예외)에 단위 테스트가 있다.
      **골든 파일로 대신하지 않는다** — 표본에 없는 입력은 골든 파일이 지켜주지 않는다 (D34)
- [ ] `java -jar` 한 번으로 워처·집계·웹이 모두 뜬다. 별도 프로세스를 띄우지 않아도 된다

## 나중에 정할 것 (지금은 아래 기본값으로 진행)

**여기서 멈추지 마세요.** 데이터가 더 쌓여야 정할 수 있는 것들입니다. 기본값으로 만들고, 나중에 `analysis_config` 값만 바꿔서 조정합니다.

| 항목 | 1차 기본값 | 언제 정하나 |
|---|---|---|
| 경기력 → 승률 회귀식 | **쓰지 않는다.** 축소 목표값은 챔피언 강도 기대값(Bradley-Terry)만 사용. `z_deal`/`z_tank`/`z_kda` 컬럼은 채우되 목표값 계산에는 넣지 않는다 | 표본이 더 쌓인 뒤 `tools/perf_signal.py` 재실행 |
| 티어 등급 컷라인 | 축소추정 승률 백분위 고정 — S+ 상위 5% / S 10% / A 20% / B 30% / C 20% / D 15% | 실제 분포를 보고 |
| 감쇠 반감기 | `self_decay_half_life = 2`, `meta_decay_half_life = 12` | 패치 전후 승률 변화를 쌍 단위로 잴 수 있을 때 |
| 축소 강도 | `prior_strength_k0 = 24`, `prior_strength_k1 = 15` | 위와 동일 |
| "상성 의심" 표시 임계 | 경기력 z 절대값 1.0 이상 | 오탐률을 보고 |

회귀식이 없어도 화면·집계·완료 기준은 전부 동작합니다. 회귀는 2차에 얹는 개선입니다.

## 지금 있는 것

- `src/main/resources/db/migration/V1__init.sql` — DDL 전문. **스키마의 유일한 원본.**
  Windows PG16.15 적용 검증 완료, 제약 검증 24/24 (D23 2차)
- `structure.md` — 파일 구조와 패키지 경계의 근거
- `decision.md` — 설계 결정과 근거. 측정값 포함
- `savefile.md` — 세이브 파일 구조 레퍼런스
- `banpick.md` — 밴픽 시뮬레이터 설계 (다음 구현)
- `tests/verify_schema.sql` — 스키마 제약 검증. **24/24 통과 확인됨**. 읽기 전용(ROLLBACK)
- `db-review.md` — 초기 OCR 전제 스키마 검토. **폐기됨.** 기록용
- `db/migration/V3__seed_champions.sql` — 챔피언 40종 + 역할군. **시드의 유일한 원본**
- `tools/nrbf.py` — MS-NRBF 파서 (**동작 확인됨**). Java 포팅의 원본
- `tools/save_model.py` — 경기·패치·현재날짜 추출. 진영은 챔피언 이름으로 매칭(D20)
- `tools/make_baseline.py` — 골든 파일 생성기. `--check` 로 대조만 할 수 있다
- `tests/baseline/*.json` — **골든 파일 3슬롯 생성됨.** 언어 무관 계약
- `tools/watch_save.py` — 세이브 쓰기 감지·주기 측정기
- `tools/feasibility.py` — 카운터·시너지 표본 커버리지
- `tools/combo_check.py` — 효과 크기, 조합 집중도, 가짜 극단값, 축소추정 비교
- `tools/counter_signal.py` — 챔피언 강도를 뺀 진짜 상성 계산
- `tools/perf_signal.py` — 승패 기반 vs 경기력 기반 신호 비교
- 웹 코드는 없음

> `fixtures/*.tfm` 은 커밋하지 않는다(`.gitignore`). 세이브 스냅샷 3개는 로컬에만 둔다.
> `tests/baseline/*.json` 은 커밋한다 — Java 파서의 정답지다.

**아직 없고 만들어야 하는 것**

- `tools/feasibility.py` · `combo_check.py` · `counter_signal.py` · `perf_signal.py` — 표본·효과크기 측정 스크립트. 아직 없다
- `tools/watch_save.py` — 세이브 쓰기 감지·주기 측정기. 아직 없다
- 챔피언 이미지 — 없으면 비워둔다. 필수 아님

챔피언 40종의 역할군은 **`V3__seed_champions.sql` 에 들어갔다.** `savefile.md` 관측 40종과 정확히 일치하고,
분포는 전사 10 / 원거리 7 / 마법사 7 / 전투보조 9 / 암살자 7 이다.

## 확인 방법

Windows PowerShell 에서 실행한다. WSL 을 쓰지 않는다.

```powershell
Get-Service postgresql-x64-16              # 서비스 실행 확인
$env:TFM_DB_PASSWORD = 'your-password'     # 따옴표 필수. 없으면 즉시 실패한다
.\gradlew.bat bootRun                      # Flyway 자동 실행 → http://localhost:8088
```

포트는 8088 이다. 8080 은 로컬 LLM 서버가 쓰고 있어서 비켰다 (`${TFM_PORT}` 로 변경 가능).

DB 는 `tfm`(운영) / `tfm_test`(테스트) 두 개. 둘 다 `-E UTF8` 로 만든다.

**psql 로 SQL 파일을 직접 적용할 때는 인코딩에 주의한다.** `client_encoding` 이 한글 Windows 콘솔의
UHC 를 따라가서 UTF-8 파일의 한글 주석에서 멈춘다. `V1__init.sql` 은 첫머리에
`SET client_encoding = 'UTF8'` 이 들어 있어 그대로 통과한다. 다른 SQL 파일을 만들 때도 같이 넣을 것.

적재·집계는 실행 중인 앱에서:

```powershell
$save = "$env:USERPROFILE\AppData\LocalLow\samoyed\Teamfight Manager\slot_638683925954242004.tfm"
$body = @{ path = $save } | ConvertTo-Json
Invoke-RestMethod -Method Post -Uri http://localhost:8080/api/slots -Body $body -ContentType 'application/json'
```

- `/tier` 티어표 · `/champion/{code}` 챔피언 상세(카운터) · `/synergy` 조합 · `/gaps` 표본 부족 목록
- `/draft` 밴픽 시뮬레이터 (다음 구현)
- 워처는 앱에 내장돼 있다. 별도 프로세스를 띄우지 않는다

**실물 세이브 파일 경로 (Windows에서 확인함)**

```
%USERPROFILE%\AppData\LocalLow\samoyed\Teamfight Manager\slot_*.tfm
E:\SteamLibrary\steamapps\common\Teamfight Manager\            # 게임 설치 경로
```

세이브 3개(2.6~2.75MB) 확인. **같은 폴더에 `slot_*.tfm_backup` 이 있다.**
글롭은 `slot_*.tfm` 으로 정확히 걸어야 한다 — 백업까지 잡으면 같은 커리어가 두 벌 적재된다
(`file_hash` 중복 검사는 슬롯 안에서만 돌아서 못 막는다). 경로에 공백이 있으니 인용 처리할 것.

**단, 라이브 파일을 테스트에 직접 쓰지 말 것.** 게임을 켜두면 스크림 한 판마다 내용이 바뀌어서 재현 가능한 테스트가 안 된다. 저장소의 `fixtures/` 로 스냅샷을 뜨고, 파서 기대 출력을 골든 JSON으로 함께 커밋한다.

## 제약

**써야 하는 것**

- **Spring Boot 4.1.1 / Java 21 / Gradle 9.5.1** — 이 프로젝트의 목적 중 하나가 Spring 학습이다.
  3.x 는 Initializr 가 더 이상 제공하지 않는다 (D31). **SNAPSHOT·M 버전을 쓰지 않는다** — 내용이 바뀌면
  골든 파일 검증의 전제가 무너진다
- **Thymeleaf + HTMX** — 서버 렌더링. 별도 SPA를 두지 않는다
- **PostgreSQL 16** (Windows 네이티브 설치) + **Flyway** — DDL 이 Postgres 전용 기능을 쓴다 (`GENERATED ALWAYS AS ... STORED`, `UNIQUE NULLS NOT DISTINCT`(PG15+), ENUM 타입). H2·SQLite 로 바꾸면 다시 짜야 한다. **이 PC 에서 Docker 가 기동되지 않아 Docker Compose 를 쓰지 않는다. WSL 도 쓰지 않는다 — 개발·실행 전부 Windows 네이티브다 (D27)**
- **Spring Data JPA**(적재) + **JdbcTemplate**(집계) — 집계 쿼리가 가중합·2단 축소·백분위라 JPA로 풀지 않는다
- `SseEmitter` (실시간 갱신), Spring Security(최소 설정)
- JUnit 5. **Testcontainers 는 쓰지 않는다** (Docker 불가). 테스트는 로컬 Postgres 의 별도 DB(`tfm_test`)에 붙고, 각 테스트는 트랜잭션 롤백으로 격리한다
- 스키마는 `db/migration/V1__init.sql` 기준. 바꾸면 `V2__*.sql` 을 추가하고 `decision.md` 에 이유를 남긴다.
  **`V1__init.sql` 을 직접 수정하지 않는다** — Flyway 체크섬이 깨진다

**지켜야 하는 것**

- 파서는 게임 DLL에 의존하지 않는다. 순수 Java 로 NRBF를 읽는다
- **Java 의 `byte` 는 부호가 있다. NRBF 는 unsigned 를 다룬다.** `& 0xFF` 를 빼먹으면 조용히 틀린 값이 나온다. 7-bit encoded int 길이 접두사와 UTF-8 문자열 길이도 직접 처리한다
- **`tools/` 의 Python 스크립트를 레퍼런스 구현으로 유지한다.** Java 파서 출력이 Python 출력과 일치해야 한다. 집계 결과도 마찬가지로 대조한다
- **`tests/baseline/*.json` 골든 파일은 언어 무관 계약이다.** Java 파서도 이 파일을 그대로 통과해야 한다
- 통계는 화면에서 실시간 계산하지 않는다. 집계 테이블에 배치로 넣고 화면은 읽기만 한다
- 승률·픽률·밴률을 원본으로 저장하지 않는다. 카운트만 저장하고 비율은 유도 컬럼
- 임계값(표본 기준, 축소추정 강도, 감쇠 반감기)을 코드에 박지 않는다. 전부 `analysis_config` 테이블에서 읽는다
- `GLOBAL`(슬롯 합산·패치 무시)과 `CAREER`(슬롯 내부·패치 반영) 집계를 절대 섞어서 보여주지 않는다
- 밴률은 공식 경기만으로 계산한다(스크림에 밴이 없다)
- 세이브 파일은 읽기만 한다. 어떤 경우에도 쓰지 않는다
- **참가자의 진영은 챔피언 이름으로 매칭한다.** `GameStat.ChampStat` 의 인덱스 순서는 경기의 20%에서 `BluePick+RedPick` 과 어긋난다
- **현재 게임 내 날짜는 `TodayData.<Time>k__BackingField` 에서 읽는다.** `max(GameStat.Day)` 로 추정하면 최대 5일 뒤처진다
- 세이브 쓰기 감지 후 1.5초 디바운스를 두고 읽는다. 쓰기 도중에 읽으면 스트림이 잘린다
- 경기력 지표(딜·탱·힐·KDA)를 티어 점수에 가산하지 않는다. 축소 목표값과 조기 지표로만 쓴다

**환경**

- 개인 프로젝트. 로컬 실행이 전제다(`java -jar`). 클라우드 비용이 발생하는 선택지를 쓰지 않는다
- 유료 API가 필요한 선택지는 도입 전에 물어본다

## 디자인 요구

디자인 담당을 따로 두지 않고 프론트엔드가 겸합니다. 그래서 판단이 필요한 것만 여기 적습니다.

**기본 방향**

- 데스크톱 우선, 최소 1280px. 모바일 대응 안 함
- 다크 기본. 게임 옆에 띄워두고 보는 도구다
- 밀도 우선. 티어표 40행이 스크롤 한 번 안에 들어오는 것을 목표로

**Thymeleaf + HTMX 로 만든다**

- 정렬·필터는 서버가 처리하고 **표 본문 조각만 교체**한다(`hx-get` + `hx-target`). 전체 페이지 리로드로 화면이 깜빡이지 않게
- 실시간 갱신은 SSE(`hx-ext="sse"` + `sse-swap`)로 해당 조각만 다시 그린다
- 표본에 따른 시각적 약화, 신뢰구간 막대, 티어 뱃지는 **서버에서 클래스·폭을 계산해 내려보낸다.** 클라이언트가 통계를 다시 계산하지 않는다
- 시너지 2인/3인 탭은 별도 URL로 둔다. 클라이언트 상태를 만들지 않는다
- 프론트엔드 빌드 도구를 두지 않는다. HTMX 는 스크립트 태그 하나로 넣는다

**불확실성 표시 — 이 프로젝트의 핵심이다**

승률만 크게 띄우고 경기 수를 회색 작은 글씨로 밀면 "숫자의 정직함" 원칙이 화면에서 무너집니다. 25경기 84%와 60경기 73% 중 어느 쪽이 더 믿을 만한지가 한눈에 보여야 합니다.

- **경기 수는 승률과 같은 위계로 배치한다.** 부가 정보로 밀지 않는다
- 표본이 적을수록 행 전체를 시각적으로 약하게 한다(투명도·채도). 임계값에서 딱 끊지 말고 연속적으로
- 신뢰구간을 범위나 막대로 보여준다. 단일 숫자만 크게 띄우지 않는다
- "표본 축적 중"은 빈 값이 아니라 **진행 상태**로 보여준다 — 몇 경기가 더 필요한지 함께

**카운터 행**

- 주값은 **상성 이득**(예: `+33.2%p`). 실제 승률과 기대 승률은 보조 위계로
- 이득의 부호가 색으로 즉시 읽혀야 한다

**티어 뱃지**

- 뱃지 옆에 **항상 승률 숫자를 병기**한다. 뱃지 단독으로 쓰지 않는다
- 등급 간 색 대비를 과하게 주지 않는다. 기저 승률 표준편차가 3.6%p라 S+와 B의 실제 차이가 작다. 색이 차이를 과장하면 안 된다

**스코프 구분**

- `GLOBAL`(슬롯 합산·패치 무시)과 `CAREER`(슬롯 내부·패치 반영)를 화면 상단에 고정 표시한다. 배경이나 테두리로 구분해서 스크롤 중에도 헷갈리지 않게

**정렬**

- 현재 정렬 축과 그에 따라 적용된 표본 필터를 표 머리에 문장으로 표시한다
  (예: "승률 기준 정렬 · 10경기 미만 제외")

## 판단 기준

선택이 갈릴 때의 우선순위. **기본값을 그대로 써도 됩니다.**

> 동작하는 것 우선 / 확장성보다 단순함 / 추측하지 말고 질문

추가로 이 프로젝트에서만:

- **숫자의 정직함 > 화면의 화려함.** 표본이 모자라면 그럴듯한 값을 만들지 말고 아예 빼거나 경기 수를 같이 보여준다
- **게임이 이미 하는 건 만들지 않는다.** 만들 가치는 게임이 못 하는 곳(시즌 누적, 카운터, 시너지)에만 있다
- 데이터로 확인할 수 있는 건 추측하지 말고 측정한다. `tools/` 의 스크립트가 그 방식의 예다
- **막히면 "나중에 정할 것"의 기본값으로 진행한다.** 정밀도를 위해 멈추지 않는다

## 작업 방식

**에이전트 팀을 두지 않는다. 혼자 구현하고, 검증만 `agy`(Antigravity CLI) 서브에이전트에 넘긴다.**

개인 프로젝트 규모에서 6명 분업은 조율 비용이 산출보다 크다. 특히 이 프로젝트는
어려운 부분(카운터·시너지 분석)이 한 사람 머릿속에 있어야 하고, 나눌 수 있는 부분
(보일러플레이트)은 애초에 어렵지 않다.

`agy` 에 넘기는 것 — **파일을 많이 읽어야 하는 읽기 전용 작업**:

- Java 파서 출력과 Python 레퍼런스 대조
- 골든 파일 회귀 확인
- 구현이 `decision.md` 의 결정과 어긋나지 않는지 대조
- 저장소 전체 훑기 (죽은 코드, 규칙 위반)

`agy` 에 넘기지 않는 것: 설계 판단, 구현, 최종 결정.

---

[[teamFighterManger.README]]
