# 밴픽 시뮬레이터

D13 이 2차로 미뤄뒀던 기능이다. 우선순위가 올라와 다음 구현이 됐다.
표본 문제는 그대로이므로 **기능을 미루는 대신 추천을 거부하는 규칙**으로 푼다.

전제: 팀당 밴 3 · 픽 4, 두 팀. 순서는 D26 의 14스텝 고정, 원본은 `draft_step` 테이블.

## 무엇을 만드나

드래프트를 한 스텝씩 진행하면서, **지금 차례에 무엇을 고르는 게 좋은지**를
이미 쌓인 통계(챔피언 강도 · 카운터 이득 · 시너지 · 역할군 구성)로 계산해 보여준다.
게임 AI 와 대전하는 것이 아니라, 사용자가 양쪽을 다 조작하며 상황을 만들어 보는 도구다.

## 안 하는 것

- **게임 AI 와의 대국** — 스트림 2 에 그 AI 의 학습된 카운터·조합 행렬이 실제로 들어 있다(D32).
  다만 셀 인덱스와 챔피언의 대응을 아직 모른다. 풀리면 상대 모델링에 쓴다
- **최적 드래프트 자동 탐색** — 14스텝 완전탐색은 조합이 터지고, 무엇보다
  각 스텝 평가의 오차가 표본에서 오는데 그걸 14번 곱하면 결과가 무의미해진다
- **승률 예측 숫자 단독 제시** — 효과 크기가 작다(페어 상성 평균 2%p). 하나의 큰 숫자로
  띄우면 없는 정밀도를 만들어낸다. 항상 근거 항목과 표본을 분해해서 같이 보여준다

## 상태 기계

`draft_session.next_step` 이 1~14 를 진행하고 15 면 종료. 각 스텝에서:

1. `draft_step` 에서 이번 스텝의 `side` / `action` / `side_seq` 를 읽는다
2. 후보 = 전체 챔피언 − 이 세션에서 이미 밴/픽된 챔피언 (`draft_selection` 유니크가 강제)
3. 후보별 점수를 계산한다 (아래)
4. 사용자가 하나를 확정하면 `draft_selection` 에 넣고 `next_step` 을 +1

**되돌리기는 스텝 단위로만.** 마지막 `draft_selection` 을 지우고 `next_step` 을 −1 한다.
중간 스텝을 임의로 바꾸면 이후 점수가 전부 무효라, 그 뒤를 잘라내는 것 외의 편집은 막는다.

## 픽 점수

승률 퍼센트포인트(%p) 공간에서 더한다. 각 항은 이미 "기저를 뺀 이득"이라 단위가 같다.

```
score(c) = base(c)  +  Σ counter_effect(c, e)  +  Σ synergy_effect({c} ∪ a)  +  comp_delta(c)
                       e ∈ 상대 확정 픽              a ⊂ 아군 확정 픽
```

| 항 | 출처 | 비고 |
|---|---|---|
| `base` | `champion_performance.adjusted_win_rate` − 0.5 | 스코프·패치는 세션 설정을 따른다 |
| `counter_effect` | `champion_matchup.counter_effect` | 상대 확정 픽 각각에 대해. D14 |
| `synergy_effect` | `synergy_stat.synergy_effect` | 아군 픽 1명이면 2인 조합, 2명 이상이면 3인 조합 우선·없으면 2인 합 |
| `comp_delta` | `category_comp_stat` | 남은 자리를 채웠을 때 도달 가능한 역할군 구성의 최댓값 − 현재 기대값 |

**단순 가산인 이유.** 항 사이의 상호작용을 모델링할 표본이 없다. 3인 조합 최다 36경기,
4인은 최다 9경기다(D11). 상호작용 항을 넣으면 추정하는 게 아니라 지어내는 것이 된다.
**가산 모델이 틀렸다는 것을 알고 쓰는 것이지, 맞다고 믿고 쓰는 게 아니다.** 화면에
항별 기여도를 분해해 보여주는 이유가 그것이다 — 합계가 아니라 근거를 읽게 만든다.

## 밴 점수 — 픽과 목적 함수가 다르다

**1차 밴 (스텝 1~4).** 공개된 픽이 없다. 상대 조합을 모르는 상태의 밴은
"전체적으로 센 챔피언 지우기"밖에 안 된다.

```
ban_value_1(c) = base(c)   (+ 밴률·픽률을 참고 지표로 병기)
```

이 단계는 **추천의 근거가 약하다는 것을 화면에 명시한다.** 순위 하나를 내밀지 않고
기저 강도 상위 목록 + 실제 밴률을 나란히 보여주고 사용자가 고르게 한다.

**2차 밴 (스텝 9·10).** 여기가 이 기능의 핵심이다. 픽 4개가 이미 공개돼 있다.
목적은 내 이득을 올리는 것이 아니라 **상대가 다음 픽에서 얻을 최대 이득을 깎는 것**이다.

```
ban_value_2(c) = max score_opp(x)  −  max score_opp(x)
                 x ∈ 후보              x ∈ 후보 \ {c}
```

`score_opp` 는 위 픽 점수를 상대 진영 기준으로 계산한 값이다. 즉 c 를 지웠을 때
상대의 최선수가 얼마나 나빠지는가. 한 수 앞만 본다 — 그 이상은 오차가 신호를 덮는다.

## 표본 미달이면 점수를 내지 않는다

D13 이 이 기능을 미뤘던 이유가 이것이다. 기능 대신 **거부 규칙**으로 대체한다.

- 각 항은 근거 행의 원시 `games` 가 `analysis_config.min_sample` 미만이면 **드롭한다.
  0 으로 치지 않는다.** 0 으로 치면 "상성이 없다"가 되는데, 실제로는 "모른다"다
- 드롭된 항의 개수와 종류를 후보마다 표시한다 (`근거 3/5`)
- 근거가 하나도 없으면 점수 대신 **"판정 불가 · N경기 더 필요"**를 표시한다
- **드롭된 항이 있는 후보와 없는 후보를 한 축으로 정렬하지 않는다.** 근거가 적을수록
  극단값이 나오기 쉬워서, 섞어 정렬하면 표본 적은 후보가 상위를 점령한다.
  근거 완전성으로 먼저 묶고 그 안에서 점수 정렬한다

이건 `/gaps` 화면과 같은 원칙이다 — 모르는 것을 아는 척하지 않고, 무엇을 더 모아야
알 수 있는지를 알려준다.

## 화면

`/draft` 하나. 서버 왕복 한 번에 스텝 하나 (D22 의 판단대로 클라이언트 상태를 만들지 않는다).

- 상단: 스코프(`GLOBAL`/`CAREER`)·패치·슬롯. 세션 생성 시 고정하고 중간 변경 불가
- 중앙: 14스텝 진행 바. 현재 스텝 강조, 확정된 밴/픽 표시
- 하단: 후보 목록. 열은 `점수 / 근거 완전성 / 항별 분해 / 원시 경기 수`
- 후보 행을 펼치면 항별 기여도(base · 카운터 각각 · 시너지 · 구성)와 각 항의 표본 수

HTMX 로 후보 목록 조각만 교체한다. 점수 계산은 전부 서버.

## 완료 기준

- [ ] 14스텝이 `draft_step` 순서대로 진행되고, 임의 스텝 편집이 막힌다
- [ ] 한 세션에서 같은 챔피언이 밴과 픽에 중복으로 들어가지 않는다 (DB 제약으로)
- [ ] 커리어 초반 2밴 규칙으로는 시뮬이 돌지 않는다 — 항상 3밴이다
- [ ] 표본 미달 근거가 0 으로 치환되지 않고 드롭되며, 그 사실이 후보 행에 보인다
- [ ] 근거가 없는 후보에 점수가 아니라 "N경기 더 필요"가 뜬다
- [ ] 스텝 9 의 밴 추천이 스텝 1 의 밴 추천과 다른 목록을 낸다 (픽 4개를 반영하므로)
- [ ] 후보 행을 펼치면 점수가 항별로 분해되고, 합이 표시된 점수와 일치한다
- [ ] `GLOBAL` 세션에서는 패치 선택이 뜨지 않는다 (D24)
- [ ] 되돌리기가 마지막 스텝만 지운다

## 미해결

- **`Banpick.MultiNetwork` 해독** — 구조는 확인했다(D32). `Counters`·`Pairs` 가 50×50 행렬이고
  값도 학습돼 있다. 남은 것은 셀 인덱스 ↔ 챔피언 대응과 `A`/`B` 의 의미다.
  풀리면 상대 모델링과 우리 추정치의 교차 검증에 쓴다. 안 풀려도 시뮬은 성립한다
- **`pick_order` 검증** — D25. 통과하면 스텝별 픽 분포를 사전값으로 쓸 수 있다
- **가산 모델의 오차** — 항이 늘수록 과대추정될 가능성이 있다. 실제 경기로
  사후 검증할 수 있다: 과거 드래프트를 스텝 8 까지 재생하고 점수 상위 픽이
  실제로 승률이 높았는지 본다. 표본이 쌓이면 가중치를 조정한다

---

← [README](README.md)
