# 작업 형식

한 사람이 총괄하고, **범위가 좁고 판단이 필요 없는 검증만** 서브에이전트에 배분한다.

## 넘기지 않는 것

설계 판단 · 구현 · 문서 갱신 · 커밋 · 최종 결정.

이 프로젝트의 어려운 부분(카운터·시너지·2단 축소)은 결정들이 서로 얽혀 있어
한 사람 머릿속에 있어야 한다. 나눌 수 있는 부분(보일러플레이트)은 애초에 어렵지 않다.
D22 가 이미 짚었다 — **보일러플레이트에 에너지를 다 쓰고 정작 분석에 도달하지 못하는 것이 최대 위험**이고,
분업은 조율 비용으로 그 위험을 키운다.

## 넘기는 것

파일을 많이 읽어야 하지만 판단은 필요 없는 일.

| 상황 | 에이전트 |
|---|---|
| 코드를 막 썼다 | `code-reviewer` |
| 스키마·엔티티·쿼리 | `database-reviewer` |
| 오류가 삼켜지지 않았나 | `silent-failure-hunter` |
| 타입이 불변조건을 표현하나 | `type-design-analyzer` |
| 주석이 코드와 맞나 | `comment-analyzer` |
| 커밋 전 민감한 코드 | `security-reviewer` |
| 죽은 코드 제거 | `refactor-cleaner` |
| 빌드·타입 오류 | `build-error-resolver` |

## 문서 배분 — 이게 핵심이다

결정은 파일 하나당 하나다. 에이전트에게 **파일 경로를 그대로 열거해서** 준다.
역할별로 필요한 결정만 지정한다. 에이전트에게는 "`decisions/D20_*.md` 를 읽어라" 로 준다 (이제 grep이 필요 없다).

| 역할 | 문서 | 결정 |
|---|---|---|
| 파서 | `savefile.md` | `decisions/D18_*.md` 현재날짜 · `decisions/D20_*.md` 이름매칭 · `decisions/D26_*.md` 드래프트 · `decisions/D34_*.md` 골든파일의 한계 |
| 스키마·DB | `V1__init.sql` | `decisions/D07_*.md` · `decisions/D09_*.md` · `decisions/D14_*.md` · `decisions/D15_*.md` · `decisions/D22_*.md` · `decisions/D23_*.md` · `decisions/D24_*.md` · `decisions/D26_*.md` · `decisions/D28_*.md` |
| 적재 | `savefile.md` | `decisions/D08_*.md` 스크림 시점 · `decisions/D17_*.md` 저장주기 · `decisions/D20_*.md` · `decisions/D28_*.md` 백업파일 |
| 집계 | — | `decisions/D09_*.md` 표본 · `decisions/D10_*.md` 정렬 · `decisions/D14_*.md` 카운터 · `decisions/D15_*.md` 축소·감쇠 · `decisions/D19_*.md` 경기력 |
| 밴픽 | `banpick.md` | `decisions/D13_*.md` · `decisions/D24_*.md` 패치축 · `decisions/D26_*.md` 순서 · `decisions/D32_*.md` 게임AI |
| 화면 | `project.md`「디자인 요구」 | `decisions/D21_*.md` 화면구성 · `decisions/D09_*.md` · `decisions/D10_*.md` |

## 프롬프트에 반드시 넣는 것

1. **읽을 파일을 열거한다.** "저장소를 훑어라" 는 컨텍스트를 태운다. (의사결정 문서가 분할된 덕분에 이제 이 원칙을 실제로 지킬 수 있게 되었다)
2. **읽기 전용이면 명시한다.** "파일을 수정하지 마라"
3. **이미 아는 것을 알려준다.** 중복 보고를 막는다
4. **환경을 알려준다.** Spring Boot 4 / Hibernate 7 — Boot 3 자료는 안 맞는다
5. **추측 금지를 명시한다.** "근거가 없으면 미확인이라고 적어라"
6. **없으면 없다고 하라.** 억지로 발견을 만들지 않게

## 첫 배분 결과 (2026-08-25)

`code-reviewer` · `database-reviewer` · `silent-failure-hunter` 세 곳에 파서와 엔티티를 맡겼다.
합쳐 22만 토큰을 썼지만 총괄 쪽 컨텍스트에는 결론만 들어왔다.

**세 에이전트가 각자 다른 파일을 봤는데 결함의 성격이 같았다** — 전부
"표본에 없어서 골든 파일이 못 잡는 것"이었다(D34). 서로 다른 각도에서 같은 구멍을 짚은 것이
이 방식의 값어치였다. 한 사람이 한 번 훑어서는 세 번 다 나오지 않는다.

받은 지적 9건 중 8건을 수용했고, 1건은 심각도를 낮췄으며, 1건은 고칠 위치를 바꿨다.

## 테스트를 배분했을 때

**받은 테스트를 그대로 믿지 않는다.** 통과하는 테스트는 그 자체로는 아무것도 증명하지 않는다.
**그 테스트가 막으려는 버그를 실제로 코드에 넣고, 실패하는지 본다.** 통과하면 그건 테스트가 아니다.

실제로 두 번 걸렀다 — `tdd-guide` 가 만든 D20 테스트는 인덱스 매칭으로도 통과했고
(단언이 스키마 제약이 이미 강제하던 것이었다), D8 테스트는 이름과 달리 다른 값을 보고 있었다.
둘 다 다시 썼고, 다시 쓴 것을 변조로 검증했다.

**워처에서 세 번 더 걸렀다. 원인이 전부 같았다** (D40):

> **이 단언이 검증 대상 말고 다른 경로로도 만족되는가?**

- 재훑기 테스트가 워처를 켠 채로 파일을 만들어, 정상 CREATE 이벤트만으로 통과했다
- 정지 테스트가 취소할 타이머가 없는 상태에서 정지해, 취소 로직을 지워도 통과했다
- 생성 테스트가 `Files.writeString`(CREATE+MODIFY)을 써서 CREATE 구독을 지워도 통과했다

셋 다 "다른 경로" 를 막고 나서야 진짜 테스트가 됐다 — 워처를 끄고, 만료 전에 정지하고,
원자적 이동으로 바꿨다. **테스트를 배분할 때 이 질문을 요구 사항에 넣어라.**

## 수치 반복법은 운영 규모로 한 번 돌려본다

**단위 테스트는 그 크기에서 드러나는 것만 지킨다** (D48). Bradley-Terry 적합이 챔피언 2~5종
테스트 16개를 전부 통과한 채로 실제 데이터(40종 · 관측 19,200개)에서 수렴하지 않아 집계가
통째로 죽었다. 작은 표본에서는 반복이 몇 번 만에 끝나 문제가 생길 시간이 없었다.

반복법·수렴·수치 안정성이 걸린 코드에는 **운영 규모의 입력을 만들어 한 번 돌리는 테스트**를
같이 둔다. 실제 DB 가 아니라 같은 모양의 합성 데이터면 된다 — 씨앗을 고정하면 결정적이다.

그리고 **"끝났다" 를 순위나 대소로 단언하지 마라.** 그건 한 번 반복해도 만족된다.
그 방법이 무엇을 푸는지 그 정의를 단언으로 옮겨라 — Bradley-Terry 라면 우도방정식이다.

## 이미 끝난 경로에 새 일을 매달지 마라

D56 에서 팀 이름 붙이기를 `TeamRegistry.ensure()` 안에 뒀다. 테스트는 전부 통과했는데
운영에서 팀 56행이 **전부 이름 NULL** 이었다. `ensure()` 는 "경기를 새로 넣거나 팀 번호를
백필할 때" 만 불리는데, 그 둘이 이미 다 끝나 있었다.

테스트가 못 잡은 이유가 핵심이다. 단위 테스트는 그 메서드를 **직접** 불렀고, 통합 테스트는
**빈 DB에 처음 적재하는 경로**만 봤다. 운영의 실제 상태 — *아무것도 새로 넣을 것이 없는
재적재* — 를 아무도 재현하지 않았다.

**증분 기능을 추가할 때 물어라: 이 코드가 "할 일이 없는 상태" 에서도 도는가?**
누적을 다루는 앱에서는 그 상태가 예외가 아니라 **평상시**다.

## 결과를 받은 뒤

**서브에이전트의 보고를 그대로 믿지 않는다.** 지적을 받으면 해당 코드를 직접 확인하고
판단한다. 특히 "이론상 가능"한 지적과 "실제로 그 입력이 온다"는 지적을 구분한다 —
D34 가 그 차이를 비싸게 가르쳐줬다.


