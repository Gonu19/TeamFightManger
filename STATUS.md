> **답하는 질문:** 지금 무엇이 돌고, 무엇을 하는 중이고, 무엇이 막혀 있나
> **읽을 때:** 세션 시작 직후. 이 파일과 `decisions/README.md` 라우팅 표면 방향이 잡힌다
> **크기:** 3KB 이하로 유지한다. `RUNBOOK.md` 와 같은 한도이고, 둘을 합친 6KB 가
> 세션 시작의 정액 예산이다. 넘으면 내용이 `decisions/` 로 갈 때가 된 것이다

# 지금 상태 (2026-09-02)

## 도는 것

| 계층 | 상태 |
|---|---|
| `parser/` | 세이브 파싱 완료. 경기·팀·선수·**매치 일정** |
| `ingest/` | 적재 완료. 운영 DB 팀 56 · 공식 698건 · 선수 실명 |
| `analysis/` | 카운터·티어 + **출력 기반 쌍 효과**(D76). 승률 기반은 교차검증용 |
| `story/` | 사실→해석→창작→대조→저장. 팀·선수 실명. **플레이어 팀 켜짐** |
| `story/gallery/` | 게시판 20편. 기사와 독립 (D73). 이슈는 뺐다 (D74) |
| `web/` | 티어 · 챔피언 · **연대기(사이클)** · 갤러리. 넷이 같은 껍데기 (D79) |
| `draft/` | 비어 있음 (`banpick.md` 가 설계) |

테스트 **541개** (건너뜀 3 — 픽스처 세이브가 gitignore 라 이 PC 밖에서는 34).
마이그레이션 `V1`~`V14` **전부 적용됨**. 결정 **D1~D84**.

## 진행 중

**화면 계층은 섰다** — D77~D83. 통계 화면 · 감쇠 · 사이클 화면 · 챔피언 한글화 ·
생성을 요청 밖으로 · 커리어 드롭다운 · 집계 버튼(실측 1.5초).

**지금은 기사의 서술을 잡는 중이다 (D84).** 세부 전투에서 리그 맥락으로 옮겼다 —
우리 팀 관점 · 순위 변동 · 남은 경기를 태그로 주고, 선수 합계 표를 기사에도 준다.
**실모델로 다시 뽑아 보지 않았다.**

## 막힌 것

- **`.env` 는 기본 체크아웃에만 있다** (gitignore). 워크트리에서는 복사한다
- **`tools/` 실호출 스크립트가 임시 파일에만 있다.** opt-in 으로 만들어야 넣는다
- **Groq 무료 티어 TPM 8000.** 한 호출로 20편을 못 뽑는 이유다 (D74)
- **문턱 0.10σ · 반감기 2/12 는 고른 값이다** — 잰 것이 아니다 (`decisions/OPEN.md`)
- **슬롯 삭제가 없다.** `SaveWatcher` 가 `ENTRY_DELETE` 를 안 받아 유령 슬롯이 남는다.
  `CASCADE` 가 걸려 있어 `DELETE FROM save_slot` 한 줄이면 되는데 기능이 없다
- **대회 이름표가 없다.** `competitionName()` 이 항상 null 이라 프롬프트가 못 보는 것을
  강조하라고 시키고 있다. D56 방식의 시드 표가 필요하다 (D84)
- **실모델 성공 경로를 아직 못 봤다.** 확인한 것은 401 실패 경로뿐이다 (D81)

## 이 파일의 규칙

- **커밋할 때 같이 고친다.** 낡으면 없느니만 못하다
- 여기에는 **상태만** 쓴다. 이유는 `decisions/`, 구조는 `architecture.md` 다
