> **답하는 질문:** 지금 무엇이 돌고, 무엇을 하는 중이고, 무엇이 막혀 있나
> **읽을 때:** 세션 시작 직후. 이 파일과 `decisions/README.md` 라우팅 표면 방향이 잡힌다
> **크기:** 3KB 이하로 유지한다. `RUNBOOK.md` 와 같은 한도이고, 둘을 합친 6KB 가
> 세션 시작의 정액 예산이다. 넘으면 내용이 `decisions/` 로 갈 때가 된 것이다

# 지금 상태 (2026-08-31)

## 도는 것

| 계층 | 상태 |
|---|---|
| `parser/` | 세이브 파싱 완료. 경기·팀·선수·**매치 일정** |
| `ingest/` | 적재 완료. 운영 DB 팀 56 · 공식 698건 · 선수 실명 |
| `analysis/` | 카운터·티어 집계 완료. **시너지는 미착수** |
| `story/` | 사실→해석→창작→대조→저장. 팀·선수 실명. **플레이어 팀 켜짐** |
| `story/gallery/` | 기사 아래 게시판 20편 (D72). 조회수·추천수·개념글 (D71) |
| `web/` | 연대기 + 갤러리(`/story/{id}/gallery`). 통계 화면은 **미착수** |
| `draft/` | 비어 있음 (`banpick.md` 가 설계) |

테스트 **428개** (건너뜀 33 — 픽스처 세이브가 gitignore 라 이 PC 밖에서는 없다).
마이그레이션 `V1`~`V11` **전부 적용됨**. 결정 **D1~D72**.

## 진행 중

**레퍼런스 모드를 웹앱에 녹였다 (D72).** 버튼 셋 — 「최근 경기 기사」(호출 2)
「그날 총평」(호출 2) 「갤러리 만들기」(**호출 4**). 자동 생성은 안 한다.

갤러리가 이 계층의 새 축이다. **우리 매치 기사가 모드의 [이슈] 자리에 들어가고**
그 아래 짧은 글 20편이 유형 할당제(10종)로 붙는다. 세트 나열을 프롬프트가 아니라
구조로 푼 것 — 글 하나가 한 각도만 잡으므로 나열할 대상이 없다.

**갤러리는 아직 실물로 안 돌려봤다.** 테스트는 통과하지만 프롬프트가 실제 모델에서
어떻게 나오는지는 확인 전이다.

다음은 통계 화면이고, 그때 상단 탭과 루트 목적지를 `/tier` 로 바꾼다.

## 막힌 것

- **워크트리에 `.env` 가 없다** (gitignore). 기사를 뽑으려면 복사하거나
  `$env:TFM_GROQ_API_KEY` 로 넘긴다
- **`tools/` 실호출 스크립트가 임시 파일에만 있다.** opt-in 으로 만들어야 넣는다
- **Groq 무료 티어 TPM 8000.** 기사 한 편이 호출 2회라 둘째가 자주 429 다.
  **갤러리는 호출 4회**라 더 걸린다 — 조각을 나눈 이유의 절반이 이것이다 (D72)
- **`bootRun`·psql 은 사용자가 돌린다.** 테스트는 `$env:TFM_DB_PASSWORD` 로 에이전트도 된다

## 최근에 뒤집힌 것

`decisions/README.md` 의 **「오래된 문서를 믿기 전에 본다」** 로 옮겼다 —
세션마다 늘어 이 파일의 한도를 계속 밀어냈다.

## 이 파일의 규칙

- **커밋할 때 같이 고친다.** 낡으면 없느니만 못하다
- 여기에는 **상태만** 쓴다. 이유는 `decisions/`, 구조는 `architecture.md` 다
