---
title: 팀파이트 매니저 티어 분석 - 아키텍처
created: 2026-08-29
updated: 2026-08-29
para:
project:
areas: []
status:
tags: [teamfight-manager, architecture, design]
---

# 아키텍처

## 이 문서의 자리

세 문서가 다른 질문에 답한다. 겹쳐 쓰면 셋 다 늙는다.

| 문서 | 답하는 질문 |
|---|---|
| `structure.md` | 파일이 **어디** 있나 |
| `decision.md` | **왜** 그렇게 정했나 (D1~D58) |
| **`architecture.md`** | **경계가 어디**고, 무엇이 무엇에 기대나. 그리고 화면 지도 |

그래서 이 문서는 결정을 다시 설명하지 않고 **번호로 가리킨다.**

---

## 1. 계층과 의존 방향

```
  세이브 파일 (읽기 전용)
        │
        ▼
  parser/     Spring 의존 0 · DB 무관
        │     바이트 → 순수 DTO. 관대하다 — 구 데이터도 통과시킨다
        ▼
  ingest/     JPA
        │     DTO → DB. 엄격하다 — 형식이 아니면 버리거나 던진다 (D35)
        ▼
  analysis/   JdbcTemplate. 순수 계산 계층에는 Spring 의존이 없다
        │     행 → 감쇠·축소·강도 → 행 (D46: SQL 은 행만 꺼낸다)
        ▼
  story/      기사. analysis 와 ingest 를 읽는다
        │
        ▼
  web/        Thymeleaf + HTMX. 위 전부를 읽기만 한다
```

**화살표는 한 방향이다.** 지켜야 할 것 넷:

1. `analysis/` 는 `ingest/` **엔티티를 모른다.** DAO 가 행만 꺼내고, 전개·감쇠·축소는 순수 함수다 (D46). DB 없이 변조 검증이 가능해야 한다는 요구가 이 경계를 만들었다.
2. `story/` 는 `analysis/` 를 읽지만 **`analysis/` 는 `story/` 를 모른다.**
3. **LLM 출력은 절대 위로 올라가지 않는다.** 아래 2절이 이 규칙의 전부다.
4. `web/` 은 계산하지 않는다. 표본에 따른 시각적 약화·신뢰구간 폭·티어 뱃지까지 **서버가 계산해 내려보낸다** (`project.md` 디자인 요구).

---

## 2. 사실 · 해석 · 창작 — 가장 중요한 경계

이 앱은 세 종류의 값을 다룬다. **섞이면 프로젝트의 전제가 무너진다.**

| | 사실 | 해석 | 창작 |
|---|---|---|---|
| 무엇 | 세이브가 말한 것 | 집계가 계산한 것 | LLM 이 쓴 것 |
| 어디 | `match_record` · `team` · `athlete` | `champion_matchup` · `champion_performance` | `article` · `article_comment` |
| 재현 | 세이브를 다시 읽으면 같다 | 언제든 버리고 다시 만든다 | **재현 불가** |
| 규칙 | 덮지 않거나(팀 이름) 스냅샷 갱신(선수) | 통째로 재계산 | 근거와 함께 저장 |

**창작은 아래로 흐르지 않는다.**

- 기사는 별도 표에 살고, 집계는 그 표를 **조회하지 않는다.**
- 기사에 인용된 수치는 **생성 후 코드로 대조**한다. brief 에 없는 숫자면 플래그를 단다.
- 화면에서 기사 옆에 **사실 패널**(실제 스코어·밴픽·K/D/A)을 나란히 둔다.

`/tier` 에서 승률 옆에 신뢰구간을 병기하는 것과 같은 원칙이다 — **모르는 것과 지어낸 것을 아는 것처럼 보이게 하지 않는다.**

---

## 3. 이름의 출처

세이브는 이름을 거의 담지 않고 **참조**만 담는다. 규칙을 한곳에 모은다.

| 대상 | 1순위 | 2순위 | 안 쓰는 것 |
|---|---|---|---|
| 팀 | 세이브 `TeamInfo` (D56) | `team_name_seed` 로 키 해석 | `common.data` (D57 — 키가 안 맞는다) |
| 선수 | 세이브 `Athlete.Name` 의 `` `N `` (D58) | `athlete_name_seed` (에셋 추출) | — |
| 챔피언 | 시드 40종 (V3) | — | — |

**두 규칙이 반대인 곳이 하나 있다.**

- **팀 이름은 덮지 않는다.** 그 커리어의 기록이라, 나중에 프로필을 커스터마이즈해도 지나간 표가 바뀌면 안 된다 (D55).
- **선수 속성은 덮는다.** 나이·연봉·팬 수는 실제로 변하는 값이고 세이브엔 지금 값만 있다 (D58).

그래서 **"그때 연봉" 을 기사에 쓸 수 없다.** 이력이 없다.

**해석 결과와 원본 사실을 나눠 저장한다** — `team.name`(해석) / `team.name_key`(사실), `athlete.name` / `athlete.name_index`. 시드가 틀려도 **재적재 없이 시드만 고치면 된다.**

---

## 4. 분석 계층 — 무엇을 빼고 무엇을 남기나

**한 문장으로.** raw 승률에는 **챔피언 자체 강도가 섞여 있다.** 그걸 빼고 남은 것이 상성(카운터)이고 시너지다. 티어는 반대로 **그 강도 자체가 답**이라 빼지 않는다 (D14·D50).

### 파이프라인

```
  match_observation  (승리 팀 4 / 패배 팀 4)          ← DAO 는 행만 꺼낸다 (D46)
        │
   ┌────┴─────────────────────────────────────────────────────┐
   │ 전개                                                     │
   │   카운터 : 승4 × 패4 = 16관측/경기   (적팀 쌍, D4)        │
   │   티어   :            8관측/경기   (출전 + 승패)          │
   │   시너지 : 팀당 C(4,2)=6 → 12관측/경기  ← 아직 없다       │
   └────┬─────────────────────────────────────────────────────┘
        │
        ▼  감쇠   w = 0.5^(자기변경/2) × 0.5^(경과패치/2)      D15a·D42·D53
        │
        ▼  누적   Σw · Σw·win   (원시 카운트도 함께 남긴다)
        │
        ▼  강도   Bradley-Terry MM 반복 + 기하평균 정규화      D43·D48
        │
        ▼  기대   expected = s_A / (s_A + s_B)                 D14
        │
        ▼  축소   1단 → expected (k₀) · 2단 → 전체 누적 (k₁)   D15b
        │
        ▼  이득   effect = adjusted − expected
```

**티어만 이 줄기에서 갈라진다.** 강도를 빼지 않고 축소 목표도 0.5 다 — `PerformanceCalculator` 의 시그니처가 강도를 아예 받지 않아 실수로 섞을 수 없게 해뒀다 (D50).

### 경계가 왜 그렇게 그어졌나

| 규칙 | 안 지키면 |
|---|---|
| 전개·감쇠·축소를 **SQL 에 넣지 않는다** (D46) | 조인 하나가 어긋나 아군끼리 쌍이 생겨도 결과는 여전히 0~1 의 그럴듯한 승률이다. 순수 함수여야 **DB 없이 변조로** 잡힌다 |
| 축소 목표가 0.5 가 아니라 **기대 승률** | 약한 챔피언의 정당한 열세까지 중앙으로 밀려, **걷어내려던 강도가 잔차에 다시 섞인다** |
| 감쇠 두 인자는 "경기 **이후** 흐른 양" (D45) | 뒤집으면 최신 경기가 가장 세게 깎여 정확히 반대로 동작하는데, 값은 여전히 (0,1] 이라 눈으로 못 잡는다 |
| 표본 기준선으로 **계산에서 거르지 않는다** (D24) | 미달 조합도 계산해 저장하고 노출은 화면이 정한다. 원시 카운트를 들고 있어야 "12경기 · 추정 54.1%" 가 화면에 뜬다 |
| BT 는 매 반복 **정규화** (D48) | 우도가 거의 스케일 불변이라 비율은 맞는데 절대 크기가 미끄러진다. 작은 표본에서는 반복이 몇 번에 끝나 문제가 안 보인다 |

### 한 바퀴에 도는 이유

카운터와 티어를 같은 `AggregationService` 한 바퀴에 둔다. **같은 관측을 쓰기 때문이다.** 따로 돌리면 경기를 두 번 읽는 것도 문제지만, 더 나쁜 것은 **두 표가 서로 다른 시점의 데이터로 만들어질 수 있다**는 것이다. 화면은 둘을 나란히 놓는데(D21) 어긋난 것을 아무도 못 알아챈다.

- **스코프는 `CAREER` 만** (D53) × **스크림 포함/제외 두 벌** (D47) → 슬롯마다 경기를 두 번만 읽는다
- 쓰기는 **업서트 + `agg_run` 한 트랜잭션.** 도중에 죽으면 아무것도 안 바뀐다
- 임계값은 `analysis_config` 여섯 키. **코드에 기본값을 두지 않는다** (D44)

### 시너지 — 구조는 베낄 수 있고, 기대값은 못 베낀다

`counter/` 와 **공유하는 것**: 전개 → 감쇠 → 누적 → 축소, 유효표본(ess), 설정, 스코프, 쓰기 경로. 그래서 "구조가 거의 같아 빠르다" 는 말은 맞다.

**다른 것 하나가 전부다 — 기대값의 정의.**

카운터의 기대값은 **머리를 맞댄 둘 사이의** 승률(`s_A/(s_A+s_B)`)이다. 시너지는 두 챔피언이 **같은 팀**이라 그런 것이 없고, 관측 단위도 "그 팀이 이겼나" 다.

| | 기대값 | 위험 |
|---|---|---|
| **A. 팀 강도 대 팀 강도** | `s_T = ⊕ s_i`, `expected = s_T/(s_T+s_O)` | 결합 연산 `⊕`(합·곱·평균)에 **근거가 아직 없다** |
| B. 두 챔피언 개별 기대의 결합 | 나머지 둘과 상대 구성을 무시 | 상대가 약했던 편차가 시너지로 둔갑한다 |
| C. 전체 평균 0.5 로 축소 | 가장 단순 | **D14 가 이미 겪은 실패** — 센 조합이 상단을 도배한다 |

**C 는 안 된다.** D14 가 카운터에서 정확히 그 실패를 겪었고, 그래서 이 프로젝트가 BT 를 도입했다.

**A 가 유력하지만 결합 연산을 지금 정하지 않는다.** 재고 정한다 — 방법은 카운터에서 쓴 것과 같은 **분산 분해**다. 관측 분산을 `조합 고유 시너지 / 챔피언 강도 / 노이즈` 로 쪼개서 **시너지 성분이 존재하기는 하는지 먼저 본다.** D14 에서 페어 고유 상성은 분산의 **3%** 였다. 시너지가 그보다 작으면 **화면을 만들 값어치가 없다** — 그 결론도 정당한 결과다.

**표본** (D11 실측):

| 크기 | 가능 | 관측 | 10경기 이상 | 최다 | 경기당 관측 |
|---|---|---|---|---|---|
| 2인 | 780 | 761 (98%) | 475 | 128회 | 12 |
| 3인 | 9,880 | 3,914 (40%) | 114 | 36회 | 8 |
| 4인 | 91,390 | 1,976 (2%) | **0** | **9회** | — |
| 역할군 4인 | **70** | — | — | — | 2 |

**4인은 챔피언 조합으로 만들지 않는다.** 10경기 이상이 0개다 — 통계가 아니라 경기 목록이다. 역할군 4자리 중복조합 70가지로 대체하고(D11), 밴픽에서 실제로 궁금한 것도 특정 4챔 조합이 아니라 **구성 균형**이다. DB 가 `synergy_max_size ∈ {2,3}` 으로 강제한다.

### 아직 안 정한 것 (분석)

- **시너지 기대값의 결합 연산** — 분산 분해가 먼저다. 성분이 없으면 화면도 없다
- **티어 등급 컷라인** — 관측 σ 3.32%p 인데 표본 오차만으로 ±3.4%p 다. `tier_report.sql` 질의 9·10 이 진짜 강도 차이를 역산해야 정해진다 (D51)
- **`prior_strength_k0 = 24` 는 카운터용이다.** 티어에도 맞는지 **확인된 적이 없다**
- **경기력 z값** (D19) — 회귀식이 미결이라 NULL 이다
- **집계 트리거** — 아래 8절

---

## 5. 화면 지도

### 왜 통계와 기사를 섞지 않나

**기사는 통계의 반대 방향이다.** 통계는 표본을 모아 불확실성을 줄이고, 기사는 한 경기를 확대해 이야기로 만든다. 한 화면에 섞으면 둘 다 나빠진다 — 티어표 옆의 기사는 근거처럼 보이고, 기사 본문에 낀 신뢰구간은 읽히지 않는다.

**분리하되 양방향으로 잇는다.**

```
  ┌─────────────── 통계 ────────────────┐      ┌──── 연대기 ────┐
  │                                     │      │                │
  │  /tier ──────► /champion/{code}     │◄─────│  /story        │
  │    │                  ▲             │      │     │          │
  │    │                  │             │      │     ▼          │
  │    ├──► /synergy      │             │◄─────│  /story/{id}   │
  │    │                  │             │      │   기사+댓글     │
  │    ├──► /gaps ────────┘             │      │   +사실 패널    │
  │    │                                │      │                │
  │    ├──► /teams ──► /team/{id}       │◄─────│                │
  │    │                                │      └────────────────┘
  │    └──► /draft ◄────────────────────┘             │
  │           (팀 성향을 입력으로)        │             │
  └─────────────────────────────────────┘             │
              ▲                                       │
              └───── 좁은 링크 한 줄만 ─────────────────┘
```

- **나가는 링크가 값어치다.** 기사 속 챔피언·팀·선수를 누르면 그 통계로 간다. 지금 통계 화면의 약점이 "왜 이걸 봐야 하는지" 가 없다는 것인데, **기사가 그 이유를 준다.**
- **들어오는 링크는 좁게.** `/team/{id}` 에 "이 팀 기사" 목록 한 줄, `/champion/{code}` 에 "이 챔피언이 화제였던 경기" 정도. **통계 화면 안에 기사 본문은 넣지 않는다.**
- 상단 탭: `티어 · 시너지 · 표본 공백 · 팀 · 밴픽 · 연대기` (6개)

### 화면별 책임

| 경로 | 답하는 질문 | 상태 |
|---|---|---|
| `/tier` | 지금 무엇이 센가 · **얼마나 믿을 수 있나** | 설계 완료 (D21) |
| `/champion/{code}` | 이 챔피언은 누구에게 강한가 | 설계 완료 |
| `/synergy` | 어떤 조합이 붙는가 | 미착수 — **기대값 미정** (4절) |
| `/gaps` | **무엇을 모르는가 · 몇 판이면 아는가** | 설계 완료 |
| `/teams` · `/team/{id}` | 상대는 무엇을 뽑고 무엇을 미는가 | 신규 (D54~D56) |
| `/draft` | 지금 무엇을 뽑아야 하나 | `banpick.md` |
| `/story` · `/story/{id}` | **무슨 일이 있었나** | 이 문서 6절 |

**공통 규칙** — 상단 바에 커리어(슬롯) 선택·패치 기준·스크림 토글이 고정. `GLOBAL/CAREER` 배지 자리를 **커리어 선택이 대신한다** (D53 으로 GLOBAL 생산이 멈춰 표시할 것이 하나뿐이다).

**새 커리어 1일차에는 통계에 볼 것이 없고 기사만 있다.** 앱이 처음부터 쓸모 있어지는 것은 부수 효과가 아니라 노린 것이다.

---

## 6. `story/` 설계

```
story/
  MatchBrief        스케줄 1건의 사실만 모은 순수 레코드 (LLM 을 모른다)
  BriefRenderer     MatchBrief → 텍스트
  Notability        이 매치가 얼마나 중요한가 → 분량을 정한다
  StoryClient       인터페이스 하나. Groq · Cerebras · 로컬이 구현
  FactCheck         생성된 기사의 숫자를 brief 와 대조
  dao/              article · article_comment
```

### 단위는 스케줄이다

`MatchSchedule` 190건이 `Round · BlueTeam · RedTeam · Date · Competition · 스코어 · 킬` 을 준다. **기사 한 편 = 스케줄 1건**(세트 묶음)이다. 세트는 기사 안의 문단이 된다.

### `MatchBrief` — 우리가 모드보다 나은 지점

모드는 **최근 시리즈 1건**만 안다. 우리는 DB 에 전 시즌이 있으므로 brief 에 이런 것들이 정확히 들어간다:

- 그 경기의 밴픽·세트별 결과·선수별 K/D/A (사실)
- **리그 순위와 그 경기의 순위 영향** (계산)
- **라이벌 전적** — 표본과 함께. 10세트 미만이면 "판정 불가" 로 넣는다
- **양 팀 픽 성향** — 빈도 기준이라 표본 필터 없음
- **패치 이력** — 그 시점 직전 패치가 있었다면 메타 기사가 된다

brief 는 **순수 레코드**다. LLM 도 HTTP 도 모른다. 그래서 DB 없이 테스트되고, 텍스트 렌더링과 따로 검증된다.

### `Notability` — 분량을 정하는 축

모드의 `주목도 n/20` 과 같은 발상이지만, 우리는 **글 20개**가 아니라 **기사 1편의 길이와 댓글 수**를 정한다. 입력은 넷:

`내 팀 경기인가` · `순위 싸움인가` · `라이벌인가` · `업셋인가(약팀이 강팀을 이겼나)`

### 프롬프트 — 두 목소리로 쪼갠다

모드는 커뮤니티 유저 **한 목소리**다(디시체·날조·초성어). 우리는 둘이고, **그 분리가 설계의 핵심**이다.

| | 기사 | 댓글 |
|---|---|---|
| 화자 | 매체 | 독자 |
| 사실 | **brief 밖 수치 금지** | 과장·날조 허용 |
| 문체 | 리드 + 본문 | 커뮤니티 말투 |
| 검증 | 숫자 대조 | 안 함 |

**사실은 기사가 지고, 감정은 댓글이 진다.** 모드는 둘이 한 덩어리라 "재밌지만 틀린 글" 밖에 안 나오는데, 우리는 재미를 댓글로 몰아내고 기사를 정직하게 유지할 수 있다.

출력 스키마:

```
{ headline, dek, body[], quotes[], comments[] }
```

### 기사 종류

| 종류 | 언제 | 통계와의 관계 |
|---|---|---|
| 경기 리포트 | 내 팀 경기 | 밴픽·상성 이득을 근거로 |
| 단신 | 같은 라운드 다른 경기 | 짧게 |
| 특집 | 승격·강등·우승 | 시즌 흐름 |
| **메타 기사** | **패치가 있었을 때** | **여기가 통계의 자리다** |

메타 기사가 이 앱만 쓸 수 있는 재료다 — `LightningMage` 가 마지막 패치에서 `magic -55` 를 받았고 그 승률의 63%가 하향 이전 데이터라는 것(D53 실측)은 게임도 커뮤니티도 못 쓴다.

### 가상 인용

게임에 선수 발화가 없다. 기사에 코멘트를 넣으려면 지어내야 한다. **허용하되 화면에서 "가상 인용" 으로 표시한다.** 금지하면 기사가 밋밋해지고, 구분 없이 허용하면 사실과 섞인다. 티어 뱃지의 `컷라인 미정`, 시드 없는 팀 이름 비우기와 같은 처리다.

### 안전

팀·선수 이름이 사용자 커스텀이라 프롬프트에 들어가고 그대로 돌아 나온다. 로컬 단일 사용자라 위험은 낮지만 **출력은 텍스트로만 렌더한다**(HTML 이스케이프). 모드는 `innerHTML` 로 넣는데 우리는 안 그런다.

---

## 7. 외부 의존

### 가져오는 것

| | 무엇 | 왜 |
|---|---|---|
| **Spring AI 2.0** | LLM 클라이언트 | 2026-06-12 GA · Boot 4.0/4.1 · FW 7.0 · Jackson 3 베이스라인 — 우리 스택과 정확히 맞는다. `base-url` + `completions-path` 로 **Groq · Cerebras · 로컬을 같은 클라이언트로** 받는다 |
| **htmx-spring-boot** | `@HxRequest` · Thymeleaf 다이얼렉트 | "표 본문 조각만 교체" 를 그대로 지원 (D22) |
| htmx | 스크립트 태그 하나 | 프론트 빌드 도구를 두지 않는다 |

**직접 짜는 쪽도 봤다.** `RestClient` 로 120줄이면 되고 의존성이 0이라 D41(Security 제거) 정신엔 맞는데, **JSON 스키마 강제와 파싱 복구**를 손으로 짜면 그게 정확히 D22 가 최대 위험이라 부른 보일러플레이트다. LangChain4j 도 Boot 4 스타터가 있지만 호환 이슈 보고가 있었고, 우리가 쓸 기능은 chat completion 한 번뿐이라 스택 정합성이 나은 쪽을 골랐다.

**D31 을 적용한다.** Boot 4 가 자동설정을 기술별 모듈로 쪼개서 Flyway 가 **로그 한 줄 없이** 안 돌았다. 스타터가 실제로 도는지 확인하는 테스트를 붙이기 전에는 신뢰하지 않는다. `htmx-spring-boot` 의 Boot 4 지원은 **아직 미확인**이다.

### 안 가져오는 것 — 이유를 남긴다

| 안 쓰는 것 | 이유 |
|---|---|
| CSS 프레임워크 (Pico 등) | 밀도 우선·1280px·다크 단일이라 classless CSS 는 방해다 |
| 차트 라이브러리 | 신뢰구간·스파크라인은 **서버가 계산해 내려보낸다**. 인라인 SVG 로 충분 |
| 통계 라이브러리 (Commons Math 등) | BT·2단 축소는 이미 손으로 짰고 **변조로 검증**됐다. 갈아끼우면 그 검증을 버린다 |
| Testcontainers | D23 — 이 PC 에서 Docker 가 기동되지 않는다 |
| Spring Security | D41 — 로컬 단일 사용자라 인증할 상대가 없다 |

### API 제공자 — Groq 기본

| | 무료 | 한도 | 구조화 출력 | 카드 |
|---|---|---|---|---|
| **Groq (기본)** | **진짜 무료** | 14,400 req/day · 30K TPM | 지원 | 불필요 |
| Cerebras (옵션) | 트라이얼만 | $5 / 30일 · 5 RPM | `strict:true` | 이후 필요 |
| 로컬 (폴백) | 무료 | RTX 3050 8GB | 모델에 따라 | — |

**계산.** 매치당 입력 ~6K + 출력 ~4K = **10K 토큰**. 커리어 100매치 = **1M 토큰**.

- Groq 30K TPM → 분당 3매치, **100매치 백필이 약 35분에 공짜로** 끝난다
- Cerebras 트라이얼(1M TPD)은 **백필 한 번이 하루 한도를 정확히 태운다** (무료 티어가 2026-07-21 폐지됐다)
- 로컬은 8GB VRAM 이라 Qwen3-8B(5.4GB)까지고, 제일 좋은 30B-A3B(17.3GB)는 못 올린다. 매치당 1~2분

**속도가 중요한 진짜 이유는 백필이 아니라 프롬프트 반복이다.** 말투·분량·스키마 준수를 맞추려면 수십 번 돌려야 하는데, 로컬이 한 번에 1~2분이면 그 반복이 몇 시간짜리가 되고 결국 대충 맞추고 끝나게 된다.

**셋 다 OpenAI 호환이라 `base-url` 만 다르다.** 이 선택은 설정 한 줄로 뒤집힌다.

**키는 환경변수로만 받는다** — `TFM_DB_PASSWORD` 와 같은 방식이다. 코드·설정 파일·커밋에 절대 들어가지 않는다.

**외부 전송을 명시한다.** 켜는 순간 팀명·선수명·경기 기록이 밖으로 나간다. 개인정보는 아니지만 로컬 전용이던 앱의 성격이 바뀌는 것이라 **기본은 꺼져 있고 화면에 표시한다.**

---

## 8. 실패 방식과 방어선

이 프로젝트가 가장 경계하는 것은 **조용한 실패**다 — Flyway 가 로그 한 줄 없이 안 돈 것(D31), 집계가 실패했는데 화면에 이전 결과가 남는 것, 워처가 안 도는데 "정상 기동" 으로 보이는 것.

계층마다 정책이 다르다. **무엇이 본체인가**가 기준이다.

| 계층 | 실패하면 | 왜 |
|---|---|---|
| `parser/` | **던진다** | NRBF 는 틀려도 예외 없이 잘못된 값을 낸다. 골든 파일이 유일한 방어선 |
| `ingest/` (경기) | **파일 단위 롤백** | 부분 적재는 "적재 건수가 파서와 일치" 를 깬다 |
| `ingest/` (이름·선수) | **WARN 후 계속** | 표시용이고 경기가 본체다. 결과는 화면에 이름 없는 팀으로 드러난다 |
| `analysis/` | **던진다** | 집계 실패가 성공과 구별되지 않으면 낡은 표가 그럴듯하게 남는다 |
| `story/` | **기사 없음** | 앱은 살아야 한다. 없는 것이 화면에 그대로 보인다 |

**삼키는 것과 다른 점은 "결과가 드러나는가" 다.** WARN 을 남기고 화면에 빈자리가 보이면 조용한 실패가 아니다.

### 증분 기능의 함정

**"할 일이 없는 상태에서도 도는가" 를 물어야 한다** (`workflow.md`). D56 에서 팀 이름 붙이기를 이미 끝난 경로에 매달았다가 운영에서 56행 전부 이름이 비었다. 테스트는 전부 통과한 상태였다 — 단위 테스트는 그 메서드를 직접 불렀고, 통합 테스트는 빈 DB에 처음 적재하는 경로만 봤다.

**누적을 다루는 앱에서 "새로 넣을 것이 없는 상태" 는 예외가 아니라 평상시다.**

### 아직 안 정한 것

- **집계 트리거** — `run()` 을 아무도 안 부른다(플래그 제외). 언제 돌릴지, 그리고 **무엇이 끝난 뒤에** 돌릴지가 미결이다. 실측상 집계가 따라잡기 적재보다 항상 먼저 끝난다
- **기사 생성 트리거** — 같은 문제다. 적재 직후인지, 화면 요청 때인지, 수동인지
- **`ApplicationRunner` 실행 순서는 정의되어 있지 않다.** `tfm.reingest-on-start` 와 `tfm.aggregate-on-start` 를 같이 켜면 안 된다

---

## 9. 뒤집힐 조건

| 결정 | 뒤집히는 때 |
|---|---|
| 통계와 기사 분리 | 기사가 통계의 진입점으로만 쓰이는 게 확인되면 `/tier` 상단에 요약을 붙일 수 있다 |
| Groq 기본 | 무료 티어가 바뀌거나, 한국어 품질이 모자라면 Cerebras·Gemini 로. 설정 한 줄이다 |
| Spring AI 채택 | 스타터가 Boot 4.1 에서 안 돌면 `RestClient` 직접 구현으로 내려간다 |
| 팀 이름 시드 | 게임 로컬라이제이션에서 키→이름을 뽑아내면 시드가 그것으로 대체된다 |
| 가상 인용 허용 | 사실과 헷갈린다는 신호가 나오면 금지로 바꾼다 |
| 선수 속성 덮어쓰기 | 세이브에 이력이 생기면 스냅샷이 아니라 시계열로 바뀐다 |

---

[[teamFighterManger.README]]
