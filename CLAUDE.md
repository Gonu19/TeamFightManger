# TeamFighter — 에이전트 가이드

## 구조 규칙
- `decision.md` (단일 파일) 폐기됨
- 설계 결정은 `decisions/` 디렉토리에 1결정 1파일로 분리 저장

## 결정(Decision) 탐색 순서
1. `decisions/` 디렉토리 목록 조회 (파일명 = 제목)
2. 필요한 개별 파일만 선별적 열람
3. 주제 식별 불가 시: `decisions/README.md` 상단 라우팅 표 참고
4. 미결 사항: `decisions/OPEN.md` 참고
*주의: 라우팅 표가 지정한 만큼만 읽는다. 표에 없는 파일을 3개 넘게 뒤지고 있다면 라우팅 표가 잘못된 것이다 — 표를 고쳐라.*

## 핵심 문서 맵핑
- `structure.md`: 파일 및 컴포넌트 위치
- `architecture.md`: 시스템 경계 및 아키텍처 의존성
- `decisions/`: 의사결정 근거 (D1~D62)
- `decisions/OPEN.md`: 미결 과제
- `savefile.md`: 세이브 파일 구조 명세
- `workflow.md`: 에이전트 역할 및 프롬프트 규칙

## 새 결정(Decision) 추가 규칙
- 명명 규칙: `decisions/D<고유번호>_<제목-하이픈>.md` (번호 재사용 금지)
- 목차 갱신: `decisions/README.md` 결정 목록에 항목 추가
- 필수 내용: 문서 본문에 **뒤집힐 조건** 반드시 명시
- 수정 규칙: 원본 문서 수정/삭제 금지. 새 번호 발급 후 "D<n>을 고친다"로 연결

## 절대 금지 사항
- 적용 완료된 Flyway 마이그레이션(V*.sql) 주석의 `decision.md`는 옛 경로이며 절대 수정하지 않는다 (체크섬 깨짐 방지)
- 적용 완료된 Flyway 마이그레이션(`V1`~`V7`) 파일 수정 금지 (주석 변경 시 체크섬 오류)
- `git push` 실행 금지 (원격 푸시는 사용자 수동 처리)
