-- 이슈를 걷어낸다 (2026-08-31, decisions/D74_*.md)
--
-- V12 가 모드의 [🔥 팀파 이슈] 사이드바를 들여왔다. 하루 만에 뺀다 — <b>값이 아니라
-- 값을 얻는 비용이 문제였다.</b>
--
-- 이슈는 모델 호출 하나를 통째로 더 쓴다(출력 4,000토큰). 무료 티어의 분당 한도가
-- 8,000 이라 그 하나가 페이지 전체를 1분 넘게 늘렸다. 게시판이 뜨는 시간이 곧
-- 이 기능의 값어치인데, 사이드바 여섯 줄을 위해 그걸 배로 늘린 셈이다.
--
-- 표를 남겨두지 않는다. 안 채워지는 표는 다음 사람이 읽을 때 "왜 비어 있지" 가 되고,
-- 그 답이 코드 어디에도 없다. 되살릴 일이 생기면 V12 를 그대로 다시 쓰면 된다.

SET client_encoding = 'UTF8';

DROP TABLE IF EXISTS gallery_issue;
DROP TYPE IF EXISTS gallery_issue_category;
