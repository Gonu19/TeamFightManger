---
title: 팀파이트 매니저 티어 분석 - 설계 결정 기록
created: 2026-08-10
updated: 2026-08-10
para:
project:
areas: []
status:
tags: [teamfight-manager, decision-log, data-analysis, database]
---

# 설계 결정 기록

무엇을 왜 그렇게 정했는지, 그리고 어떤 과정을 거쳐 거기 도달했는지 남긴다.
각 항목에 **뒤집힐 조건**을 같이 적는다. 근거가 사라지면 결정도 다시 봐야 한다.

수치는 전부 실제 세이브 파일 3개(공식 800경기 + 스크림 407경기 = 1,207경기, 챔피언 40종)를
파싱해서 측정한 값이다. 측정 스크립트는 `tools/` 에 있다.

---

## 과정 요약

처음 출발점은 "경기 결과 스크린샷을 OCR로 읽어 티어표를 만든다"였다.
그런데 세이브 파일을 열어보니 게임이 이미 모든 것을 구조화해서 저장하고 있었고,
그 순간 프로젝트의 성격이 **수집 문제에서 분석 문제로** 바뀌었다.

이후 결정은 대부분 "게임이 이미 해주는 것"과 "게임이 못 하는 것"을 가르는 작업이었다.

```
OCR 전제 설계
   ↓ 세이브 파일이 평문 BinaryFormatter임을 확인
세이브 파싱으로 전환
   ↓ 4v4, 라인 없음, 역할군 5종 확인
스키마 전면 수정
   ↓ 세이브가 현재 시즌만 보관함을 확인
"시즌 누적"이 프로젝트의 핵심 가치가 됨
   ↓ 패치가 커리어마다 고유 생성됨을 확인
패치 처리를 챔피언 버전 기준으로 전환
   ↓ 효과 크기와 조합 집중도를 측정
표본 기준 10경기 + 축소추정 정렬로 확정
```

---

## D1. 수집 방식: OCR → 세이브 파일 파싱

**상황.** 최초 기획은 경기 결과 스크린샷 OCR이었다.

**확인한 것.** `%USERPROFILE%\AppData\LocalLow\samoyed\Teamfight Manager\slot_*.tfm` 가
.NET BinaryFormatter 평문이었다. 암호화도 압축도 없고 필드명이 파일에 그대로 있었다.
`GameStat` 안에 `BlueBan / BluePick / RedBan / RedPick / WinTeam` 이 전부 들어 있었다.

**근거.**

| | OCR | 세이브 파싱 |
|---|---|---|
| 경기 1건 확보 비용 | 스크린샷 1장 + 사람 확인 1회 | 0 (이미 저장돼 있음) |
| 과거 데이터 | 없음. 0건에서 시작 | 슬롯 3개에 1,207경기가 이미 존재 |
| 정확도 | 100% 불가, 확인 단계 영구 필요 | 원본 그대로 |
| 챔피언 인식 | 초상화 아이콘이라 OCR이 아니라 이미지 매칭 필요 | 문자열로 저장됨 |

**결정.** 세이브 파일 파싱. OCR 관련 테이블(`ingest_image`, `ingest_field_review`,
`champion_alias`, `is_confirmed`)은 전부 삭제.

**뒤집힐 조건.** 게임 업데이트로 세이브가 암호화되면 OCR로 되돌아가야 한다.

---

## D2. 모드(Mod)를 쓰지 않는다

**검토.** 팀파이트 매니저 **2**에는 공식 모드 SDK가 있다(Rust 네이티브 DLL, 세이브 데이터 읽기 훅).

**근거.** 현재 플레이 중인 것은 1편이다(세이브 폴더명 `Teamfight Manager`). 1편에 모드를
붙이려면 BepInEx/Harmony 주입이 필요한데, **모드로 얻을 데이터가 이미 세이브에 다 있다.**
비용은 크고 이득은 0이다.

**결정.** 모드 사용 안 함.

**뒤집힐 조건.** 2편으로 이주하면 공식 SDK가 있으므로 재검토. 특히 경기 중 실시간 데이터가
필요해지면 모드가 유일한 방법이다.

---

## D3. 파서는 C# 대신 직접 구현

**상황.** `Scripts.dll` 을 참조하면 `BinaryFormatter.Deserialize()` 한 줄로 끝난다.

**근거.** 개발 PC에 .NET SDK가 없었다(Python 3.13, Node 24만 있음). 그리고 BinaryFormatter는
.NET 9에서 제거돼서 .NET Framework 4.8이나 .NET 8로 고정해야 한다. MS-NRBF 포맷은 문서화돼
있고, 실제로 파이썬 파서를 300줄로 구현해서 3슬롯 전부 파싱에 성공했다(0.39초).

**결정.** NRBF 파서를 직접 구현한다. 프로토타입은 Python(`tools/nrbf.py`), 제품은 TypeScript로 포팅.
게임 DLL에 의존하지 않으므로 배포도 자유롭다.

**뒤집힐 조건.** 포팅 비용이 예상보다 크면 Python 워커를 별도 프로세스로 두는 것도 가능하다.

---

## D4. 4v4, 라인 개념 없음

**확인한 것.** 289경기 전부 예외 없이 팀당 픽 4 / 밴 3 / 경기당 챔피언 8명.
픽 리스트는 순서 있는 배열일 뿐 포지션 정보가 없다.

**결정.** `lane` ENUM(TOP/JUNGLE/MID/BOT/SUPPORT)을 삭제하고 `pick_order`(1~4),
`ban_order`(1~3)로 대체. **라인별 티어표는 이 게임에서 만들 수 없다.**

---

## D5. 역할군 5종, 챔피언 속성으로 취급

**확인한 것.** `ChampionCategory` enum = 전사 / 원거리 / 마법사 / 전투 보조 / 암살자 (+ 전체).
게임 로컬라이제이션 JSON에서 확인.

**핵심 구분.** 역할군은 **챔피언 고유 속성**이지 경기 슬롯이 아니다. 라인은 "이번 경기에
미드를 갔다"지만 역할군은 "닌자는 암살자다"로 경기와 무관하게 고정이다.

**결정.**
- `match_participant` 에 역할군을 넣지 않는다. `champion` 테이블에 두고 조인한다
- 티어표는 전체 한 묶음 + 역할군 필터
- 챔피언→역할군 매핑은 세이브에 없다. 게임 에셋에 있지만 40종뿐이고 정적 데이터라
  **시드 파일로 손으로 만든다.** 에셋 추출 자동화는 비용 대비 이득이 없다

---

## D6. 프로젝트의 핵심 가치는 "시즌 누적"

**확인한 것.** 세이브는 **현재 시즌 경기만** 보관한다. 2026 슬롯에는 2026시즌 278경기만 있고
2025시즌은 없다. 시즌이 넘어가면 이전 경기 기록이 사라진다.

**함의.** 게임은 시즌을 넘어 누적하지 못한다. 웹이 그걸 한다.
그리고 이건 **수집이 지속적이어야 한다**는 뜻이다. 수동 업로드 방식이면 시즌 롤오버를
한 번 놓치는 순간 그 시즌은 영영 복구할 수 없다.

**결정.** 실시간(세이브 쓰기 감지) 수집을 선택 기능이 아니라 기본 구조로 둔다.

---

## D7. 패치 처리: 구간별이 아니라 챔피언 버전별

**상황.** 시즌 안에서 밸런스 패치가 1~3회 들어간다. 패치를 무시하면 분석이 부정확하고,
패치 구간으로 자르면 표본이 죽는다.

**확인한 것.**
- 패치 시점과 내용은 `PatchNews.Date` / `PatchNews.Patches` 에 전부 있다. **사용자 입력 불필요**
- 패치는 커리어마다 고유하게 생성된다. 슬롯 간에 패치를 합칠 수 없다
- 패치당 6~10챔만 바뀌고, 시즌 내내 안 바뀌는 챔피언이 40종 중 21~25종이다

**측정.** 카운터 쌍 중 20경기 이상을 확보한 쌍의 개수(슬롯 1개 기준):

| 슬롯 | 경기 | 패치 | ① 패치 무시 | ② 패치 구간별 | ③ 챔피언 버전별 |
|---|---|---|---|---|---|
| S2025-a | 289 | 3회 | 102쌍 | **10쌍** | 90쌍 |
| S2025-b | 233 | 3회 | 66쌍 | **0쌍** | 34쌍 |
| S2026 | 278 | 1회 | 122쌍 | **46쌍** | 72쌍 |

②는 표본의 0~38%만 남기고, ③은 52~88%를 보존한다.

**결정.** 카운터·시너지는 **두 챔피언의 버전 조합**을 키로 묶는다.
`(Jiangshi v2, Knight v0)` 같은 형태다. 관련 챔피언이 안 바뀐 구간은 전부 합칠 수 있다.
챔피언 단위 승률·픽률·밴률은 구간당 58~139경기로 버티므로 패치 구간별(②)로 간다.

**스코프 분리.** `GLOBAL`(슬롯 합산, 패치 무시)과 `CAREER`(슬롯 내부, 버전 반영)를
별도 집계로 둔다. 섞으면 숫자가 거짓말이 된다.

---

## D8. 스크림 포함, 시점은 워처가 보정

**근거.** 스크림을 넣으면 카운터 쌍당 경기 수 중앙값이 13 → 20으로 오르고, 30경기 이상인
쌍이 236 → 470으로 두 배가 된다. 표본의 34%를 버릴 이유가 없다.

**더 중요한 것.** 스크림은 **원하는 매치업을 골라서 찍을 수 있다.** 웹이 표본이 빈 조합을
알려주면 유저가 스크림으로 메꾼다. 게임이 못 해주는 기능이고, 수동적 통계 뷰어가 아니라
데이터 수집을 유도하는 도구가 되는 지점이다.

**문제.** `ScrimStat` 에는 Season / Day 가 없다. 패치를 붙일 수 없다.

**결정.** 스크림은 `GLOBAL` 집계에 포함하고 `CAREER`(패치 반영) 집계에서는 제외한다.
단 **워처가 돌고 있으면 스크림이 처음 발견된 시점의 게임 내 Day 를 기록**해서
(`match_record.observed_day`) 그 이후 스크림은 패치를 붙인다.
게임이 주지 않는 정보를 우리가 만들어내는 것이고, 이것이 실시간 수집의 세 번째 이유다.

밴률 집계에서는 스크림을 반드시 제외한다(스크림에는 밴이 없어 분모가 왜곡된다).
그래서 모든 집계 테이블에 `include_scrim` 을 키로 넣어 두 버전을 다 갖는다.

---

## D9. 표본 기준선 10경기

**논점.** "표본이 적어도 그냥 분석하면 안 되나. 변수가 적은 게임이라 조금만 쌓여도 유의미하지 않나."

**측정 1 — 효과 크기.** 이 게임은 생각만큼 결정론적이지 않았다.

| 표본 | 쌍 수 | 관측 승률 표준편차 | 우연만으로 생길 흩어짐 | 신호/잡음 |
|---|---|---|---|---|
| n≥30 | 470 | 0.094 | 0.074 | 1.27배 |
| n≥50 | 194 | 0.096 | 0.063 | 1.52배 |

역산하면 **진짜 카운터 효과의 표준편차는 약 6%p**다. 대부분의 매치업은 실제로 44~56%다.
다만 꼬리는 두껍다 — Werewolf vs Fighter 75.9%(58경기), Ghost vs Bard 75.0%(40경기).
**센 카운터는 적은 표본으로 잡히고, 미묘한 카운터는 아무리 쌓아도 안 잡힌다.**

**측정 2 — 가짜 극단값.** 이론이 아니라 실제 데이터에서 센 값이다.

| 표본 구간 | 쌍 수 | 100%/0% 나온 쌍 | 동전던지기여도 나올 개수 |
|---|---|---|---|
| 1~3경기 | 78 | 50 | 40 |
| 4~6경기 | 120 | 6 | 8 |
| 7~10경기 | 204 | **0** | 1 |

1~3경기의 100% 승률은 거의 전부 우연이다. 4~6경기 구간은 관측(6)이 우연 기대치(8)보다
오히려 적어서 전부 우연으로 설명된다. **7경기를 넘기면 가짜 극단값이 사라진다.**

**측정 3 — 커버리지 비용.** (1,207경기 기준, 관측된 조합 중 살아남는 비율)

| 기준 | 2인 카운터 | 2인 시너지 | 3인 시너지 |
|---|---|---|---|
| 5경기 | 93% | 85% | 462가지 |
| **10경기** | **77%** | **62%** | **114가지** |
| 15경기 | 62% | 45% | 39가지 |
| 20경기 | 50% | 34% | 12가지 |

**결정.** **10경기.** 가짜 극단값이 죽는 지점(7) 바로 위이고, 15로 올리면 신뢰도 이득보다
잘려나가는 조합이 훨씬 크다. `analysis_config.min_sample` 로 빼서 조정 가능하게 둔다.

**미달 조합은 화면에 노출하지 않는다.** 승률과 무관하게 표본만으로 자른다.

---

## D10. 정렬은 축소추정으로

**문제.** 표본 기준을 통과한 조합들 사이에서도, 10경기짜리 90%가 60경기짜리 73%보다
위에 오면 순위표가 망가진다.

**방법.** `표시승률 = (승 + k/2) / (경기 + k)`, `k = 24`.

측정된 효과 분산으로 역산한 통계적 최적값은 k ≈ 74지만, 그러면 Werewolf-Fighter 같은
진짜 강한 쌍까지 과하게 눌린다. 실무적 절충으로 24를 쓰고, **원본 승률과 경기 수를 항상
같이 노출**한다. 순위는 축소값으로, 판단은 사용자가.

**효과 확인.**

```
── 원본 승률 정렬 상위 ──            ── 축소추정 정렬 상위 ──
100.0% (n=4)  Gambler vs Executioner   84.0% (n=25)  Werewolf vs Swordman
100.0% (n=3)  Swordman vs Exorcist     75.9% (n=58)  Werewolf vs Fighter
100.0% (n=3)  Jiangshi vs Ghost        73.3% (n=60)  Dancer vs Fighter
```

왼쪽은 전부 노이즈, 오른쪽은 전부 실제 신호다. 데이터를 하나도 버리지 않고 정렬 기준만 바꾼 결과다.

---

## D11. 시너지는 3인까지, 4인은 역할군으로

**측정.** 같은 팀 조합의 실제 분포. 균등분포 가정은 틀렸고 실제 드래프트는 심하게 몰려 있다.

| 크기 | 가능 | 관측 | 최다 | 5회↑ | 10회↑ |
|---|---|---|---|---|---|
| 2인 | 780 | 761 (98%) | 128회 | 649 | 475 |
| 3인 | 9,880 | 3,914 (40%) | 36회 | 462 | 114 |
| 4인 | 91,390 | 1,976 (2%) | **9회** | 21 | **0** |

3인은 상위 조합이 26~36경기까지 쌓인다(`Archer + Bard + Gunner` 36회 55.6% 등).
4인은 최다가 9경기, 10경기 이상인 조합이 0개다. 통계가 아니라 경기 목록이다.

**결정.** 챔피언 조합은 **2인·3인만** 집계한다.

**4인은 역할군으로 한다.** 역할군 5종에서 4자리 중복조합은 **70가지**뿐이라
1,207경기 × 2팀 = 2,414개 팀 구성을 뿌리면 조합당 평균 34경기가 나온다.

> "전사2 + 마법사1 + 전투보조1 → 승률 58%, 187경기"

밴픽할 때 실제로 궁금한 것도 특정 4챔 조합이 아니라 구성 균형이다.

**뒤집힐 조건.** 수만 경기가 쌓이면 4인 챔피언 조합도 상위권은 볼 수 있게 된다.

---

## D12. 실시간 = 세이브 파일 쓰기 감지

**측정.** 파싱이 전체 0.39초, 경기 스트림만 0.15초다. 파일 감시 → 재파싱 → 신규 경기 diff →
DB → 화면 갱신 전체가 1초 안에 끝난다. **병목은 우리 쪽이 아니라 게임이 세이브를 쓰는 주기다.**

**설계.**
- 트리거는 "게임이 세이브 파일에 쓰는 순간". 유저는 웹에서 아무것도 누르지 않는다
- 쓰기 도중에 읽으면 스트림이 잘린다 → 마지막 변경 후 1.5초 조용하면 읽는 디바운스 필요
- diff 는 `GameStat.ID` 기준. 이미 넣은 ID는 건너뛴다

**미확정.** 게임이 세이브를 얼마나 자주 쓰는지, 실행 중 파일이 잠기는지는 아직 측정 전이다.
`tools/watch_save.py` 로 측정한다. 결과에 따라:

| 관측 | 결론 |
|---|---|
| 경기일마다 쓰기 | 실시간(경기일 단위) 성립 |
| 저장 눌러야 쓰기 | 준실시간. 유저 저장 습관에 의존 |
| 종료 시에만 쓰기 | 세션 단위 배치로 설계 변경 |

**이 결과는 구현 방식만 바꾸고 스키마는 바꾸지 않는다.**

---

## D13. 밴픽 시뮬레이터는 2차 → **다음 구현으로 승격 (2026-08-25)**

**원래 근거.** 추천의 품질이 전적으로 표본에 달려 있다. 카운터 쌍당 10경기를 넘긴 쌍이
현재 77%인데, 이 상태에서 추천을 내면 근거 없는 조언이 된다.

**원래 결정.** 1차에서는 만들지 않되 **데이터는 지금부터 모은다.**
`pick_order`, `ban_order`, `draft_session` 테이블은 1차에서도 채우거나 만들어 둔다.

**변경.** 사용자가 우선순위를 올렸다. 표본 문제는 사라지지 않았으므로 **기능을 뒤로 미루는
대신 추천을 거부하는 규칙으로 푼다** — 표본이 기준에 못 미치는 후보는 점수를 내지 않고
"판정 불가"로 표시한다. 근거 없는 조언을 막는 목적은 그대로 달성되고, 표본이 쌓이면
같은 화면이 저절로 좋아진다. 설계는 `banpick.md`.

---

## D14. 카운터는 챔피언 강도를 빼고 본다

**문제.** raw 승률에는 챔피언 자체 강도가 섞여 있다. Werewolf 기저 승률이 61%라
raw 정렬로는 Werewolf 가 거의 모든 상대에서 상위를 차지한다. 그건 카운터표가 아니라
"센 챔피언 목록"이다.

**측정.** 카운터 승률 분산 분해 (n≥10, 1,204쌍).

| 출처 | 표준편차 | 분산 비중 |
|---|---|---|
| 챔피언 자체 강도 | 0.0525 | 19% |
| 페어 고유 상성 | 0.0197 | 3% |
| 순수 노이즈 | 0.1027 | 78% |

**결정.** Bradley-Terry 로 두 챔피언 기저 강도만으로 예측되는 승률을 구하고, 실제와의
차이를 상성 이득으로 본다.

```
expected(A,B) = p_A(1-p_B) / ( p_A(1-p_B) + p_B(1-p_A) )
counter_effect = adjusted(A,B) - expected(A,B)
```

정렬은 `counter_effect`, 화면에는 실제 승률·기대 승률·상성 이득을 함께 표시한다.
효과가 실제로 뒤집힌다:

```
raw 정렬:  Werewolf vs Swordman  84.0% (기대 66.6%, 이득 +17.4%p)
잔차 정렬: Fighter  vs DarkMage  76.0% (기대 42.8%, 이득 +33.2%p)
```

Fighter 는 전체 승률 42%인 약한 챔피언인데 DarkMage 상대로는 76%다. raw 정렬로는 안 보인다.

**주의.** 페어 고유 상성의 평균 크기는 2%p로 작다. 대부분의 매치업에는 상성이 없고,
소수가 +33%p로 튄다. 전체 매트릭스를 색칠하기보다 **유의한 쌍만 뽑아 보여주는 게 맞다.**

**챔피언 티어에는 보정하지 않는다.** 티어는 "이 챔피언이 센가"를 묻는 것이므로
기저 승률 자체가 답이다. 보정은 카운터·시너지에만 적용한다.

---

## D15. 패치 가중치: 2단 축소 + 이중 감쇠

**문제.** 패치 구간으로 자르면 표본이 죽고(D7), 무시하면 부정확하다. 그리고 패치가
Werewolf 를 너프하면 **Werewolf 를 안 건드렸어도 Fighter 의 승률은 바뀐다.** 상대 풀이
달라지기 때문이다. 즉 안 바뀐 챔피언도 감쇠를 받아야 하는데, 그러면 남는 데이터가 없다.

**결정.** 세 가지를 동시에 쓴다. D7 의 버전 키 방식을 이것으로 대체한다.

**(a) 이중 감쇠 — 자기 변경은 세게, 메타 변화는 약하게**

```
w(g) = 0.5^(자기변경횟수 / 2)  ×  0.5^(경과패치수 / 12)
```

자기 변경은 1차 효과, 메타 변화는 2차 효과다. 반감기 12패치는 시즌 3~4개 분량이라
최근 시즌은 거의 안 깎이고 4시즌 전 데이터가 절반이 된다. 버전 키(hard cut)는
반감기 0인 극단, 무가중은 무한대인 극단이고 이건 그 사이를 연속적으로 움직인다.

**(b) 2단 축소 — 핵심**

패치별로 완전히 분리하지도, 완전히 합치지도 않는다. 각 패치의 추정치를
**그 쌍의 전체 누적 추정치** 쪽으로 당긴다.

```
1단 (전체 누적, 감쇠 적용, 기저값으로 축소)
  θ_all(A,B) = ( Σ w·win + k₀·expected(A,B) ) / ( Σ w + k₀ )     k₀ = 24

2단 (특정 패치, 전체 누적값으로 축소)
  θ_patch(A,B,p) = ( n_p·wr_p + k₁·θ_all(A,B) ) / ( n_p + k₁ )   k₁ = 15
```

데이터가 많은 패치는 자기 데이터가 지배하고, 적은 패치는 전체 누적값으로 수렴한다.
**패치 반영과 표본 부족을 동시에 푸는 유일한 구조다.**

축소 대상이 3단 계단을 이룬다: **패치 추정 → 전체 누적 추정 → 챔피언 강도 기대값.**
데이터가 없을수록 위로 올라가며 "모르면 아는 것에 가깝게" 답한다.

**(c) 경기력 지표로 정보량 보강** — D19 참조.

**저장.** 원시 카운트(`games`)와 가중 합계(`weighted_games`, `weighted_wins`)를 둘 다 둔다.
표본 기준(10경기) 판정과 화면 표시는 원시 카운트로, 승률과 정렬은 가중값으로.
유효표본수 `ess = (Σw)² / Σw²` 로 신뢰구간을 잡는다.

**뒤집힐 조건.** 반감기 2와 12는 측정으로 정한 값이 아니다. 패치 전후 승률 변화를
쌍 단위로 재려면 표본이 더 필요하다. `analysis_config` 에서 조정한다.

---

## D16. 이벤트전은 처리할 것이 없다

**확인한 것.** 이벤트전을 실제로 치르고 세이브를 다시 파싱했다.

```
이벤트 스케줄 ID109  Progress 0.0 → 1.0, 스코어 0:1
GameStat   294 → 294 (+0)
ScrimStat  180 → 180 (+0)
```

**이벤트전은 밴픽·챔피언·개인스탯을 저장하지 않는다.** 스케줄에 스코어만 남는다.

`EventMatchData = { Type, Category }` 이고 Category 가 `ChampionCategory` 이므로
역할군 제약이 걸리는 특수 룰 경기로 보인다. 정상 드래프트가 아니라 통계에 섞이면
곤란한데, 애초에 데이터가 안 남으니 **제외 로직도 `is_event` 컬럼도 필요 없다.**

**뒤집힐 조건.** 확인한 것은 `Type=7 / Category=0` 한 종류뿐이다. 다른 타입이 데이터를
남기면 그때 대응한다. 위험은 낮다 — 안 남기는 쪽이 기본 동작으로 보인다.

---

## D17. 자동 저장 확정 — 경기 단위, 파일 잠김 없음

**측정.** `tools/watch_save.py` 실측 로그.

```
23:37:46  스크림 +1                          파싱 0.46s
23:39:18  스크림 +1        (직전 +92.6s)      파싱 0.45s
23:47:46  변화 없음, 파일 +2,942B (+507.4s)   파싱 0.42s
00:02:11  스크림 +1        (+66.6s)          파싱 0.45s
```

1. **스크림 한 판 끝날 때마다 즉시 저장된다.** 하루 단위가 아니다. 66~93초 간격이
   한 판 도는 시간이다. 사용자가 저장을 누를 필요도 없다
2. **파싱 에러 0건.** 게임 실행 중에도 파일이 잠기지 않는다. 임시 복사 우회 불필요
3. 쓰기 3~4건 중 1건은 경기 증가가 없다(훈련·골드·뉴스). 파싱이 0.45초라 매번 파싱하고
   diff 가 0이면 스킵하는 것으로 충분하다

**D8 이 실증됐다.** 스크림 179·180번이 Day 41에 발생했다는 것을 워처가 알아냈다.
게임은 `ScrimStat` 에 시점을 저장하지 않는데 워처는 안다. 스크림 407건(표본의 34%)을
패치 분석에 편입시키는 길이 열렸다 — 워처 가동 이후 것부터.

---

## D18. 현재 날짜는 `TodayData.Time` 에서 읽는다

**발견 경위.** 이벤트전을 여러 번 치렀는데 워처가 찍는 최대 Day 가 41에서 안 움직였다.

**원인.** 최대 Day 를 `max(GameStat.Day)` 로 구했기 때문이다. 이벤트전은 GameStat 을
남기지 않고(D16), 스크림에는 Day 가 없다. 그래서 **공식 경기가 없는 날은 이 값이 멈춘다.**

```
TodayData.Time     : S2025 Day 46 18:00   ← 실제 현재 날짜
max(GameStat.Day)  : 41                   ← 5일 뒤처짐
진행완료 스케줄 최대 : Day 45
```

**결정.** 현재 게임 내 날짜는 `TodayData.<Time>k__BackingField` (SeasonTime) 에서 읽는다.
`TodayData` 는 세이브에 1개뿐이고 `Hour`/`Minute` 까지 있다.

**중요한 이유.** D8 의 스크림 타임스탬프 보정이 이 값에 의존한다. GameStat 기반으로
날짜를 추정했다면 스크림에 최대 5일 틀린 날짜가 붙었을 것이다.

---

## D19. 경기력 지표는 티어에 안 쓰고 축소 목표값에 쓴다

**있는 것.** `MatchStat` 에 세트별·챔피언별로
`Kill, Death, Assist, Dealing, Tanking, Healing, LiveDuration` + `AthleteID`.

**측정.** 카운터 쌍 782개(n≥10)에서 승패 기반과 경기력 기반의 신호/잡음 비교.

| 지표 | 신호/잡음 | 승률과의 상관 |
|---|---|---|
| 승패 | 1.10 | — |
| 탱킹/분 | **1.46** | −0.220 |
| 딜/분 | **1.35** | +0.194 |
| KDA | 1.14 | **+0.475** |
| 힐/분 | 0.99 | 0.115 |

**두 가지가 보인다.**

탱킹·딜은 승패보다 신호가 선명하다. 관측당 신호/잡음으로 환산하면 탱킹이 승패의 2.4배라
**같은 확신에 표본이 5~6배 적게 든다.** 승패는 경기당 1비트인데 딜·탱은 연속값이고
아군 3명에 덜 오염된다.

그러나 경기력은 승리가 아니다. 탱킹은 승률과 **음의 상관**(−0.220)이다 — 많이 맞았다는
것은 압박받았다는 뜻이다. KDA 만 승률과 제대로 상관(+0.475)이 있다.

**결정.**

- **티어 점수에 직접 가산하지 않는다.** 승률이 목표 변수인데 그 대리 지표를 섞으면
  표본이 충분할 때 오히려 나빠진다
- **축소추정의 목표값을 만드는 데 쓴다.** 표본이 적을 때 상수(50%)나 기저 강도로만
  수렴시키는 것보다, 경기력으로 예측한 승률로 수렴시키는 것이 낫다.
  KDA 의 r=0.475 는 R²≈0.23 으로, 상수 대비 분산을 23% 줄인다
- **챔피언 성격 기술에 쓴다.** Jiangshi 는 탱킹 844k / 딜 126k 로 한눈에 탱커다.
  역할군 시드 검증에도 쓸 수 있다
- **조기 지표로 쓴다.** 표본 10~15경기라 승률로는 판정 못 할 때 "이 매치업에서 딜이
  평소보다 1.2 표준편차 낮다"가 먼저 잡힌다. `/gaps` 의 스크림 우선순위 근거가 된다

**주의.** KDA 는 승리의 결과이기도 하다(이기면 킬이 많고 데스가 적다). 독립적인 증거로
취급하면 안 되고, 축소 목표값으로 쓸 때 가중치를 보수적으로 잡아야 한다.

---

## D20. ChampStat 순서를 믿으면 안 된다

**측정.** `GameStat.ChampStat` 의 순서가 `BluePick + RedPick` 순서와 일치하는 경기는
805건 중 **640건(79.5%)** 뿐이다.

**결정.** 인덱스로 매칭하지 말고 **챔피언 이름으로 매칭한다.** 한 경기에 같은 챔피언이
두 번 나올 수 없으므로(스키마의 `match_participant_unique_champ`) 이름 매칭은 안전하다.

인덱스로 매칭했다면 경기의 20%에서 진영이 뒤바뀌어 카운터 분석이 통째로 오염됐을 것이다.

---

## D21. 화면 구성은 lol.ps 를 차용하되 두 가지를 바꾼다

**구조.** `/tier` 티어표 → 챔피언 클릭 → `/champion/[code]` 상세(카운터·시너지).

**티어표 열 (전부 정렬 가능).** 티어 / 승률 / 픽률 / 밴률 / 경기 수 / 최근 변화 /
KDA·딜·탱·힐. 필터는 역할군 / 커리어(슬롯) / 패치 / 스크림 포함 / 기간.

**정렬 축에 따라 표본 기준이 달라진다.** 승률 정렬에서는 10경기 미만을 빼야 하지만,
픽률 정렬에서는 "픽률이 낮다"는 것 자체가 정보라 빼면 안 된다. UI 에 명시한다.

**lol.ps 에서 안 가져오는 것**

- 라인 탭 — 이 게임에 라인이 없다. 역할군 필터로 대체
- OP 스코어 같은 단일 종합 점수 — 진짜 효과 크기가 작아서 소수점까지 있는 점수는
  근거 없는 정밀도가 된다

**대신 추가하는 것**

- `/gaps` — 표본이 부족한 매치업과 "스크림 몇 판이면 판정 가능". 게임도 lol.ps 도
  못 하는 화면이고, 수동적 뷰어를 데이터 수집 도구로 바꾸는 지점이다

---

## D22. 스택을 Spring Boot + Thymeleaf/HTMX 로 전환 (2026-08-11)

**상황.** 원래 목적이 Spring 학습이었다는 것이 뒤늦게 확인됐다. Next.js + Supabase 로
진행 중이었고 TS 파서 954줄(`lib/parser/*.ts`)과 골든 테스트가 나온 시점이었다.

**전환 시점이 저렴한 이유.**

- UI·DB·API 코드가 아직 없다. Next.js 의존성만 깔려 있고 `app/` 디렉토리가 없다
- 버려지는 것은 TS 파서 954줄뿐인데, 로직이 언어 무관이고 Python 원본(`tools/nrbf.py`)도 있어
  Java 는 3번째 구현이다
- **`tests/baseline/*.json` 골든 파일이 이미 있다.** Java 포팅의 정답지가 완성돼 있어 검증이 자동이다

**원래 계획에 있던 구멍도 드러났다.** 워처는 세이브 파일을 볼 수 있는 곳에서 돌아야 하는데
그 파일은 사용자 Windows PC 에 있다. Vercel 에 배포하면 워처가 파일을 못 본다.
**Next.js 계획도 결국 로컬 실행이 필요했다.** Spring 은 `java -jar` 하나로 워처·집계·웹이
전부 뜨므로 이 제약과 오히려 잘 맞는다.

**프론트엔드는 Thymeleaf + HTMX.** 선택 근거:

- 화면이 **데이터는 빽빽한데 상호작용은 가볍다.** 정렬·필터가 전부고 40행은 서버가 다시 그려도
  눈에 띄지 않는다. 드래그·복잡한 클라이언트 상태·폼이 없다
- 디자인 요구(표본에 따른 시각적 약화, 신뢰구간 막대)는 서버에서 클래스·폭을 계산해 내려보내면 된다
- SSE 실시간 갱신은 `hx-ext="sse"` 로 조각만 교체한다. 순수 Thymeleaf 여도 SSE 용 JS 는
  어차피 필요하므로 "JS 0" 은 선택지가 아니었다
- **학습 목적이 결정적이었다.** Thymeleaf 는 Spring MVC 를 통째로 쓰지만(뷰 리졸버, 모델 바인딩,
  Security 연동), REST + SPA 로 가면 Spring 은 `@RestController` 와 DTO 만 담당하게 되어
  Spring 학습량이 오히려 줄어든다
- 6명 에이전트가 붙어 있는 상황에서 프로젝트 1개 / 빌드 1개 / 배포 1개가 조율하기 쉽다

**DB 는 Postgres 를 유지한다.** `schema.sql` 이 Postgres 전용 기능을 네 개 쓴다 —
`GENERATED ALWAYS AS ... STORED`, `UNIQUE NULLS NOT DISTINCT`(PG15+), ENUM 타입 6개,
부분 유니크 인덱스. H2·SQLite 로 가면 넷 다 다시 짜야 한다. ~~Docker Compose~~ + Flyway.
→ **Docker Compose 부분은 D23 에서 폐기** (WSL 로컬 PG16), **그 WSL 도 D27 에서 폐기.**
최종은 Windows 네이티브 PG16 이다.

**JPA 로 집계하지 않는다.** 가중합·2단 축소·백분위 컷·`NULLS NOT DISTINCT` 업서트는
Hibernate 가 약한 영역이고 Postgres ENUM 매핑도 번거롭다. **적재는 JPA, 집계는 JdbcTemplate.**

**남는 위험.**

1. **분석 로직이 어려운 부분인데 언어와 무관하다.** Spring 보일러플레이트에 에너지를 다 쓰고
   정작 카운터·시너지 분석에 도달하지 못하는 것이 최대 위험이다. 그래서 `tools/` 의 Python
   스크립트를 레퍼런스 구현으로 유지하고 Java 결과를 계속 대조한다
2. **Java 의 `byte` 는 부호가 있다.** NRBF 는 unsigned 를 다루므로 `& 0xFF` 를 빼먹으면
   조용히 틀린다. ~~골든 JSON 이 잡아준다~~ → **틀린 기대였다. D34 참고**
3. 팀 6명이 이미 TS 궤도에 올라와 있어 재브리핑이 필요하다

**뒤집힐 조건.** 밴픽 시뮬레이터(2차)가 서버 왕복으로 감당이 안 될 만큼 무거워지면
그 화면만 클라이언트 렌더링으로 떼는 것을 검토한다. 지금은 픽 한 번에 서버 왕복 한 번이면
충분하다 — 추천값이 어차피 DB 에서 나온다.

---

## D23. 스키마 검증 완료 · Docker 대신 로컬 Postgres (2026-08-12)

**검증.** `schema.sql` 을 PostgreSQL 16.14 에 실제 적용했다. **오류 0, 첫 시도 통과.**

| 항목 | 결과 |
|---|---|
| 객체 | 테이블 19 · 뷰 2 · ENUM 4 · 생성열 6 · 인덱스 39 |
| `UNIQUE NULLS NOT DISTINCT` | 4개 인덱스 적용. NULL 중복 실제 차단 확인 |
| 생성열 | 44/58 → 0.7586, pick 0.58, ban 0.10 |
| `v_match_participant.is_winner` | BLUE t / RED f |
| `team_size=5` · `wins>games` · `ban_order=4` | 전부 거부 |
| 한 경기 같은 챔피언 · 같은 pick_order | 전부 거부 |

가장 위험했던 `UNIQUE NULLS NOT DISTINCT`(PG15+)가 통과했다.
**설계 검증 라운드 하나를 실행 테스트로 대체했다** — 검증 칸이 20KB DDL 을 읽고 추론할 필요가 없다.

**Docker 를 쓰지 않는다.** 이 PC 에서 Docker Desktop 엔진이 기동되지 않는다.
`docker-desktop` WSL 배포판은 직접 띄우면 올라오지만 데몬이 파이프를 열지 않는다.

| 영향 | 조치 |
|---|---|
| Docker Compose 로 Postgres | ~~WSL 에 PGDG 저장소로 PG16 직접 설치~~ → **D27 에서 Windows 네이티브 설치로 변경** |
| Testcontainers | **제거.** 로컬 `tfm_test` DB + 트랜잭션 롤백 격리 (유지) |

대가는 환경 재현성이다. 다만 이 프로젝트는 `java -jar` 로컬 실행이 전제(D22)라 Docker 가
필수인 구간이 없다.

**뒤집힐 조건.** Docker 가 살아나면 Testcontainers 로 되돌릴 수 있다. 그때까지 테스트가
로컬 DB 상태에 의존하지 않도록 롤백 격리를 지킨다.

### 2차 검증 (2026-08-25) — 밴픽 모델 추가 후 재실행

D26 의 드래프트 테이블과 D24/D28 의 제약을 넣은 뒤 Windows 네이티브 PG16.15 에 다시 적용했다.
**적용 오류 0, 제약 검증 24/24 통과.**

| 항목 | 결과 |
|---|---|
| 객체 | 테이블 21 · 뷰 3 · ENUM 5 · 생성열 6 · `UNIQUE NULLS NOT DISTINCT` 4 |
| 시드 | `analysis_config` 6행 · `draft_step` 14행 |
| `ban_order` | 3 통과 / **4 거부** (팀당 밴 3) |
| `pick_order` | 1 통과 / 5 거부 |
| `draft_step` 순서 | 진영 `BRBRBRRBRBRBBR` · 액션 `bbbbppppbbpppp` — 2차 밴이 픽 뒤에 온다 |
| 드래프트 중복 | 밴한 챔피언 재픽 거부 · 스텝 15 거부 · `next_step=16` 거부 |
| D24 | `GLOBAL`+패치 거부 / `CAREER`+패치 통과 |
| 기존 제약 | `wins>games` · `team_size=5` · 같은 챔피언 2회 · 자기 매치업 · 역할군 합≠4 · 시너지 크기 4 전부 거부 |

**일회성 확인이 아니라 스크립트로 남겼다** — `tests/verify_schema.sql`. 픽스처를 넣고
위반을 시도한 뒤 `ROLLBACK` 한다. 스키마를 고칠 때마다 다시 돌린다.
D23 이 "검증 라운드를 실행 테스트로 대체했다"고 한 것의 반복 가능한 버전이다.

---

## D24. "패치별 밸류"는 필터가 아니라 축소추정 시점 선택이다 (2026-08-25)

**문제.** 목적은 "패치버전마다 챔피언 밸류를 한눈에"인데, 문서 세 곳이 서로 달랐다.

- `savefile.md` — 패치는 커리어마다 고유 생성. 슬롯 간 공유되는 "패치 14.3" 같은 축이 없다
- D15 — 패치로 자르지 않는다. 자르면 표본이 0~38%만 남는다
- `project.md` 완료 기준 — "패치 필터를 걸면 **해당 구간 경기만 집계**"

세 번째가 앞의 둘을 무효로 만든다. 하드 필터를 쓰면 2단 축소가 할 일이 없어진다.

**결정.** 화면의 패치 선택은 **집계 대상을 자르는 필터가 아니라 추정 시점의 선택**이다.
패치 P 를 고르면 그 구간 경기만 세는 것이 아니라, `θ_patch(P)` 를 보여준다 — 그 패치의
자기 데이터가 지배하되 모자란 만큼 전체 누적값으로 채운 값이다(D15 2단).

따라서:

- 패치 선택은 `CAREER` 스코프에서만 뜬다. `GLOBAL` 은 슬롯을 합산하므로 패치 축이 없다
- 화면에 원시 경기 수(그 패치 구간의 실제 관측)와 축소추정 승률을 **함께** 띄운다.
  "이 패치 12경기 · 추정 54.1%" 처럼 읽혀야 한다
- "이 패치 구간 경기만"을 정말 보고 싶은 경우를 위해 원시 카운트는 계속 저장한다.
  다만 그것이 기본 정렬이 되지는 않는다

**뒤집힐 조건.** 패치당 경기 수가 충분히 커져서(쌍당 10경기 이상) 하드 컷으로도
표본이 죽지 않게 되면 필터 방식이 더 정직하다.

---

## D25. `pick_order` 는 드래프트 순서가 아니다 — 검증 전까지 (2026-08-25)

**문제.** `match_participant.pick_order` 는 `BluePick[4]` 의 배열 인덱스로 채운다.
그런데 그 배열 순서가 실제 픽 순서인지 **아무도 확인하지 않았다.** D20 에서
`ChampStat` 순서를 믿었다가 경기의 20%에서 진영이 뒤바뀔 뻔한 것과 같은 종류의 가정이다.

밴픽 시뮬은 픽 순서를 입력으로 쓴다. 여기가 틀리면 "5번 스텝에서 이 챔피언이 자주 나온다"
같은 결론이 통째로 거짓이 된다.

**결정.** 검증 전까지 `pick_order` 는 **배열 슬롯 번호**로만 취급한다.
드래프트 순서로 해석하는 코드를 쓰지 않는다. 시뮬의 스텝 순서는 `draft_step`(D26)에서 오고,
과거 경기의 픽 순서에 의존하는 통계는 만들지 않는다.

**검증 방법.** `BluePick`/`RedPick` 배열 순서를 `draft_step` 의 스텝 순서와 대조한다.
드래프트 규칙상 블루의 2번째 픽은 레드가 2개를 고른 뒤에 온다(스텝 8). 배열이 실제
순서라면 픽 시점의 정보량 차이가 통계로 잡혀야 한다 — 예를 들어 나중에 뽑히는 픽일수록
상대 조합에 대한 카운터 관계가 강해야 한다. 그런 신호가 없으면 배열은 그냥 저장 순서다.

**뒤집힐 조건.** 위 검증이 통과하면 `pick_order` 를 드래프트 순서로 승격하고,
스텝별 픽 분포 통계를 시뮬 추천의 사전값으로 쓸 수 있다.

---

## D26. 드래프트 순서는 14스텝 고정, 원본은 `draft_step` 테이블 (2026-08-25)

**확인한 것.** 팀당 밴 3 · 픽 4, 두 팀. 진행 순서는 고정이다.

```
phase 1 (1차 밴)   1.B밴  2.R밴  3.B밴  4.R밴
phase 2 (1차 픽)   5.B픽  6.R픽  7.R픽  8.B픽
phase 3 (2차 밴)   9.R밴 10.B밴
phase 4 (2차 픽)  11.R픽 12.B픽 13.B픽 14.R픽
```

전반부가 `B1 · R2 · B1`, 후반부가 `R1 · B2 · R1` 로 **진영을 바꿔 정확히 대칭**이다.
1차 밴도 블루 시작, 2차 밴은 레드 시작으로 대칭이다. 6밴 8픽이 정확히 맞는다.

**설계에 결정적인 것: 2차 밴(스텝 9·10)이 픽 4개가 공개된 뒤에 온다.**
이 시점의 밴은 "센 챔피언 지우기"가 아니라 **상대가 이미 고른 3~4개 조합에 대한 대응**이다.
따라서 밴 추천 엔진은 픽 추천 엔진과 목적 함수가 다르다 — 픽은 내 조합의 기대 승률을
올리고, 2차 밴은 **상대가 다음 스텝에서 얻을 최대 이득을 깎는다.** `banpick.md` 참고.

**결정.** 순서를 코드 상수로 박지 않고 `draft_step` 테이블에 둔다. 시뮬 엔진·적재 검증·
화면이 같은 원본을 봐야 하고, 이 프로젝트는 이미 임계값을 `analysis_config` 로 빼는
방식을 쓰고 있다(일관성).

**커리어 초반 2밴 경기.** 챔피언 풀이 작던 시기의 데이터에는 팀당 밴이 2개다
(`savefile.md` slot 638064, Day 7~15, 107경기). **규칙이 가변인 것이 아니라 과거
데이터의 예외다.** 그 경기는 적재하되 밴 집계에서만 빼고(D35), 시뮬은 항상 3밴으로만 돌린다.

**뒤집힐 조건.** 게임 업데이트로 순서가 바뀌면 `draft_step` 시드만 갈아끼운다.

---


---

## D27. 개발·실행 환경을 Windows 네이티브로 확정 (2026-08-25)

**상황.** D23 이 Docker 대신 WSL 로컬 Postgres 를 골랐다. 사용자가 WSL 을 쓰지 않기로 했다.

**확인한 것.** Windows 에서 필요한 것이 전부 된다.

| 항목 | 상태 |
|---|---|
| Java | 21.0.10 LTS 설치됨 |
| 세이브 파일 | `%USERPROFILE%\AppData\LocalLow\samoyed\Teamfight Manager\` 3개 (2.6~2.75MB) |
| 게임 설치 | `E:\SteamLibrary\steamapps\common\Teamfight Manager\` |
| PostgreSQL | **없음. 설치 필요** (winget 사용 가능) |

**결정.** 개발·빌드·실행·테스트 전부 Windows 네이티브. WSL 을 쓰지 않는다.

**이 결정이 오히려 원래 설계와 잘 맞는다.** D22 에서 확인했듯 워처는 세이브 파일이 있는
곳에서 돌아야 하고, 그 파일은 Windows 에 있다. WSL 에서 `/mnt/c` 로 읽는 것은
경유일 뿐이고 파일 변경 알림이 9p 계층을 건너오면서 지연·유실될 수 있다.
**워처를 Windows JVM 에서 돌리면 `WatchService` 가 네이티브 알림을 그대로 받는다.**

**대가와 대응.**

| 대가 | 대응 |
|---|---|
| `tools/` 의 Python 레퍼런스 실행 | Windows Python 으로 돌린다. 스크립트가 OS 의존이 없다 |
| 경로에 공백 (`Teamfight Manager`) | 설정·인자에서 항상 인용. 테스트 픽스처 경로도 공백 포함으로 둔다 |
| 파일 잠김 | Windows 는 Linux 보다 공유 위반이 잘 난다. 세이브를 열 때 공유 읽기로 연다 |
| 줄바꿈 | 골든 JSON 비교가 CRLF 에 깨지지 않게 파일을 바이트로 읽거나 LF 로 정규화 |
| 인코딩 | psql 의 `client_encoding` 이 콘솔 코드페이지(한글 Windows = UHC)를 따라간다. UTF-8 SQL 파일의 한글 주석에서 적재가 멈춘다 — `V1__init.sql` 첫머리에 `SET client_encoding = 'UTF8'` 을 박아뒀다 |
| 콘솔 출력 | 위를 UTF8 로 고정하면 이번엔 CP949 콘솔에서 결과의 한글이 깨진다. 실행 전 `chcp 65001` 로 콘솔을 UTF-8 로 맞춘다. 표시 문제일 뿐 데이터는 정상이다 |

**뒤집힐 조건.** 없다. 워처가 파일 옆에 있어야 한다는 제약이 이 방향을 강제한다.

---

## D28. `*.tfm_backup` 을 슬롯으로 잡으면 안 된다 (2026-08-25)

**발견.** 세이브 폴더에 `slot_638683925954242004.tfm_backup` 이 있다.
크기·수정시각이 본 파일과 같다. 게임이 저장할 때 직전 내용을 남기는 것으로 보인다.

**왜 위험한가.** 글롭을 `slot_*.tfm*` 로 걸면 백업이 **별도 슬롯으로 등록된다.**
`ingest_run` 의 중복 검사는 `UNIQUE (slot_id, file_hash)` 라 **슬롯 안에서만** 돈다.
백업은 `slot_key` 가 달라 다른 `save_slot` 이 되므로 해시가 같아도 걸러지지 않는다.
결과는 같은 커리어가 두 벌 적재되어 **모든 집계의 경기 수가 두 배**가 된다.
`GLOBAL` 스코프는 슬롯을 합산하므로 이게 그대로 티어표에 반영된다.

**결정.** 글롭은 `slot_*.tfm` 로 정확히 건다. 확장자 완전 일치로 판정하고,
`.tfm_backup` 은 명시적으로 제외한다.

**추가 방어.** 폴더 등록 시 이미 등록된 슬롯과 `file_hash` 가 같은 파일이 들어오면
슬롯이 달라도 경고한다. 백업 외에 사용자가 세이브를 복사해 두는 경우도 같은 문제를 낸다.

**써먹을 데.** 백업 파일은 **직전 저장본**이므로 테스트 픽스처로 쓸 수 있다.
같은 커리어의 두 시점을 비교하면 "저장 사이에 무엇이 늘었는가"를 실제로 볼 수 있다 —
D17 의 저장 주기 측정과 워처 증분 적재 테스트의 재료다.

---

---

## D29. 에이전트 팀 대신 단독 작업 + `agy` 검증 (2026-08-25)

**변경.** `project.md` 가 6명 에이전트 분업(설계·백엔드·프론트·QA)을 상정했다.
**혼자 구현하고, 읽기 전용 검증만 `agy`(Antigravity CLI) 서브에이전트에 넘긴다.**

**근거.** 이 프로젝트는 나누기 나쁜 모양이다. 어려운 부분(카운터·시너지·2단 축소)은
결정들이 서로 얽혀 있어 한 사람 머릿속에 있어야 하고, 나눌 수 있는 부분(Spring
보일러플레이트)은 애초에 어렵지 않다. D22 가 이미 같은 위험을 지적했다 —
"보일러플레이트에 에너지를 다 쓰고 정작 분석에 도달하지 못하는 것이 최대 위험."
분업은 조율 비용으로 그 위험을 키운다.

**`agy` 가 맞는 자리.** 파일을 많이 읽어야 하는 읽기 전용 작업이다. 컨텍스트를 많이
먹지만 판단은 필요 없는 일 — Java 출력과 Python 레퍼런스 대조, 골든 파일 회귀,
구현이 `decision.md` 의 결정과 어긋나지 않는지 대조, 저장소 훑기.
설계 판단·구현·최종 결정은 넘기지 않는다.

---

## D30. 파일 구조: 기능별 분할, 경계로 규칙을 강제한다 (2026-08-25)

상세는 `structure.md`. 여기에는 판단만 적는다.

**`parser/` 에 Spring 의존을 0 으로 둔다.** 조용히 틀릴 위험이 가장 큰 코드다
(`& 0xFF`). Spring 컨텍스트 없이 순수 JUnit 으로 골든 파일과 대조할 수 있어야
검증이 빠르고 확실하다. 라이브러리처럼 취급한다.

**`ingest/`(JPA)와 `analysis/`(JdbcTemplate)를 패키지로 가른다.** D22 의 규칙인데
같은 패키지에 두면 지켜지지 않는다. **문서에만 적힌 규칙은 지켜지지 않는다** —
파일 위치로 강제한다.

**`draft/` 를 `analysis/` 아래에 두지 않는다.** 시뮬은 집계값을 소비할 뿐 생산하지 않는다.

**빌드 DSL 은 Groovy(`build.gradle`).** Kotlin DSL 이 요즘 기본값이지만 이 프로젝트의
목적 중 하나가 Spring 학습이고, 학습 자료 대부분이 Groovy 다. 학습 대상이 아닌 곳에서
문법 하나를 더 배우게 만들 이유가 없다.

**`schema.sql` 을 `db/migration/V1__init.sql` 로 옮기고 루트 사본을 없앴다.**
440줄 DDL 이 두 벌이면 반드시 어긋나고, 어느 쪽이 진짜인지 애매해지는 순간
"바꾸면 마이그레이션을 추가한다"는 규칙이 무너진다. 원본은 하나다.

**빈 클래스를 미리 깔지 않는다.** `structure.md` 의 트리는 목표이고, 파일은 필요할 때 만든다.

---

---

## D31. Spring Boot 4.1.1 로 간다 — 3.x 는 선택지가 아니다 (2026-08-25)

**상황.** `project.md` 제약이 "Spring Boot 3.x" 였다. Initializr 가 제공하는 목록을 확인했다.

```
4.2.0 (SNAPSHOT) · 4.2.0 (M1) · 4.1.2 (SNAPSHOT) · 4.1.1 · 4.0.9 (SNAPSHOT) · 4.0.8
```

**3.x 가 없다.** 제약이 이미 만족 불가능한 상태였다.

**결정.** **4.1.1.** 목록에서 최신 GA(정식 릴리스)다.

**SNAPSHOT·마일스톤을 쓰지 않는 이유.** SNAPSHOT 은 같은 버전 번호인데 내용이 매일 바뀐다.
어제 되던 빌드가 오늘 깨져도 내 코드는 그대로라 원인을 못 찾는다.
**이 프로젝트는 골든 파일로 "결과가 언제나 같아야 한다"를 강제하는 구조다**(D22).
토대가 매일 바뀌면 그 검증이 무의미해진다. M1 도 API 가 아직 움직인다.

**실제로 빌드해서 확인한 것.**

| | |
|---|---|
| Spring Boot | 4.1.1 |
| Spring Framework | 7.0.9 |
| Thymeleaf | 3.1.5 |
| Gradle | 9.5.1 |
| Java | 21.0.10 |
| 결과 | `compileJava` · `bootJar` · `build` 전부 성공 |

**Gradle 은 받지 않았다.** `winget` 에 Gradle 패키지가 없었는데,
`~/.gradle/wrapper/dists` 에 9.4.1 과 9.5.1 이 이미 있었다(다른 프로젝트가 받아둔 것).
그걸로 wrapper 를 생성했다. 이제 `gradlew.bat` 하나로 돌아간다.

**남는 위험.** Boot 4 는 Framework 7 · Hibernate 7 · Jackson 3 을 끌고 온다.
학습 자료 대부분이 아직 Boot 3 기준이라 **문서와 실물이 어긋나는 순간이 온다.**
특히 Postgres ENUM 의 JPA 매핑(D22 가 이미 번거롭다고 지적한 곳)은 Hibernate 버전에 민감하다.
막히면 자료를 의심하지 말고 실제 API 를 먼저 확인한다.

**위험이 즉시 현실이 됐다 — Flyway 가 조용히 안 돌았다.**

Boot 3 에서는 `flyway-core` 만 넣으면 마이그레이션이 자동으로 실행된다.
Boot 4 는 **자동설정을 기술별 모듈로 쪼갰다**(`spring-boot-hibernate`, `spring-boot-jdbc`,
`spring-boot-flyway` …). `flyway-core` 만 있으면 Flyway 라이브러리는 클래스패스에 있지만
**자동설정이 없어서 아무 일도 일어나지 않는다.**

**오류도 경고도 없다.** 로그에 Flyway 가 한 줄도 안 나오는 것이 유일한 증상이었다.
눈치채지 못했으면 나중에 "왜 테이블이 없지"부터 거꾸로 파야 했을 것이다.
`org.springframework.boot:spring-boot-starter-flyway` 를 추가해서 해결했다.

**교훈.** Boot 4 에서 어떤 기능이 "그냥 안 도는" 증상을 보이면 라이브러리가 아니라
**자동설정 모듈이 빠졌는지를 먼저 본다.** 로그의 침묵이 단서다.

**부팅 확인 (2026-08-25).**

| | |
|---|---|
| Flyway | `Successfully applied 1 migration to schema "public", now at version v1` |
| Hibernate | 7.4.5.Final · PostgreSQLDialect · `tfm/public` |
| Tomcat | 11.0.24, 포트 **8088** |
| 기동 시간 | 7.2초 |

**포트를 8088 로 옮겼다.** 8080 은 로컬 LLM 서버(`llama-server`)가 쓰고 있다.
`${TFM_PORT:8088}` 이라 환경변수로 바꿀 수 있다.

**뒤집힐 조건.** 없다. 3.x 로 돌아갈 방법이 없다.

---

---

## D32. 게임 AI 의 밴픽 모델은 "쓸 수 없다"가 아니었다 (2026-08-25)

**뒤집는 것.** `savefile.md` 가 스트림 2 를 "`LineModel` 88,154개가 전부 `{A,B}` 인
신경망 가중치. 분석에 쓸 수 없다"로 정리했다. 파서를 만들어 실제로 열어보니 **틀렸다.**
88,154개는 잎사귀였고, 그 위에 구조가 있다.

```
Banpick.MultiNetwork  Count=212
├─ Counters      10 × 2500셀   0 아닌 값 1,178~1,548   최대 2.68
├─ Pairs         10 × 2500셀   1,026~1,490            최대 2.88
├─ StatTiers     10 ×   64셀   40
├─ StrategyTiers 10 ×  450셀   120~306
└─ Props         10 × 2500셀   573~830
```

2500 = 50×50. **챔피언 쌍 행렬의 모양이고, 이름이 `Counters` 와 `Pairs` 다.**
이 프로젝트가 D14(카운터)·D11(시너지)에서 경기 데이터로 추정하려는 두 값과 정확히 같은 대상이다.
게다가 값이 실제로 학습돼 있다 — 절반가량이 0 이 아니다.

**그럼에도 티어·카운터·시너지 통계에는 쓰지 않는다.**

이 프로젝트의 전제는 **"게임이 못 하는 것"을 하는 것**이고(D6), 판단 기준은
**숫자의 정직함**이다. 게임 AI 의 가중치는 관측이 아니라 **게임의 의견**이다.
그 숫자를 가져다 쓰면 "실제 경기에서 이랬다"가 아니라 "게임 AI 는 이렇게 본다"가 되고,
그건 우리가 만들려는 것이 아니다. 게다가 학습 방법도, 무엇을 목표로 최적화됐는지도 모른다.

**두 곳에서는 값이 있다.**

1. **밴픽 시뮬의 상대 모델링** — 시뮬에서 상대는 이 AI 다(`banpick.md`).
   상대가 무엇을 고를지 예측하는 데 상대 본인의 정책보다 나은 근거는 없다.
2. **우리 추정치의 교차 검증** — 우리가 계산한 `counter_effect` 와 AI 의 `Counters` 가
   같은 쌍을 크게 다르게 본다면 둘 중 하나가 틀렸다는 신호다. 어느 쪽인지는 말해주지 않지만
   **어디를 들여다볼지는 알려준다.** `/gaps` 의 우선순위 근거로 쓸 수 있다.

**막는 것.** 이 값을 티어 점수·카운터 순위·시너지 순위에 **가산하지 않는다.**
D19 가 경기력 지표에 대해 정한 것과 같은 방식이다 — 신호로는 쓰되 점수에는 넣지 않는다.

**다음에 확인할 것.** 셀 인덱스 ↔ 챔피언 대응, `A`/`B` 의 의미, 10개 모델의 구분 축,
`Count=212` 의 정체. 이게 풀려야 1·2 를 실제로 쓸 수 있다. 안 풀려도 프로젝트는 진행된다.

---

---

## D33. 파서 레퍼런스 구현 완료 · 골든 파일 확보 (2026-08-25)

**만든 것.** `tools/nrbf.py`(MS-NRBF 저수준) · `tools/save_model.py`(도메인 추출) ·
`tools/make_baseline.py`(골든 파일 생성). 문서가 "있다"고 적어놨지만 실제로는 없던 자산이다.

**검증 — 문서의 실측치를 독립적으로 재현했다.**

| 항목 | 문서 | 파서 |
|---|---|---|
| slot_638064 초반 2밴 경기 | 107경기 | `{2: 214}` = 107×2 ✓ |
| slot_638064 스크림 인원 | 2명 17 · 3명 21 · 4명 113 | 동일 ✓ |
| slot_638064 등장 챔피언 | 31종 | 31 ✓ |
| slot_638377 GameStat / 스크림 | 233 / 117 | 233 / 117 ✓ |
| 스트림 2 객체 수 | 88,286 | 88,286 ✓ |
| **D20 ChampStat 순서 일치** | **79.5%** | **640/805 = 79.5%** ✓ |
| `savefile.md` 의 S2025 Day7 Set2 사례 | 밴픽 14개 | 필드 단위 일치 ✓ |

`slot_638683` 만 294/181 로 문서(289/177)보다 크다. 그 세이브는 문서 작성 뒤에도
플레이돼서 파일이 갱신됐다(수정일 8/11, 나머지 둘은 1/26). 파서 문제가 아니다.

**골든 파일의 계약은 바이트 일치다.** JSON 을 정규형으로만 쓴다 — 키 정렬,
UTF-8 그대로, 구분자 고정, 개행 LF. Java 의 직렬화도 이 형태를 맞춰야 한다.
`make_baseline.py --check` 가 재생성 없이 해시로 대조한다.

**설계에서 걸린 것 하나.** .NET enum 은 `{value__: 0}` 짜리 객체로 직렬화된다.
`TeamType` 이 그래서 JSON 으로 나가지 않았다. 풀지 않으면 값 비교도 안 되므로
**모든 enum 을 정수로 되돌리는 것을 파서 계층에서 처리**했다. `ChampionCategory` 등도 같은 모양이다.

**리스트 길이를 `_items` 로 세면 틀린다.** .NET `List<T>` 는 용량 여유분을 뒤에 `null` 로
달고 있어서 `_size` 만큼만 유효하다. 밴 2개인 경기가 `['A','B',null,null]` 로 보이는 이유다.

**다음.** 이제 Java 파서를 짤 수 있다. 다만 골든 파일이 막아주는 범위는 생각보다 좁다 — D34.

---

---

## D34. 골든 파일은 표본이 담고 있는 것만 지킨다 (2026-08-25)

**Java 파서가 첫 시도에 골든 파일을 통과했다.** 세이브 3개 · 경기 805건 · 바이트 단위 일치.

그래서 방어선이 진짜인지 확인했다. **`NrbfReader.readByte()` 에서 `& 0xFF` 를 빼고 돌렸다.**
D22 가 "골든 JSON 이 잡아준다"고 적어둔 바로 그 실수다.

**골든 테스트가 통과했다.** 변조한 채로.

**왜.** 세이브 파일에 0x80 이상인 바이트는 538회 넘게 나온다. 그런데 전부 `readLength()`
안에서만 쓰이고, 그 함수는 내부에서 `& 0x7F` 와 `& 0x80` 으로 다시 마스킹한다.
Java 에서 이 두 연산은 부호 확장된 음수에도 올바르게 동작한다.
부호가 실제로 문제가 되는 곳(`ObjectNullMultiple256` 의 개수 ≥ 128, 레코드 타입 ≥ 0x80)은
**이 데이터에 우연히 나타나지 않는다.**

**결론. 골든 파일은 회귀 테스트지 계약 테스트가 아니다.**
"실제 파일로 만든 정답지"라는 성질이 안심을 주지만, 지켜주는 것은
**표본이 우연히 담고 있는 경우**뿐이다. 표본에 없는 입력은 자유롭게 틀릴 수 있다.

**조치.** `NrbfReaderTest` 를 만들어 바이트 수준 계약을 직접 못 박았다 —
`readByte` 의 부호 없는 반환, `readSByte` 의 부호 유지, 7-bit 길이의 다중 바이트,
128바이트 넘는 문자열, 버퍼 초과 시 예외. 같은 변조를 다시 넣으니 **이번에는 실패한다.**

**일반화.** 이 프로젝트는 앞으로도 "실제 데이터로 검증했다"를 근거로 쓸 것이다(D14·D15·D20 전부 그렇다).
그때마다 물어야 한다 — **이 데이터가 그 경우를 실제로 담고 있나.**
담고 있지 않으면 검증한 것이 아니라 검증하지 않은 것이다.

**테스트를 믿기 전에 실패시켜 본다.** 통과하는 테스트는 그 자체로는 아무것도 증명하지 않는다.

### 확인 (2026-08-25) — 같은 패턴이 두 번 더 나왔다

서브에이전트 리뷰에서 골든 파일이 못 잡는 결함이 두 개 더 드러났다. 둘 다 D34 와 같은 이유다.

| 결함 | 왜 골든 파일이 못 잡나 |
|---|---|
| `PrimitiveType.UINT64` 를 부호 있는 long 으로 읽음 | **세이브에 UInt64 가 한 번도 안 나온다** (실측: Boolean·Byte·Int32·Int64 3회·Single·DateTime 2회뿐) |
| `objects` 가 `HashMap` — 레퍼런스의 dict 는 삽입 순서 보장 | 정렬 키가 같은 `PatchNews` 가 표본에 없다 |

**"표본에 없어서 안 드러난다" 가 세 번 반복됐다.** 이제 이건 우연이 아니라 이 검증 방식의
구조적 한계로 취급한다 — 골든 파일이 통과했다는 것은 **"레퍼런스와 같다"가 아니라
"이 세 파일에서 레퍼런스와 같다"** 는 뜻이다.

### 이제 규칙이다 — 테스트는 변조로 검증한다 (2026-08-26)

`IngestService` 를 만들면서 이 방식을 절차로 굳혔다. **테스트를 믿기 전에 그 테스트가
막으려는 버그를 실제로 넣어보고, 실패하는 것을 확인한다.**

두 번 다 값어치를 했다.

**하나.** `tdd-guide` 가 만든 D20 테스트는 "블루 4명 · 레드 4명 · 챔피언 8개가 안 겹침" 을
단언했다. **인덱스로 매칭해도 그 조건은 그대로 만족한다** — 진영만 뒤바뀔 뿐 개수와
중복 여부는 같다. 게다가 그 조건은 스키마의 `match_participant_unique_champ` 가
이미 강제하던 것이라 사실상 빈 테스트였다. 실제 `BluePick`/`RedPick` 과 대조하도록 다시 썼다.

**둘.** 다시 쓴 테스트에 진짜로 인덱스 매칭 버그를 넣고 돌렸다.
**11개 중 D20 하나만 실패했다.** 나머지 10개가 통과한 것도 맞는 신호다 — 각자 다른 것을 지킨다.

**이름이 방어선인 것과 실제로 방어하는 것은 다르다.** 그 차이는 변조해봐야만 드러난다.

**고친 방식.** 값을 맞추려 하지 않고 **크게 실패하게** 만들었다.
UInt64 가 long 으로 표현 안 되면 던지고, `intOf` 가 Int32 아닌 수치 타입을 만나면 던지고,
`TodayData` 가 없으면 던지고, short 축소가 범위를 넘으면 던진다.
표현할 수 없는 값을 그럴듯한 숫자로 바꿔 내보내는 것이 이 프로젝트에서 가장 나쁜 실패다.

---

---

## D35. 팀 인원 4명 · 픽 4 · 밴 3 고정. 아닌 경기는 적재하지 않는다 (2026-08-25)

**결정.** 우리가 다루는 경기는 하나뿐이다 — **4v4, 팀당 픽 4, 팀당 밴 3.**
스크림은 같은 4v4 이되 밴이 없다. 이 형식이 아닌 경기는 **적재 단계에서 버린다.**

V1 스키마는 팀 인원을 2~4 로 열어두고 밴 수를 가변으로 다뤘다. 구 데이터에 그런 경기가
섞여 있었기 때문이다. 그러나 **가변으로 모델링하면 적재·집계·시뮬레이터가 전부
"이 경기는 몇 명이고 밴이 몇 개인가" 를 물어야 하고, 그 물음은 어디에서도
의미 있는 답을 만들지 않는다.** 형식이 다른 경기의 승률은 같은 표에 들어갈 수 없다.

**적재 조건.**

| 종류 | 받는 것 | 버리는 것 |
|---|---|---|
| 공식전 | 픽 4+4 · 밴 3+3 | 그 외 전부 |
| 스크림 | 인원 4+4 · 밴 없음 | 그 외 전부 |

**이 결정으로 실제 버려지는 것 (실측).**

| 슬롯 | 공식전 | 스크림 |
|---|---|---|
| 638064 | 107 / 278 버림 (2밴, Day 7~15, 챔피언 풀 31종 시절) | 38 / 151 버림 (2v2 17 · 3v3 21) |
| 638377 | 0 / 233 | 0 / 117 |
| 638683 | 0 / 294 | 0 / 181 |

**버려지는 107경기는 픽이 양쪽 4개씩이고 ChampStat 도 8명이다.** 승률 자체는 성립한다.
그럼에도 버리는 이유는 **한 가지 형식만 다루기로 했기 때문**이고, 그 단순함이
경계 조건을 코드 전체에서 없애준다. 손실은 한 슬롯의 초반 구간에 몰려 있고
나머지 두 슬롯은 하나도 잃지 않는다.

**DB 가 강제하는 것과 못 하는 것.**

`V2__fixed_team_size.sql` 이 `team_size = 4` 로 조인다. 코드가 실수해도 여기서 막힌다.
다만 **"밴이 정확히 3개" 는 CHECK 로 표현할 수 없다** — 행 개수에 대한 제약이라
트리거가 필요하다. 트리거를 두는 대신 적재에서 막고 테스트로 고정한다.
`ban_order` 의 `1~3` 제약은 3을 넘는 것만 막지, 2개만 있는 것은 막지 못한다.

**파서는 관대하게 둔다.** 구 데이터를 읽을 수 있어야 하므로 배열 길이를 상수로 가정하지 않는다.
**읽는 것과 받아들이는 것은 다른 문제다.** 고정 규칙은 적재에서 적용된다.

**뒤집힐 조건.** 게임이 다른 형식의 경기를 추가하고 그것이 세이브에 남으면 그때 다시 본다.

---

---

## D36. 적재의 트랜잭션 경계를 셋으로 나눈다 (2026-08-26)

**문제.** `ingest()` 한 메서드를 `@Transactional` 로 감싸고 그 안에서 슬롯 생성·기록·적재를
다 했다. 실패하면 `run.fail(...)` 로 기록을 남기고 예외를 다시 던졌는데,
**그 기록도 롤백과 함께 사라졌다.** "실패를 조용히 삼키지 않는다" 는 원칙을 지키려고 쓴 코드가
정확히 반대로 동작하고 있었다. 서브에이전트 리뷰 두 곳이 같은 지점을 CRITICAL 로 짚었다.

**결정.** 경계를 셋으로 나눈다. 각각 다른 이유로 다른 트랜잭션이어야 한다.

| 클래스 | 왜 따로인가 |
|---|---|
| `SlotRegistry` | 슬롯은 적재가 실패해도 남아야 한다. 실패 기록이 슬롯을 참조하므로, 같이 롤백되면 외래키 위반으로 기록 자체가 불가능해진다 |
| `IngestRunRecorder` | **실패 기록이 실패와 함께 사라지면 안 된다** |
| `SaveLoader` | 파일 하나가 통째로 들어가거나 통째로 안 들어간다. 완료 기준이 "적재 건수가 파서가 세는 건수와 정확히 일치" 라 부분 적재를 허용하지 않는다 |

`IngestServiceImpl` 에는 `@Transactional` 이 없다. 조율만 한다.

**고치면서 드러난 두 번째 함정.** 실패 기록이 커밋되게만 하면
`existsBySlotIdAndFileHash` 가 그 실패까지 "이미 적재됨" 으로 보고 **재시도를 영영 막는다.**
성공한 기록만 세도록 바꿨다(`...AndErrorMessageIsNull`). 한 곳을 고치면 그 전제 위에 있던
다른 코드가 틀려진다 — 원인을 고칠 때는 그 원인에 기대고 있던 것을 같이 봐야 한다.

**파일 단위 롤백은 유지한다.** 경기 하나가 실패하면 그 파일 전체가 롤백된다.
대신 **어느 경기에서 넘어졌는지**를 예외 메시지에 담는다 — 롤백되면 그것이 유일한 단서다.

---

## D37. 성능은 재고 나서 고친다 (2026-08-26)

리뷰가 "행마다 SELECT+INSERT 가 나간다", "배치가 안 걸린다" 를 지적했다. 그럴듯했지만
**코드만 읽고 낸 추론**이었다. 이 프로젝트는 데이터로 확인할 수 있는 것을 추측하지 않는다.

**측정했다** (`IngestCostTest`, slot_638683 · 경기 475건).

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| JDBC statement | 11,901 | **1,310** |
| 경기당 | 25.1 | **2.8** |
| 엔티티 INSERT(행) | 6,181 | 6,181 |

**지적이 맞았고, 그것도 거의 정확히 맞았다.** 여분 SQL 5,720개는 `@EmbeddedId` 행 수
(참가자 3,800 + 밴 1,764 + 패치이벤트 ≈ 5,714)와 거의 일치했다 — **행 하나당 SELECT 하나씩**
더 나가고 있었다.

**원인.** `@EmbeddedId` 는 ID 가 미리 채워져 있어 Spring Data 의 기본 판정이 "기존 행" 이 된다.
그러면 `save()` 가 `persist()` 가 아니라 `merge()` 를 부르고, INSERT 전에 SELECT 를 한 번 더 낸다.
**있을 리 없는 행을 매번 확인한 것이다.**

**고친 것.** `Persistable<ID>` 구현(`isNew()` 항상 true) + `hibernate.jdbc.batch_size: 50`.
이 세 테이블은 적재에서만 쓰고 수정하지 않으므로 안전하다. 같은 키를 두 번 넣으면
유니크 제약에서 크게 실패한다 — 조용히 덮어쓰지 않는다.

**측정 코드 자체에도 버그가 있었다.** `엔티티 수 − statement 수` 로 "SELECT 수" 를 구했는데,
배치가 걸리면 엔티티 여러 개가 statement 하나로 묶이므로 그 뺄셈이 성립하지 않는다.
음수가 나와서 알았다. **재는 도구도 틀릴 수 있다.**

**임계값을 걸지 않았다.** 무엇이 정상인지 모르는 채 임계값을 걸면 방어선이 아니라
현재 상태의 박제다. 숫자를 출력하고 사람이 본다.

---

## D38. 워처 — 세이브 파일 쓰기 감지로 실시간 적재 (2026-08-27)

**D12 가 실환경에서 확인됐다.** 그때는 미확정이었던 두 가지가 이제 관측이다.

```
21:54:22  적재 완료 slot_638683925954242004.tfm — 경기 +294 · 스크림 +182
21:55:20  적재 완료 slot_638683925954242004.tfm — 경기 +0 · 스크림 +1
```

두 번째 줄이 핵심이다. 스크림 한 판이 끝나자 게임이 저장했고 워처가 **그 한 판만** 늘렸다.
사용자는 아무것도 누르지 않았다. 저장 간격 57초로 D17 의 66~93초와 같은 자리고,
`적재 실패` 가 한 줄도 없다 — **1.5초 디바운스가 잘린 스트림을 막았다.**
D17 의 "게임 실행 중에도 파일이 잠기지 않는다" 도 그대로였다. 임시 복사 우회는 넣지 않았다.

**설계.**

| 항목 | 결정 | 이유 |
|---|---|---|
| 디바운스 | **파일별**, 마지막 변경 기준 리셋 | 폴더 전체에 타이머 하나면 한 슬롯이 계속 저장되는 동안 다른 슬롯이 영영 안 들어간다 |
| 중복 판정 | **하지 않는다** | D17 에서 쓰기 3~4건 중 1건은 경기가 안 는다. 그 판단은 `IngestService` 의 해시 검사가 할 일이다. 워처가 캐시로 가로채면 판단이 두 곳으로 갈라진다 |
| 슬롯 판정 | `SlotFile.isSlotFile()` **하나만** | 정규식을 한 벌 더 두면 두 곳이 따로 늙는다. 그 규칙(D28)은 어긋나는 순간 커리어가 두 벌 적재된다 |
| 구독 이벤트 | CREATE · MODIFY (DELETE 제외) | 지워진 파일을 적재할 일이 없다. CREATE 를 빼면 **원자적 이동으로 들어온 파일**을 놓친다 — 그쪽은 MODIFY 가 안 온다 |
| 수명 | **일회용.** `stop()` 뒤 `start()` 는 던진다 | 스케줄러는 되살아나지 않는데 감시 스레드만 뜨면 "감시는 도는데 적재는 0" 이 된다. Flyway 사고와 같은 모양이다 |

**조용한 실패를 막으려고 시끄럽게 만든 곳.** 전부 실제로 있던 구멍이다.

- `stop()` 이 진행 중 적재를 안 기다렸다 → Spring 이 DataSource 를 먼저 닫아 멀쩡한 적재가
  종료 순서 때문에 실패한다. `awaitTermination` 을 넣었다
- 빈 `TFM_SAVE_DIR=` 이 **현재 작업 디렉터리를 감시**하게 만들었다. `Path.of("")` 는 존재하는
  폴더라 검사를 그냥 통과한다 — 죽지도 동작하지도 않는 상태다
- 디바운스 0·음수를 안 막았다. 증상이 워처가 아니라 **파서의 "적재 실패"** 로만 나타난다
- `watchLoop` 이 `Error` 를 못 잡아 stderr 에만 찍혔다. 앱 로그에는 한 줄도 안 남는다
- `stop()` 이 대기 중인 적재를 조용히 취소했다 (D39 참고)

**`tfm.read-only` 는 선언만 있고 강제되지 않는다.** 세이브를 쓰는 코드가 없어 가드할 대상이
없다. 값을 바꿔도 동작은 하나도 안 바뀐다 — **이 플래그를 신뢰하면 안 된다.** 실제 보장은
감시 전후로 수정시각·해시가 그대로인지 대조하는 테스트다. 남길지 없앨지는 아직 안 정했다.

**뒤집힐 조건.** 게임이 세이브를 배타적으로 잠그는 버전이 나오면 임시 복사 우회가 필요하다.
그때는 `적재 실패` 로그가 저장마다 쌓이는 것으로 드러난다.

---

## D39. 워처만으로는 새는 슬롯이 있다 — 기동 시 따라잡기 (2026-08-27)

**발견 경위.** 워처를 띄우고 스크림을 돌린 뒤 운영 DB 를 보니 **커리어 3개 중 1개만** 있었다.

**원인.** 워처는 이벤트 기반이다. 게임이 열지 않은 슬롯은 이벤트가 영영 안 온다.
앱이 꺼져 있던 동안의 저장도 같은 이유로 샌다. 누적이 이 프로젝트의 존재 이유인데
거기에 구멍이 있었다.

**결정.** 기동 시 폴더를 한 번 훑어 모든 슬롯을 적재한다. **`OVERFLOW` 처리를 그대로 쓴다** —
"폴더를 다시 훑어 놓친 변경을 따라잡는다" 가 정확히 같은 동작이라 `rescan()` 하나로 합쳤다.

**이 선택이 공짜로 준 것.** 따라잡기가 워처의 **단일 디바운스 스레드**에 실려 직렬화된다.
별도 스레드였다면 기동 직후 게임이 저장할 때 따라잡기와 워처 이벤트가 같은 파일을 동시에
`ingest()` 할 수 있었다. 테스트가 `ingest()` 동시 진입 최댓값을 세서 이걸 고정한다.

**실측.** 나머지 두 커리어가 들어왔고, **D35 의 제외 건수가 문서와 정확히 일치했다.**

```
적재 완료 slot_638064443900084435.tfm — 공식 171건(제외 107) · 스크림 113건(제외 38) · 패치 11건
적재 완료 slot_638377270248153191.tfm — 공식 233건(제외 0)   · 스크림 117건(제외 0)  · 패치 19건
이미 적재된 내용이다: slot_638683925954242004.tfm (0a0a54385292)
```

공식 총계도 맞는다. 파서 레퍼런스의 805건에서 107 을 빼면 698 이고, 실제 적재도
171+233+294 = 698 이다. 세 번째 슬롯이 해시로 걸러진 것은 **따라잡기가 이미 있는 것을
두 벌로 만들지 않는다**는 증거다 — D28 이 막으려던 사고의 반대편이다.

**기동을 막는다.** 따라잡기가 던지면 앱이 안 뜬다. `SaveWatcher.start()` 가 빈 초기화에서
이미 같은 검사를 하므로 기동 정책은 이미 "폴더가 성해야 뜬다" 이고, 러너에서만 관대하게 굴면
정책이 두 갈래가 된다. **다만 여기로 올라오는 예외가 구조적 문제만은 아니다** —
네트워크·외장 드라이브 마운트가 늦으면 `UncheckedIOException` 이 나고, 몇 초 뒤면 저절로
해소됐을 일로 기동이 실패한다. 그 경우가 실제로 생기면 재시도를 넣는다.

**`stop()` 이 취소한 것을 로그로 남긴다.** 따라잡기는 디바운스 뒤에 실행되도록 **예약만** 한다.
기동 1.5초 안에 앱이 종료되면 "따라잡기 시작" 로그만 남고 한 건도 안 들어간 채 끝나는데,
로그만 보면 성공한 것처럼 보인다 — **막으려던 실패 방식과 같은 모양이다.**

---

## D40. 테스트는 통과가 아니라 변조로 검증한다 (D34 의 반복 확인)

**D34 를 규칙으로 둔 뒤 두 번 더 같은 일이 있었다.** `tdd-guide` 가 준 테스트가
잡겠다고 적어둔 변조를 실제로는 못 잡았다.

| 테스트 | 왜 통과했나 |
|---|---|
| `OVERFLOW` 재훑기 | 워처를 켠 채로 파일을 만들어, 정상 CREATE 이벤트만으로 통과했다. `rescan()` 을 빈 메서드로 바꿔도 초록이었다 |
| `stop()` 타이머 취소 | 취소할 타이머가 없는 상태에서 정지해, 취소 로직을 통째로 지워도 통과했다 |
| `ENTRY_CREATE` 구독 | `Files.writeString` 이 CREATE 뒤에 MODIFY 를 내서, CREATE 구독을 지워도 MODIFY 로 걸렸다 |

**공통점.** 셋 다 **검증 대상 말고 다른 경로로도 단언이 만족됐다.** D20 때와 같은 종류다.

**그래서 이번 워처는 변조 28종을 걸어 각각 실패하는 것을 실측했다.** 위 셋은 그 과정에서
드러났고 테스트를 고쳤다(재훑기는 워처를 끄고, 정지는 만료 전에, 생성은 원자적 이동으로).

**규칙.** 테스트를 받으면 "이 단언이 다른 경로로도 만족되는가" 를 묻고, **막겠다는 변조를
실제로 걸어본다.** 통과했다는 사실은 근거가 아니다.

한 가지 더: `slot_x.TFM` 과 `slot_x.tfm` 을 같은 폴더에 만들던 테스트는 Windows 에서
판정이 아니라 **준비 단계에서** `FileAlreadyExistsException` 으로 깨졌다. 대소문자를 안 가리는
파일시스템에서는 그 둘이 같은 파일이다 (D27 의 대가 목록에 추가).

---

## D41. Spring Security 를 뺀다 (2026-08-27)

**증상.** 기동할 때마다 로그에 이런 것이 떴다.

```
Using generated security password: 766754c1-9335-49a7-a09c-e68774c9261b
```

`spring-boot-starter-security` 가 의존성에 있어서 자동설정이 붙었다. 화면이 없어 지금은
무해하지만, `/tier` 를 만드는 순간 **기동할 때마다 바뀌는 UUID 를 물어보는 로그인 창**이
앞을 막는다. 소스에는 security 참조가 한 줄도 없었다 — 쓰려고 넣은 게 아니라
Initializr 에서 딸려온 것으로 보인다.

**결정.** 의존성을 뺀다. `spring-boot-starter-security` 와 `spring-security-test` 둘 다.

**이유.** D27 과 같은 자리다. 이건 로컬 `java -jar` 이고, 세이브 파일 옆에서 도는
단일 사용자 앱이라 인증할 상대가 없다. 사용자 판단도 같았다 — "어차피 이 컴퓨터에서만 한다".

**`permitAll()` 로 열어두지 않은 이유.** 그러면 "보안이 있는데 다 열어놨다" 가 된다.
실제로는 없는 방어선을 있는 것처럼 보이게 하는 것이고, `tfm.read-only` 가
선언만 있고 강제되지 않는 것(D38)과 같은 종류의 착시다. 없는 것은 없어야 한다.

**뒤집힐 조건.** 이 앱을 이 PC 밖에서 띄우게 되면. 그때는 노출면이 완전히 달라지므로
`permitAll()` 을 되돌리는 게 아니라 처음부터 다시 설계해야 한다. 로컬 전제(D22·D27)가
깨지는 것이므로 그쪽 결정이 먼저 뒤집힌다.

---

## D42. 감쇠의 두 인자는 "경기 이후 흐른 양"이다. 쌍은 두 챔피언의 변경을 합친다 (2026-08-27)

**문제.** D15a 의 식 `w(g) = 0.5^(자기변경횟수/2) × 0.5^(경과패치수/12)` 에서
"자기변경횟수" 가 무엇의 횟수인지가 적혀 있지 않았다. 두 읽기가 가능하다.

- 경기 시점까지의 누적 변경 횟수 (`match_participant.change_count` 가 그대로 이 값이다)
- 경기 이후 기준 시점까지의 변경 횟수

**결정.** **후자다.** `기준시점_누적 − 경기시점_누적` 을 넘긴다.

**이유.** 감쇠가 답하는 질문은 "이 관측이 지금 얼마나 유효한가" 다. 다섯 번 너프된 챔피언의
**최신** 경기는 온전히 유효하고, 그 다섯 번의 너프 **이전** 경기가 낡은 것이다. 누적값을
그대로 쓰면 정확히 반대가 된다 — 많이 바뀐 챔피언은 최신 경기까지 통째로 깎이고,
한 번도 안 바뀐 챔피언은 몇 시즌 전 경기가 온전한 무게로 남는다.

**이게 위험한 이유.** 뒤집혀도 값은 여전히 (0,1] 안이고, 승률도 그럴듯한 범위에서 나온다.
예외도 로그도 없다. `DecayWeightTest` 의 "두 인자는 흐른 양이다" 를 문서로만 두면 지켜지지
않으므로 `of()` 가 음수를 받으면 던지게 했다 — 뺄셈을 뒤집으면 거기서 걸린다.

**쌍의 가중치는 두 챔피언의 변경 횟수를 합한다.** D15 는 단일 챔피언만 이야기하는데
카운터·시너지는 둘이 얽혀 있다. 최댓값을 쓰면 "둘 다 두 번씩 바뀐 쌍" 이 "한쪽만 두 번
바뀐 쌍" 과 같은 무게를 받는데, 앞쪽이 분명히 더 낡았다. 어느 쪽이 바뀌어도 그 상성은
낡는다는 것이 합산의 근거다.

**뒤집힐 조건.** 합산 때문에 쌍은 단일 챔피언보다 두 배 빠르게 낡는다. 이것 때문에
유효표본 `ess` 가 표본 기준선 10(D9)을 못 넘기는 쌍이 많아지면 최댓값으로 바꾼다.

**측정했다 — 뒤집지 않는다 (2026-08-27, D49).** 원시 10경기 이상 1,174쌍 중 `ess` 10 이상이
1,106쌍(94%)이고 `ess/games` 평균이 0.965 다. 유효표본이 거의 마르지 않는다.

---

## D43. Bradley-Terry 는 승률 대입이 아니라 MM 반복으로 적합한다 (2026-08-27)

**문제.** D14 는 기대 승률 식만 준다.

```
expected(A,B) = p_A(1-p_B) / ( p_A(1-p_B) + p_B(1-p_A) )
```

`p_A` 에 그 챔피언의 관측 승률을 그대로 넣으면 한 줄로 끝난다. 그런데 그러면
Bradley-Terry 를 쓰는 의미가 없다.

**결정.** 승패 목록에서 MM 반복으로 강도 `s` 를 적합하고 `expected = s_A/(s_A+s_B)` 를 쓴다.

**이유.** **드래프트는 무작위 매칭이 아니다.** 어떤 챔피언은 센 상대만 만나고 어떤 챔피언은
약한 상대만 만난다. 관측 승률은 그 편차를 챔피언 강도로 착각한다. 걷어내려는 것이 바로
챔피언 강도인데, 그 추정치 자체가 상대 풀에 오염되어 있으면 잔차에 오염이 남는다.
MM 반복은 "누구를 이겼는가" 를 함께 본다.

**D14 의 식을 버리는 것이 아니다.** 강도를 승산 `p/(1-p)` 로 주면
`s_A/(s_A+s_B)` 는 위 식과 대수적으로 같다. 문서의 식은 이 구현의 특수한 경우다.
`BradleyTerryTest` 가 두 값이 일치하는 것을 못 박는다.

**가상 관측으로 정규화한다.** 강도 1 인 가상 상대와 1경기를 반씩 나눠 가진 것으로 친다.
정규화가 없으면 두 가지가 깨진다 — 전승 챔피언의 강도가 무한대로 발산하고,
서로 붙은 적 없는 두 무리 사이의 상대 크기가 정해지지 않는다. 슬롯이 셋이라 후자는
`GLOBAL` 스코프에서 실제로 일어날 수 있다.

**이 값을 `analysis_config` 로 빼지 않았다.** 화면에 드러나는 임계값이 아니라 적합이
유한하게 끝나게 하는 수치 장치다. 사용자가 조정할 것이 아니다.

**수렴하지 않으면 던진다.** 반복 한도에서 조용히 빠져나오면 절반쯤 수렴한 강도가
그대로 `expected_win_rate` 에 저장되고, 그 값도 0~1 사이라 아무도 눈치채지 못한다.

**뒤집힐 조건.** 4v4 라 한 관측이 아군 3명에 오염되어 있다(D19 가 짚은 것과 같은 문제).
표본이 충분히 쌓이면 팀 단위 BT(팀 강도 = 구성원 강도의 합)로 올리는 것이 더 정확하다.
지금은 쌍 단위로 둔다 — D14 의 분산 분해도 쌍 단위로 측정한 값이다.

---

## D44. 집계 임계값에 코드 기본값을 두지 않는다 (2026-08-27)

**결정.** `AnalysisConfig` 는 `analysis_config` 테이블의 여섯 키를 전부 요구한다.
하나라도 없으면 던진다. 코드에 기본값이 없다.

**이유.** 기본값을 두면 임계값이 시드(V1)와 코드에 두 벌이 되어 반드시 어긋난다 —
루트의 `schema.sql` 을 없앤 것과 같은 이유다. 그리고 키가 빠졌을 때 조용히 기본값으로
넘어가면 **"설정을 바꿨는데 결과가 안 바뀐다"** 가 되는데, 로그만 보면 성공이다.
빈 `TFM_SAVE_DIR` 가 현재 작업 디렉터리를 감시하게 만든 것과 정확히 같은 모양이다.

**모르는 키는 무시한다.** 설정 항목을 추가하는 마이그레이션이 구버전 앱의 기동을 막을
이유가 없다. 없는 것은 위험하고 남는 것은 위험하지 않다 — 대칭이 아니다.

**뒤집힐 조건.** 없다. 이건 D15 의 "`analysis_config` 로 빼서 조정 가능하게 둔다" 를
실제로 성립시키는 조건이다.

---

## D45. 감쇠는 방향이 아니라 거리를 잰다 (2026-08-27)

**문제.** D24 는 화면의 패치 선택을 "집계 대상을 자르는 필터가 아니라 추정 시점의 선택" 으로
정했다. 그런데 과거 패치 P 를 고르면 **P 보다 뒤에 치러진 경기**가 생긴다. 그 경기들의
"경과 패치 수" 는 음수가 된다. D42 가 넣은 음수 가드에 그대로 걸려서, 과거 패치를 고르는
순간 집계가 통째로 죽는다.

**결정.** 두 인자 모두 **절댓값**으로 준다. 감쇠는 거리를 재는 함수다.

**이유.** 인접한 패치의 메타는 서로 닮았다. 패치 P 직후의 경기는 P 에 대해 P 직전의 경기와
같은 정도로 정보를 준다 — 한쪽만 버릴 근거가 없다. 자기 변경도 마찬가지다. P 이후에 버프된
챔피언의 P 이후 데이터는 P 를 설명하기에 딱 그 버프만큼 부적합하고, 그건 P 이전 데이터가
너프 하나만큼 부적합한 것과 대칭이다.

**기본 시점에서는 아무것도 안 바뀐다.** 기준이 가장 최근 패치면 모든 경기가 과거라 절댓값을
씌우나 마나 같은 값이다. 차이는 과거 패치를 골랐을 때만 난다.

**음수 가드는 그대로 둔다.** 절댓값은 `ReferencePoint` 가 씌우고, `DecayWeight` 는 여전히
음수를 받으면 던진다. 뺄셈 방향을 뒤집는 실수(D42 가 경고한 그것)를 잡는 자리는 그대로 있어야
한다 — 절댓값을 감쇠 함수 안으로 옮기면 그 실수가 조용히 정상값이 된다.

**`GLOBAL` 스코프의 기준 시점.** 슬롯마다 패치 역사가 따로 생성되므로 슬롯을 가로지르는
패치 축은 없다(D24). `GLOBAL` 은 **슬롯별로 그 슬롯의 마지막 패치를 기준 시점으로 잡아
각각 감쇠한 뒤 합산한다.** 커리어가 3개면 감쇠 기준도 3개다.

**뒤집힐 조건.** 패치 전후의 비대칭이 실제로 측정되면 — 예를 들어 너프 이후 데이터가
너프 이전 데이터보다 그 패치를 훨씬 잘 설명한다면 — 방향별로 다른 반감기를 줘야 한다.
지금은 그걸 잴 표본이 없다.

---

## D46. 집계 조회는 행만 꺼낸다 — 전개도 감쇠도 SQL 에 넣지 않는다 (2026-08-27)

**논점.** 쌍 전개(승리 팀 4명 × 패배 팀 4명)와 감쇠 가중치를 SQL 로 하면 한 번의 쿼리로
집계가 끝난다. `self join` 과 `power(0.5, ...)` 로 충분히 쓸 수 있다.

**결정.** SQL 은 참가자 행을 꺼내는 일만 한다. 전개·감쇠·축소는 전부 Java 순수 함수다.

**이유.** **DB 없이 변조로 검증할 수 있는지가 갈림길이다.** "아군끼리는 쌍이 안 생긴다",
"양방향 가중치가 같다", "두 챔피언의 변경 횟수를 합친다" 같은 것은 조인 하나만 어긋나도
깨지는데 결과는 여전히 0~1 사이의 그럴듯한 승률이다. SQL 안에 있으면 실제 DB 를 띄워야만
확인할 수 있고, 그건 Claude 가 비밀번호를 넣지 못해 사용자 손을 거쳐야 한다는 뜻이다 —
검증 주기가 느려지면 변조 검증(D40)이 실제로는 안 돌아간다.

**비용은 작다.** 경기 1,200건에 참가자 8명이면 1만 행이다. 전부 메모리에 올려도 몇 MB 고,
쌍으로 펴도 2만 개다. D37 이 정한 대로 성능은 재고 나서 고친다.

**SQL 이 조용히 틀리는 자리는 따로 있다.** 전개가 아니라 **무엇이 빠지는가** 다.
`patch` 조인이 INNER 면 커리어 시작 직후 경기가 통째로 사라지고, `include_scrim` 조건이
빠지면 스크림이 공식전 표에 섞인다. 둘 다 예외를 내지 않고 "데이터가 좀 적네"·"승률이 좀
이상하네" 로만 보인다. 그래서 DAO 테스트는 정확성이 아니라 **누락**을 겨냥해 썼다.

**`team_size = 4` 필터는 두 번째 방어선이다.** 처음엔 이것도 같은 종류의 누락 위험으로 봤는데,
테스트를 돌려보니 V2 의 `CHECK (team_size = 4)` 때문에 비4v4 경기는 **DB 에 들어오지도
못한다.** 규칙이 문서가 아니라 제약으로 있는 것이다(D35 가 의도한 그대로다). 그래도 필터는
남긴다 — D35 의 뒤집힐 조건이 실현돼 2·3인 경기를 다시 적재하게 되면, 그때 카운터 집계가
그것을 섞지 않게 막는 것은 이 필터뿐이다. 제약을 트랜잭션 안에서만 떼어내 그 상황을 만들고
필터가 실제로 걸러내는지 확인했다.

**기준 시점의 누적 변경 횟수는 적재와 같은 방식으로 센다.** `SaveLoader` 는
`champion_patch_event` 를 전부 세고 `is_new` 를 뺀 적이 없다. 집계에서만 걸러내면
두 누적의 기준이 어긋나 그 차이가 그대로 감쇠 지수로 들어간다.

**뒤집힐 조건.** 슬롯이 수십 개가 되거나 경기가 수십만 건이 되면 메모리 전개가 안 맞는다.
그때는 SQL 로 내리되, 지금의 순수 함수를 골든 값 생성기로 써서 두 결과를 대조한다.

---

## D47. 기저 강도는 스코프마다 따로 잡는다. 스크림 포함/제외는 두 벌을 미리 만든다 (2026-08-27)

**기저 강도.** `CAREER` 의 강도와 `GLOBAL` 의 강도는 다른 값이다. 커리어 안에서 센 챔피언과
전체에서 센 챔피언이 같으리라는 보장이 없다 — 커리어마다 리그 레벨과 선수 능력치가 다르고,
그 차이가 승률에 그대로 실린다. 한쪽 강도를 다른 쪽 잔차에 쓰면 **어느 쪽도 아닌 값**이
나온다. 그래서 Bradley-Terry 적합을 스코프마다 따로 돌린다.

`GLOBAL` 의 적합은 슬롯별로 감쇠가 끝난 관측을 **이어붙여서** 한 번에 돌린다. 슬롯마다
따로 적합해서 평균을 내면 그건 커리어별 강도의 평균이지 전체 강도가 아니다.

**스크림 포함 여부.** 두 벌을 미리 계산해서 둘 다 저장한다. 스키마가 `include_scrim` 을
유일키에 넣어둔 것이 그 뜻이다. 화면에서 껐다 켰다 하려면 미리 있어야 하고, 요청 때마다
집계를 다시 도는 것은 D12 가 약속한 응답 시간과 안 맞는다.

공식전만 698경기, 스크림까지 1,110경기라 어느 쪽을 보느냐로 표가 실제로 달라진다.
스크림은 시점 정보가 없어 워처가 관측 시각으로 보정한 값이고(D8), 상대도 연습 상대라
공식전과 조건이 다르다 — 섞을지 말지는 사용자가 볼 때 정할 문제이지 우리가 미리 정할
문제가 아니다.

**집계 한 바퀴는 한 트랜잭션이다.** 도중에 죽으면 아무것도 안 바뀐다. 절반만 갱신된 카운터
표가 화면에 뜨는 것보다 낫다. 그래서 `agg_run` 에는 **실패의 흔적이 남지 않는다** —
함께 롤백되기 때문이다. 그래도 되는 이유는 실패가 예외로 올라가서 조용하지 않기 때문이다.
`finished_at` 이 NULL 인 행은 실패 기록이 아니라 지금 돌고 있는 집계다.

**뒤집힐 조건.** 경기가 수십만 건이 되어 한 바퀴가 몇 분씩 걸리면 한 트랜잭션이 안 맞는다.
그때는 스코프별로 트랜잭션을 쪼개고, 화면이 어느 `agg_run` 의 결과를 보는지 명시해야 한다.

---

## D48. Bradley-Terry 는 매 반복 정규화해야 한다 — 작은 표본이 가린 실패 (2026-08-27)

**증상.** 실제 데이터로 집계를 처음 돌리자 죽었다.

```
IllegalStateException: Bradley-Terry 적합이 10000회 안에 수렴하지 않았다
```

테스트 16개가 전부 통과한 상태였다. 그런데 그 테스트들은 챔피언 2~5종에 관측 수십 개짜리였고,
운영 DB 는 챔피언 40종에 관측 19,200개다.

**원인.** 우도가 **거의 스케일 불변**이다. 강도를 전부 같은 배로 키워도 기대 승률
`s_A/(s_A+s_B)` 는 그대로이므로 우도가 거의 안 변한다. 가상 관측(D43)이 스케일을 붙잡고
있긴 하지만 챔피언당 1경기어치라 실제 수천 경기에 눌린다. 그래서 반복은 **강도 비율은 이미
맞았는데 절대 크기만 한없이 미끄러지는** 상태로 기어간다. 허용오차를 1e-6 으로 풀면 수렴하고
잠재 순위도 되찾는다 — 값이 틀린 게 아니라 끝나지 않는 것이었다.

작은 표본에서는 이게 안 드러난다. 챔피언이 몇 종이면 반복이 몇 번 만에 끝나서 평평한 방향을
따라 기어갈 시간이 없다.

**결정.** 매 반복 뒤 **기하평균이 1 이 되게 맞춘다.** 평평한 방향이 사라지고 수렴이 끝난다.

**부작용 하나를 받아들인다.** 가상 상대의 뜻이 "강도 1 인 상대" 에서 **"평균 강도인 상대"**
로 바뀐다. 각 챔피언을 평균 쪽으로 당기는 것이므로 정규화 항으로서는 오히려 자연스럽고,
기대 승률은 비율만 쓰므로 결과에 영향이 없다.

**테스트가 가짜였던 것도 같이 고쳤다.** 재현 테스트를 만들고 나서 "한 번만 반복하고 반환"
변조를 넣어봤더니 **통과했다.** 순위 회복(잠재 순위와 70% 일치)은 MM 반복 한 번만으로도
만족되기 때문이다. 순위를 보는 단언으로는 "수렴했다" 를 증명할 수 없다.

그래서 적합의 정의 자체를 단언으로 바꿨다 — **우도방정식**: 강도가 맞으면 그 강도로 예측한
승수가 실제 승수와 같아야 한다. 챔피언마다 기대 승수와 실제 승수의 차이가 1.5경기 안이어야
한다(정확히 0 이 아닌 것은 가상 관측 때문이다). 이 등식은 실제로 수렴해야만 성립하고,
한 번 반복 변조도 허용오차를 1e-2 로 푸는 변조도 여기서 잡힌다.

**교훈은 D34 와 같다.** 골든 파일이 표본에 담긴 것만 지키듯, 단위 테스트는 그 크기에서
드러나는 것만 지킨다. **수치 반복법은 운영 규모의 입력으로 한 번은 돌려봐야 한다.**
`workflow.md` 에 넣었다.

**뒤집힐 조건.** 챔피언이 수백 종이 되면 40×40 조밀 반복이 부담이 된다. 그때는 희소 표현과
더 빠른 해법(뉴턴, 또는 MM 의 가속)으로 바꾼다. 지금은 40종이라 문제가 아니다.

---

## D49. 첫 실물 집계 — D14 는 재현됐고, D42 는 뒤집지 않는다 (2026-08-27)

운영 DB(공식 698 · 스크림 412 · 슬롯 3개)로 카운터 집계를 처음 돌렸다. 설계가 실제
데이터에서 무엇을 하는지 처음 본 것이라, 결정 세 개에 답이 나왔다.

**D14 가 재현됐다.**

```
Fighter vs DarkMage   25경기   실제 76.0%   기대 43.5%   이득 15.0%p
```

D14 가 예고한 값은 `76.0% (기대 42.8%)` 였다. 실제 승률은 소수점까지, 기대 승률은 0.7%p
안에서 맞는다. 전체 승률 42% 짜리 약한 챔피언이 특정 상대에게 76% 라는 것이 파서·적재·집계를
새로 통과해 독립적으로 나왔다.

**다만 D14 의 "이득 +33.2%p" 는 축소 이전 값이다.** 우리 값 15.0%p 는
`축소추정 − 기대` 이고, 33.2%p 는 `raw − 기대` 다. D14 는 식을 `adjusted − expected` 로
써놓고 예시는 raw 로 계산했는데, D15(2단 축소)가 D14 보다 나중에 정해졌으니 그 예시가
축소 이전에 만들어진 것이다. **문서의 숫자가 낡은 것이지 구현이 다른 것이 아니다.**

**raw 정렬이 실패하는 방식은 D14 의 예고와 달랐다.** D14 는 "센 챔피언이 상단을 도배한다"
고 했는데, 실제로는 **작은 표본이 도배했다** — raw 상위 20 의 표본이 10~13경기에 몰려 있다
(`Ogre vs Ninja` 10경기 90%). 이득 상위 20 은 25·87·40·50경기다. 도배하는 챔피언은 없었다.
결과적으로 두 정렬의 상단 20 은 **5개만 겹친다**(기준 통과 1,174쌍 중). 정렬을 바꾼 값어치는
있었지만, 그 값어치의 출처는 강도 보정보다 **축소(D10)** 쪽이 컸다.

**해결됐다 — D35 제외였다.** D14 가 raw 정렬 예시로 든
`Werewolf vs Swordman 84.0% (n=25)` 를 직접 찾으니 DB 에는 13경기뿐이었다. 승률이 아니라
**표본 수가 절반**이었다.

골든 파일(파서 출력 전체, D35 제외 이전)에서 세어 갈렸다.

| 쌍 | 골든 (제외 전) | DB (제외 후) | 문서 D14 | 슬롯별 골든 |
|---|---|---|---|---|
| Werewolf vs Swordman | **25** | 13 | 25 | **[22, 0, 3]** |
| Fighter vs DarkMage | **25** | 25 | 25 | [0, 19, 6] |

`slot_638064443900084435` 는 **제외가 발생한 유일한 커리어**다(공식 107 · 스크림 38).
Werewolf-Swordman 은 25건 중 22건이 거기 있고, Fighter-DarkMage 는 **0건**이다.
그래서 하나는 절반이 되고 하나는 그대로였다. 같은 문서의 같은 숫자 25 가 서로 다른 결과를
낸 이유가 이것이다.

**D14 의 숫자들은 D35 이전에 측정된 것이고, 우리 구현은 정확하다.** 앞으로도 문서의 표본
수와 DB 가 어긋나면 먼저 이것을 의심한다 — `tools/pair_in_baseline.py` 로 바로 갈린다.

**진영 배정 오염은 아니었다.** 함께 본 전체 검산도 깨끗하다. 참가자 구성은 1,111경기가
**전부 4+4** 이고(다른 조합 0건), 승리 진영은 공식 342:356 · 스크림 197:216 으로 균형이다.
D20 이 경고한 "인덱스 매칭이었다면 20% 에서 진영이 뒤바뀐다" 는 흔적이 없다.

**D42 를 뒤집지 않는다 — 측정했다.**

```
원시 10경기 이상 1,174  →  ess 10 이상 1,106  (94% 생존)
ess/games 평균 0.965
```

쌍 감쇠를 합산해도 유효표본이 거의 마르지 않는다. 최댓값으로 바꿀 이유가 없다.
D42 의 "뒤집힐 조건" 은 발동하지 않았다.

**감쇠 자체가 아직 약하다.** 가중 합 32,103 / 원시 합 35,552 = **10% 만 깎였다.**
반감기 12패치가 현재 데이터 범위(슬롯당 패치 11~19개) 대비 길어서다. D15 가 "반감기 2와 12는
측정으로 정한 값이 아니다" 라고 남겨둔 그 자리인데, 지금은 그 값이 결과를 거의 바꾸지 않으므로
조정할 근거도 아직 없다.

**D47 의 근거가 성립한다.** 커리어별 상위가 GLOBAL 과 다르다. `Fighter vs DarkMage` 는
GLOBAL 에서 15.0%p 인데 `slot_638377...` 안에서는 7.9%p 다. 그리고 GLOBAL 과 CAREER 의
원시 합·가중 합이 정확히 같다(35,552 / 32,103) — `merge` 가 총량을 보존한다는 뜻이라
슬롯 합산이 새거나 중복되지 않았다.

**버그 하나가 이 출력에서 드러났다.** `agg_run` 의 `started_at` 과 `finished_at` 이
마이크로초까지 완전히 같았다. `now()` 는 트랜잭션 **시작** 시각을 돌려주는데 집계 한 바퀴가
한 트랜잭션이라(D47) 몇 번을 불러도 같은 값이 나온다. 소요 시간을 재려고 만든 컬럼이 늘
0 을 가리키는데 NULL 이 아니라서 눈으로는 정상으로 보인다. `clock_timestamp()` 로 바꿨고,
`finished_at > started_at` 을 단언하는 테스트를 넣었다.

---

## D50. 챔피언 티어 — 강도를 빼지 않는다. 밴률의 분모는 따로 둔다 (2026-08-27)

**티어에는 Bradley-Terry 보정을 하지 않는다.** D14 가 이미 정한 것인데, 카운터를 먼저
만들고 나면 같은 함수를 재사용하고 싶어지는 자리라 다시 적는다. 카운터는 챔피언 강도를
걷어내야 상성이 남지만, 티어는 **"이 챔피언이 센가" 를 묻는 것이므로 기저 승률 자체가
답이다.** 빼면 모든 챔피언이 50% 근처로 눌려 티어표가 통째로 무의미해지는데, 값은 여전히
0~1 사이라 그럴듯하다.

**시그니처가 그것을 강제한다.** `PerformanceCalculator.calculate` 는 상대 정보를 아예
받지 않는다. 강도를 빼도록 바꾸려면 입력부터 바꿔야 한다 — 문서가 아니라 타입이 규칙이다.

**축소 목표값은 0.5 이고, 이건 산술이다.** 4v4 한 경기에 승자 넷과 패자 넷이 있으므로
전체 챔피언 출전의 승률은 **정확히** 50% 다. 임의로 고른 상수가 아니라 그 스코프의 실제
평균이고, 표본이 없는 챔피언을 평균으로 두는 것이다. 테스트가 그 등식(`승리×2 = 출전`)을
못 박는다.

D19 는 "표본이 적을 때 상수(50%)보다 경기력으로 예측한 승률로 수렴시키는 것이 낫다" 고
했다. 맞는 말이지만 **그 회귀식이 아직 정해지지 않았다.** 정해지면 이 상수 자리에
챔피언별 예측값이 들어간다. 그때까지 `z_deal` 등 경기력 컬럼은 NULL 로 둔다 — 근거 없는
숫자를 채우느니 비어 있는 편이 낫다.

**티어 등급(`tier_grade`)도 아직 매기지 않는다.** 컷라인이 미결이고, 기저 승률 표준편차가
작아서 고정 컷을 쓰면 대부분 한 등급에 몰린다. `tools/tier_report.sql` 의 질의 3·4 가
분포와 백분위를 낸다 — 그걸 보고 정한다. 화면은 그때까지 추정 승률로 정렬하면 된다.

**밴률의 분모를 공식전 수로 분리한다 (V4).** V1 은 `ban_rate` 를 `bans / match_count` 로
만들었는데, 밴은 공식전에만 있고 `match_count` 는 스크림을 포함한다. 공식 698 · 스크림 413
이므로 스크림 포함 스코프에서 **밴률이 실제의 63% 로 축소된다.** 예외도 NULL 도 아니고
그냥 낮은 숫자라 화면에서는 "이 챔피언은 밴을 잘 안 당하네" 로 읽힌다.

주석으로 남기지 않고 컬럼을 나눴다(`ban_match_count`) — 문서에만 적힌 규칙은 지켜지지
않는다. 픽률은 그대로 `match_count` 를 쓴다. 픽은 스크림에도 있으므로 그쪽은 맞다.

**티어와 카운터를 한 바퀴에서 만든다.** 같은 관측을 쓰기 때문이다. 따로 돌리면 경기를 두 번
읽는 것도 문제지만, 더 나쁜 것은 두 표가 **서로 다른 시점의 데이터**로 만들어질 수 있다는
것이다. 화면은 둘을 나란히 놓는데(D21) 어긋나도 아무도 알아채지 못한다.
그래서 `CounterAggregationService` 를 `AggregationService` 로 바꿨다 — 이름이 하는 일과
어긋난 채 두면 다음 사람이 티어를 또 다른 서비스에 붙인다.

**감쇠는 챔피언 하나만 본다.** D42 의 "쌍은 두 챔피언의 변경을 합친다" 는 상성 전용이다.
티어에 `forPair` 를 쓰면 유효표본이 이유 없이 절반 속도로 마르는데 승률은 거의 안 변해서
눈으로는 안 보인다. 같은 이유로 가중치를 경기가 아니라 **참가자마다** 계산한다 — 한 경기
안에서도 챔피언마다 패치로 바뀐 횟수가 다르다.

**밴만 당한 챔피언도 표에 넣는다.** 출전 누적만 훑으면 한 번도 안 뽑힌 챔피언이 사라지는데,
**"아무도 안 뽑는데 늘 밴당하는" 챔피언이야말로 티어표가 보여줘야 하는 것이다.**

**뒤집힐 조건.** D19 의 회귀식이 정해지면 축소 목표값이 0.5 에서 챔피언별 예측값으로 바뀐다.
그때 이 결정의 "0.5 는 산술이다" 는 근거는 여전히 유효하다 — 예측값의 평균이 0.5 가 되도록
맞추면 된다.

---

## D51. 첫 실물 티어 — 검산은 통과했고, 컷라인은 아직 못 정한다 (2026-08-27)

운영 DB(공식 698 · 스크림 413 · 챔피언 40종)로 티어 집계를 처음 돌렸다. 테스트 214개가
전부 통과한 상태다.

**검산이 통과했다.** 이게 이번 리포트에서 가장 중요한 줄이다.

```
scope   스크림  픽률 합   출전 합   승리 합   전체 승률%
GLOBAL  제외     8.000     5,584     2,792     50.000
GLOBAL  포함     7.999     8,888     4,444     50.000
```

공식 698경기 × 8 = 5,584, 전체 1,111 × 8 = 8,888 로 정확히 맞고 승률이 양쪽 다 정확히
50.000% 다. 경기를 흘리지도 두 번 세지도 않았다는 뜻이고, 동시에 **축소 목표값 0.5 가
산술이라는 D50 의 주장이 실제 데이터에서 확인된 것**이다. 픽률 합 7.999 는
`numeric(6,4)` 40개를 더한 반올림(최대 0.002)이지 누락이 아니다.

**V4 가 작동한다.** 밴률 분모가 698(공식전)로 전체 1,111 과 분리돼 있다. 안 고쳤다면
Sniper 밴률이 20.6% 가 아니라 13.0% 로 나왔을 것이다.

**설계가 겨냥한 것이 실제로 잡혔다.** `Exorcist` 는 **51경기 출전에 80밴** 이다. 출전보다
밴이 많은 챔피언이 넷(Exorcist · Shadowmancer · Gambler · Executioner)인데 **티어 상위에는
하나도 없다.** 출전 누적만 훑었다면 전부 표에서 사라졌을 것들이다(D50). 그리고 이건
게임 AI 의 밴 기준이 우리 승률과 다르다는 신호라 D32(게임 AI 밴픽 모델)와 이어진다.

**스크림 두 벌을 만든 것이 값을 했다 (D47).** `PlagueDoctor` 가 스크림 포함 49.1% /
공식만 56.9% 로 **7.8%p** 차이다. 한 벌만 만들었다면 어느 쪽이든 절반은 틀린 표였다.

**고정 컷은 못 쓴다 — 예고대로다.**

```
40명 · 평균 49.71% · 표준편차 3.32%p · 범위 42.5~57.1
55% 이상 2명 · 45% 미만 4명   →  34명이 45~55% 에 몰린다
백분위 5/25/50/75/95 = 43.8 / 47.9 / 49.9 / 51.5 / 54.3
```

문서가 예상한 3.6%p 에 대해 실측 3.32%p 다.

**그런데 백분위로 자르는 것도 아직 못 정한다.** 사분위 간격이 3.6%p 인데 챔피언당 평균
222경기면 **표본 오차만으로 ±3.4%p 가 흩어진다.** 관측된 흩어짐의 상당 부분이 노이즈라면
백분위 등급은 노이즈에 등급을 매기는 것이 된다 — D9 이 "1~3경기의 100% 승률은 거의 전부
우연" 이라고 잘라낸 것과 같은 실수를, 이번엔 40명 전체에 대해 하는 셈이다.

추측으로 정할 문제가 아니라 재면 된다. `tools/tier_report.sql` 의 질의 9·10 이
관측 분산에서 표본 노이즈 분산을 빼 **진짜 강도 차이**를 역산하고, 거기서 통계적 최적
`k0` 를 낸다. D10 이 카운터에서 k≈74 를 역산한 것과 같은 계산이다.

**여기서 두 가지가 한꺼번에 갈린다.** 최적 `k0` 가 24 보다 훨씬 크면 지금 티어 추정은
덜 눌린 것이고 표의 흩어짐이 실제보다 과장돼 있다. 그러면 컷라인만이 아니라
`prior_strength_k0` 자체를 다시 봐야 한다 — 지금 값 24 는 카운터를 위해 정해진 것이고
티어에도 맞는지는 **아무도 확인한 적이 없다.**

**뒤집힐 조건.** 없다 — 이건 측정을 기다리는 상태이지 결정이 아니다.

---

## D52. 패치 가중치는 지금 정할 수 없다 — 경기가 한 시즌에 몰려 있다 (2026-08-27)

**논점.** "최근 패치로 통계를 내야 의미가 있다." 원리적으로 맞는 말이고, D15 가 반감기
2와 12를 "측정으로 정한 값이 아니다" 라고 남겨둔 자리다. 그래서 쟀다
(`tools/patch_weight_analysis.py`, 골든 파일 기준 공식 4v4 805경기).

**측정 1 — 경기가 시간축에서 어디 있나.** 이게 나머지를 전부 설명한다.

| 커리어 | 경기 시점 | 패치 총수 | 경기가 걸친 패치 순번 |
|---|---|---|---|
| 638064 | 2026 한 시즌 | 11 | 10~11 (**폭 1**) |
| 638377 | 2025 한 시즌 | 19 | 16~19 (**폭 3**) |
| 638683 | 2025 한 시즌 | 19 | 16~19 (**폭 3**) |

**세이브 한 벌에는 마지막 시즌의 경기만 남는다.** 패치는 5년치가 있는데 경기는 한 시즌뿐이다.
게임이 시즌마다 경기 기록을 버리기 때문이고, 그게 이 프로젝트의 존재 이유다(D6).
지금 DB 에 있는 것은 "누적" 이 아니라 **세 개의 마지막 시즌**이다.

**측정 2 — 그래서 감쇠가 할 일이 없다.** 경과 패치가 최대 3이라 메타 가중치는 최소 0.841,
자기 변경은 86%가 0회다. 남은 비율 0.911 — D49 가 DB 에서 본 0.903 과 같은 값이다.

**측정 3 — 반감기를 바꿔도 순위가 안 바뀐다.**

| 자기 / 메타 | 남은 비율 | 순위 상관 | 최대 변동 |
|---|---|---|---|
| 없음 / 없음 | 1.000 | 1.000 | — |
| **2 / 12 (현재)** | 0.911 | **0.976** | 1.96%p |
| 2 / 3 | 0.805 | 0.946 | 2.59%p |
| 1 / 1 | 0.637 | 0.853 | 4.74%p |

감쇠를 꺼도 티어 순위가 거의 같다. 세게 걸면 유효표본만 36% 날아가고 순위는 노이즈만큼
흔들린다. **어떤 값을 골라도 지금은 답이 같으므로, 고를 근거가 없다.**

**측정 4 — 폐기했다. 두 가지가 틀렸다.** 패치가 건드린 챔피언의 전후 `|승률 변화|` 가
12.83%p, 안 건드린 챔피언이 11.31%p 로 나와 "구별 안 됨" 이라고 적었는데, 그 측정은
(1) **절댓값을 써서 방향을 버렸고** — 상향/하향은 부호가 있는 효과다 —
(2) 챔피언 전체를 평균해서 **효과를 4~6배 희석했다**(한 패치가 건드리는 것은 40명 중 6~10명).
아래 "다시 잰 것" 으로 대체한다.

**측정 6 — 패치별 추정은 무작위 분할과 같다.** `θ_patch` 와 `θ_all` 의 차이가 중앙값
3.60%p 인데, **패치 경계 대신 같은 크기의 무작위 구간으로 나누면 3.81%p** 다. 비율 0.94 —
패치 경계로 나눈 쪽이 오히려 조금 덜 다르다. 그 3.6%p 는 패치 효과가 아니라 표본 노이즈다.
D9 이 "1~3경기의 100% 승률은 거의 전부 우연" 이라고 한 것과 같은 계산이다.

**결정 셋.**

1. **반감기 (2, 12)를 그대로 둔다.** 바꿀 근거가 없고 바꿔도 결과가 같다.
   다만 기록은 "검증됐다" 가 아니라 **"아직 구별되지 않는다"** 로 남긴다.
2. **2단 축소(패치별 스코프)를 지금 만들지 않는다.** 값어치가 0으로 측정됐다.
   D24 가 설계한 화면의 패치 선택도 지금 붙이면 사용자에게 노이즈를 보여주는 것이 된다.
   `champion_matchup.patch_id` 는 스키마에 그대로 두되 계속 NULL 만 쓴다.
3. **재측정 조건을 수치로 박는다.** 아래 둘 중 하나가 되면 이 스크립트를 다시 돌린다.
   - 한 커리어의 경기가 걸친 패치 폭이 **12 이상**(메타 반감기와 같은 크기). 지금은 1~3
   - 한 슬롯에 **시즌이 2개 이상** 누적. 지금은 전부 1

**메타 반감기 12 는 패치 밀도와는 맞는다.** 실측 시즌당 패치 3.8개이므로 12패치 ≈ 3.2시즌
이고, D15 가 의도한 "시즌 3~4개 분량" 과 일치한다. 값 자체가 엉뚱한 것은 아니다.

**주의 — 지금 아무 일도 안 하는 이 파라미터가 나중에 가장 세게 작동한다.** 누적이 쌓이면
경과 패치가 수십이 된다. 시즌당 3.8패치 기준으로

```
1시즌 전 → 0.80    3시즌 전 → 0.52    5시즌 전 → 0.33    10시즌 전 → 0.11
```

15시즌 전 데이터는 사실상 삭제된다. 그게 원하는 동작인지는 그때 판단할 문제지만,
**지금 아무 효과가 없다고 해서 안전한 값이라는 뜻은 아니다.** 재측정 조건을 박아두는
이유가 이것이다.

### 다시 잰 것 — 방향을 살리고 평균 회귀를 걷어냈다

실제 패치 내역이 작지 않다는 지적을 받고 다시 쟀다(`공격 속도 0.83→1.05` 같은 변경이 매
패치 6~10개 챔피언에게). 세이브의 변경 내역은 **챔피언마다 스탯 하나씩, 부호 있는 델타**라
상향/하향을 정확히 가를 수 있다(`skill_cool` 만 증가가 하향이다).

**측정 7 — 게임은 표적을 골라 패치한다. 이건 진짜 신호다.**

```
상향 직전 승률 평균 36.9% (9건)     하향 직전 승률 평균 51.7% (18건)
```

무작위가 아니다. 게임이 못하는 챔피언을 올리고 잘하는 챔피언을 내린다. 그리고 상향된
챔피언은 다음 구간에 **+11.85%p** 올랐다.

**측정 8 — 그런데 그 상승이 전부 평균 회귀다.**

대조군(안 건드린 챔피언 147건)에서 "직전 승률 → 다음 구간 변화" 의 기울기가 **-0.866** 이다.
직전 구간에서 50% 보다 10%p 낮았던 챔피언은 패치를 안 받아도 다음 구간에 +8.7%p 오른다.

| 구분 | 표본 | 실제 변화 | 회귀 예측 | 순효과 | t |
|---|---|---|---|---|---|
| 상향 | 9 | +11.85%p | +11.10%p | **+0.75%p** | 0.12 |
| 하향 | 18 | −0.39%p | −1.72%p | **+1.33%p** | 0.41 |
| 통합 | 27 | | | **−0.64%p** | −0.22 |

상향 그룹의 +11.85%p 는 회귀 예측 +11.10%p 로 거의 전부 설명된다. **패치 순효과는 노이즈
안에 있다.** 검출력은 **5.8%p** 다 — 그보다 작은 효과는 이 표본으로 잡을 수 없다.

**기울기 -0.866 이 이 조사에서 가장 중요한 숫자다.** 기울기가 -1 이면 직전 구간의 승률이
다음 구간에 대해 **아무 정보도 주지 않는다**는 뜻이다. -0.866 은 거기서 멀지 않다.
패치 구간(47~171경기) 단위의 챔피언 승률은 사실상 노이즈다.

**그래서 결론이 바뀌지 않되, 이유가 정확해진다.** "패치 효과가 없다" 가 아니라
**"패치 구간 단위의 표본으로는 패치 효과를 볼 수 없다"** 이다. 5.8%p 미만이면 안 보이고,
실제 효과가 그 안에 있을 가능성은 충분하다. 위의 결정 셋은 그대로 유효하다 —
특히 2단 축소를 만들지 않는 근거가 더 강해졌다. 구간 추정치 자체가 노이즈이기 때문이다.

**얼마나 더 있어야 보이나.** 관측당 잔차 표준편차가 12.65%p 이므로, 3%p 효과를 t=2 로 잡으려면
건드린 챔피언 관측이 약 70건 필요하다. 지금 27건이다. 커리어당 시즌마다 패치 경계가 3개쯤
생기므로 **시즌 2~3개가 더 쌓이면 도달한다.** 재측정 조건에 이것도 함께 둔다.

**뒤집힐 조건.** 위 재측정 조건이 충족되면 전부 다시 본다 — 경기가 걸친 패치 폭 12 이상,
슬롯당 시즌 2개 이상, 또는 건드린 챔피언 관측 70건 이상.

---

## D53. 기준을 "현재 패치 · 현재 커리어" 로 옮긴다 (2026-08-28)

**논점.** 유저는 현재 패치 기준으로 플레이한다. 앱이 "A 가 좋다" 고 하면 그건 **지금** 좋다는
뜻이어야 하는데, 지금 구현은 옛 패치를 그대로 합쳐서 답한다.

D52 는 이 질문에 반쯤만 답했다. 거기서 잰 것은 **분산**(패치 구간 추정치가 노이즈인가)이고,
여기서 문제인 것은 **편향**(통합 추정치가 현재 패치에 대해 틀린가)이다. 둘은 다른 질문이다.
그리고 D51 이 근거를 준다 — 챔피언 간 진짜 강도 차이가 작으므로(관측 3.32%p, 대부분 노이즈)
**패치 효과가 3%p 만 돼도 티어 순위를 뒤집기에 충분하다.**

**측정 9 — 낡은 데이터 비율이 크다.** 가장 최근 커리어에서 40명 중 17명이 경기 기간 안에
바뀌었고, 그 챔피언들의 출전 중 "바뀌기 전" 비율이 `PlagueDoctor 83%` · `Jiangshi 74%` ·
`MagicKnight 68%` · `LightningMage 63%` 다. `LightningMage` 는 마지막 패치에서 `magic -55`
하향을 받았는데, 티어표의 그 승률은 63%가 하향 이전 데이터다.

**틀린 방향 하나를 먼저 버렸다.** "바뀐 챔피언의 옛 데이터만 세게 깎자"(자기 변경 축 강화)를
제안했다가 접었다. **한 챔피언이 하향되면 그에게 카운터당하던 챔피언의 승률도 따라 움직인다.**
효과가 바뀐 챔피언에 갇히지 않는다 — D15 가 메타 축을 둔 이유가 정확히 그것이다.
자기 축만 세게 하면 나비효과를 놓친다.

**측정 12 — 메타 축만 따로, 커리어별로.** 자기 축은 끄고 메타 반감기만 움직였다.

| 메타 반감기 | 표본 유지 | **ess≥10 챔피언** | 평균 이동 | 최대 이동 | 순위 변동 |
|---|---|---|---|---|---|
| 없음 | 1.000 | 37 | — | — | — |
| **12 (기존)** | 0.943 | 37 | 0.23%p | 0.77%p | 2칸 |
| 3 | 0.810 | 37 | 0.84%p | 2.86%p | 6칸 |
| **2 (새 값)** | 0.746 | 37 | 1.16%p | 4.02%p | 9칸 |
| 1 | 0.625 | 36 | 1.87%p | 6.41%p | 11칸 |

**진짜 비용은 "남은 비율" 이 아니라 `ess≥10` 이다.** 표본 유지 0.625 는 겁나 보이지만
챔피언당 출전이 20~180건이라 감쇠 후에도 유효표본이 기준선을 넘는다. **반감기 1까지 줄여도
화면에 뜰 자격을 잃는 챔피언이 1명뿐이다.** D52 에서 "남은 비율 0.911" 만 보고 "감쇠가
아무것도 안 한다" 고 쓴 것은 비용을 잘못된 자로 잰 것이다.

**결정 1 — 메타 반감기 12 → 2 (V5).** 12는 실측상 평균 0.23%p 로 아무 일도 하지 않았다.
실측 패치 밀도가 시즌당 3.8개이므로 2패치는 약 0.5시즌이다. 자기 변경 축(2)은 그대로 둔다 —
바뀐 챔피언이 더 무효인 것도 사실이므로 두 축을 함께 쓴다.

**이 값은 `analysis_config` 행이다.** `UPDATE` 한 줄로 1이나 4로 바꿔 재집계할 수 있다.
1은 "한 패치 지나면 절반", 4는 "한 시즌 지나면 절반" 이다.

**결정 2 — `GLOBAL` 스코프 생산을 멈춘다 (V5).** 측정 11 이 이유다. 패치는 커리어마다 고유
생성되므로 19패치를 거치면 같은 이름의 챔피언이 커리어별로 반대 방향으로 가 있다.

```
Dancer      attack -58  /  max_hp +1224  /  skill_cool +12
Berserker   max_hp -292 /  defence -1    /  max_hp +326
Bard        magic  +40  /  max_hp +886   /  magic +98
```

`max_hp +1224` 와 `-292` 는 같은 챔피언이 아니다. **표본은 3배지만 합쳐진 대상이 다르다.**
유저는 한 슬롯을 플레이하고 그 안에서는 밸런스가 일관된다.

**스키마는 그대로 둔다.** `agg_scope` ENUM 과 GLOBAL 을 허용하는 유일키는 남기고, 낡은
GLOBAL 행만 지운다. 커리어별 밸런스를 정규화할 방법을 찾으면 되살릴 자리다.
`MatchupAggregate.merge` 와 `ChampionTallyAggregator.merge` 도 지우지 않고 **"아무도 안
부른다" 고 명시**했다 — 무엇을 막아주는 척하지 않으므로 `tfm.read-only` 와 같은 착시는 아니다.

**표본이 줄어드는 것은 감수한다.** 커리어당 공식 294건 · 스크림 포함 477건이고, 플레이어
경기만이 아니라 AI 끼리의 경기도 전부 들어오므로 한 커리어로도 표본이 나온다.
커리어 초반에는 부실하지만, 그건 축소추정이 처리할 문제다.

**뒤집힐 조건.** 커리어 사이의 밸런스 차이를 보정할 방법이 생기면 GLOBAL 이 되살아난다 —
예를 들어 챔피언의 누적 스탯 변화를 알고 있으므로, 그것으로 정규화하는 모형을 세울 수 있다.
지금은 그 모형이 없다.

**남은 것 — 팀별 통계는 적재부터 고쳐야 한다.** 팀별 승률·선호 챔피언은 밴픽 추천에 바로
쓸모가 있고(가장 최근 커리어에 23팀, 상위 팀이 47~66경기), 파서는 `blueTeamId`/`redTeamId`
를 이미 읽고 있다. **그런데 적재가 그 값을 버린다** — `team` 테이블이 비어 있고
`match_record.blue_team_id`/`red_team_id` 가 전부 NULL 이다. `TeamRepository` 는 있는데
아무도 쓰지 않는다. 집계만으로는 만들 수 없고 적재 수정 + 기존 데이터 재적재가 필요하다.

---

## D54. 팀은 공식전에서만 식별된다. 스크림에는 붙이지 않는다 (2026-08-28)

**논점.** 사용자가 팀별 통계를 요청했다. 그런데 `team` 테이블이 비어 있고
`match_record.blue_team_id` / `red_team_id` 가 전부 NULL 이었다 — **파서는 읽고 있는데
적재가 버렸다.** `SaveLoader` 에 `match.setTeams(null, null, blueScore, redScore)` 라고
적혀 있었다. 스키마도 `TeamRepository` 도 처음부터 있었다. 이어붙이지 않은 것뿐이다.

**측정 — 세이브에 무엇이 있나.** 골든 파일(`slot_638683925954242004`, 공식 294 · 스크림 181):

| | 값 |
|---|---|
| `GameStat.BlueTeamID` / `RedTeamID` | 23종. 0(플레이어) 62회 · 35번 66회 · 36번 58회 … |
| `ScrimStat.TeamID` | **181건 전부 0** |
| 팀 이름 | 경기 스트림(스트림 1)에 없다 |

**결정 1 — 공식전에만 팀을 붙인다.** `ScrimStat.TeamID` 는 전부 0, 즉 **플레이어 팀 자신**이다.
상대 번호가 어디에도 없고, 두 진영 중 어느 쪽이 플레이어인지도 세이브가 말해주지 않는다.
없는 것을 추측해 채우면 팀별 통계가 조용히 틀린다 — 스크림은 NULL 로 남긴다.

이건 손해가 아니다. 팀별 통계에서 의미 있는 것은 공식전이고, 스크림은 상대가 늘 같은
연습 상대라 팀 승률의 분모로 쓸 값이 아니다.

**결정 2 — 번호가 없으면 팀도 없다.** `gameTeamId == null` 이면 `null` 을 돌려준다.
`int` 로 받아 기본값 0 으로 떨어지면 **번호 없는 경기가 전부 플레이어 팀 경기로 둔갑한다.**
플레이어 팀은 화면에서 가장 많이 보게 될 팀이라, 이 오염은 눈에 안 띄면서 제일 아프다.

**결정 3 — 팀 이름은 비워 둔다.** 경기 스트림에 없다. 다른 스트림(0 = 미리보기)에 있을 수는
있지만 확인하지 않았다. `team.name` 은 NULL 이고, 화면은 당분간 번호로 보여준다.
**모르는 것을 "팀 36" 같은 문자열로 채워 아는 것처럼 만들지 않는다.**

**결정 4 — `TeamRegistry` 는 빈이 아니다. 적재 한 번에 하나씩 만든다.**
경기마다 조회하면 왕복이 경기 수만큼 늘어 캐시가 필요한데, **캐시를 빈에 두면 트랜잭션보다
오래 산다.** `SaveLoader` 가 롤백되면 팀 행은 사라지고 캐시에는 그 `team_id` 가 남아,
다음 적재가 없는 키를 참조한다. 수명을 트랜잭션에 맞추는 가장 확실한 방법은
트랜잭션 안에서 만드는 것이다.

`SlotRegistry` 와 이름은 닮았지만 **트랜잭션 성격이 정반대**다. 슬롯은 적재가 실패해도
남아야 하고(실패 기록이 참조한다), 팀은 적재가 실패하면 같이 사라져야 한다.

**결정 5 — 백필은 적재 경로 안에서 한다.** 팀 없이 이미 들어간 경기가 수천 건이다.
그런데 평소 경로로는 그 행을 다시 지나갈 수 없다 — `ingest_run` 의
`UNIQUE(slot_id, file_hash)` 가 같은 내용을 먼저 걸러낸다.

두 갈래를 놓고 골랐다:

| | 별도 백필 SQL | 적재 경로에서 채우기 |
|---|---|---|
| 규칙이 사는 곳 | 두 곳 (따로 늙는다) | 한 곳 |
| D35 형식 필터 | 다시 써야 한다 | 그대로 적용된다 |
| 팀 번호의 출처 | **DB 에 없다** — 세이브를 읽어야 한다 | 이미 읽고 있다 |

세 번째가 결정적이다. 팀 번호는 DB 어디에도 없으므로 어떤 백필이든 **세이브를 다시 읽어야
한다.** 그러면 적재 경로를 쓰는 것이 맞다. `SaveLoader.saveGames` 가 이미 적재된 경기를
만나면, 팀이 비어 있을 때만 채운다.

**이미 팀이 붙은 경기는 덮지 않는다.** 덮으면 재적재가 값을 바꿀 수 있게 되고,
"같은 파일을 다시 적재해도 아무것도 안 바뀐다" 는 계약이 깨진다.
점수도 건드리지 않는다 — 그래서 `setTeams` 와 `assignTeams` 를 나눴다. 고치러 와서
멀쩡한 것을 깨는 경로를 아예 만들지 않는다.

**결정 6 — `tfm.reingest-on-start` (기본 꺼짐).** 백필을 실제로 돌리려면 해시 검사를
건너뛰어야 한다. `ReingestRunner` 가 `SaveLoader.load()` 를 직접 부른다 — 우회하는 것은
해시 검사 하나뿐이고 적재 규칙은 한 벌 그대로 쓴다.

게임을 계속 하는 슬롯은 해시가 바뀌어 저절로 따라잡히지만, **손대지 않는 커리어는 영원히
비어 있는 채로 남는다.** 커리어 3개 중 2개가 그 상태다 — D39 가 잡은 것과 같은 종류의 누수다.

`ingest_run` 은 남기지 않는다. 새로 적재한 것이 없기 때문이다. 억지로 남기면 유니크 위반으로
죽거나, 해시를 비틀어 "이 내용은 적재됐다" 는 기록을 두 벌로 만든다. **이건 적재가 아니라
수리다.** 다시 돌리면 백필 0건을 찍는다 — 그게 다 됐다는 신호다.

**검증.** DB 없이 도는 테스트 10개(`TeamRegistryTest` · `ReingestRunnerTest`)를 변조 4종으로
확인했다 — null 가드 제거 · 캐시 제거 · 기존 팀 재사용 제거 · 첫 슬롯에서 루프 중단.
넷 다 잡혔다. 픽스처가 필요한 3개(`IngestServiceTest` 의 D54 절)는 **아직 변조로 확인하지
않았다** — 워크트리에 `fixtures/*.tfm` 이 없어 건너뛰어진다. 메인에서 돌려야 한다.

**주의 — `ApplicationRunner` 실행 순서는 정의되어 있지 않다.** 「아직 안 정한 것」의 집계
트리거 항목과 같은 함정이다. `tfm.reingest-on-start` 와 `tfm.aggregate-on-start` 를 **같이
켜지 마라.** 집계가 재적재보다 먼저 끝나면 팀이 아직 비어 있는 상태로 집계된다.
수리 한 번, 집계 한 번 — 두 번 띄운다.

**아직 안 한 것.** 팀별 집계와 화면은 없다. 적재가 먼저다 — 채워지지 않은 컬럼 위에
집계를 얹으면 "0건" 과 "안 돈다" 를 구별할 수 없다.

**뒤집힐 조건.** 다른 스트림에서 팀 이름을 찾으면 결정 3 이 바뀐다.
스크림에 상대 팀 번호를 담는 필드를 찾으면 결정 1 이 바뀐다.

## D55. 팀 이름은 `common.data` 에서 온다. 적재 시점에 슬롯별로 박아 넣는다 (2026-08-29)

**논점.** D54 가 팀을 적재했지만 이름이 없어 화면에 `팀 36` 으로만 보였다. 세이브 파일을
바이트로 뒤져도 이름이 없다 — `Seorabal` · `Gen.G` · `Griffin` 어느 것도 안 나온다.

**찾았다 — 세이브 폴더의 `common.data` 다.** 세이브와 같은 NRBF 라 파서를 그대로 쓴다.

```
CommonStore
 ├ TeamName   : String              ← 플레이어 팀 (번호 0)
 ├ CoachName  : String
 └ AITeamData : AITeamListData
     ├ Name : String                ← 프로필 이름
     └ Data : Dictionary<int, AITeamData>
         └ KeyValuePairs[] { key, value } → AITeamData { Name, Logo }
```

**딕셔너리의 키가 곧 `game_team_id` 다.** 실측: AI 팀 52개, 번호 1~56 에 구멍 넷(8·19·30·41).
플레이어 팀(0)은 딕셔너리가 아니라 `CommonStore` 뿌리에 있다.

**구멍이 테스트의 근거가 됐다.** 배열 인덱스를 번호로 쓰면 키가 0..51 로 촘촘해진다.
`최대 번호 > 팀 수` 를 단언하면 그 실수가 바로 잡힌다 — 개수만 세는 단언으로는 못 잡는다.

**이 파일은 커리어와 무관하게 바뀐다. 그게 문제의 전부다.**

사용자가 게임에서 팀을 커스터마이즈하면 그 자리에서 갈린다. 실제로 이번에 그랬고,
**결과가 이미 관측됐다.** 커리어의 정규리그 10팀(번호 0·27·29·31~37) 중 66세트를 뛴 35번은
지금 이름표에서 `dd` 다 — 사용자가 방금 넣은 자리표시자다. 27·29·31~37 도 지금은
`Lyon Gaming` · `Chiefs Esports Club` · `KT Rolster` · `Dplus KIA` · `JD Gaming` · `Invictus
Gaming` · `dd` · `Karmine Corp` · `Cloud 9` 로, 한 리그로 보기 어려운 조합이다.

**즉 지금 이름표는 지나간 커리어의 이름이 아니다.** D53 이 패치에서 잡아낸 것과 같은 형태다 —
*같은 번호가 시점마다 다른 대상을 가리킨다.*

**결정 1 — 화면에서 실시간으로 읽지 않는다. 적재 시점에 `team.name` 에 박는다.**
`team` 의 자연키가 `(slot_id, game_team_id)` 이고 `name` 도 슬롯별이라, 커리어마다 그때의
이름을 따로 담을 수 있다. 실시간 조회로 두면 프로필을 만질 때마다 **과거 표의 이름이 통째로
바뀐다.**

**결정 2 — 이미 붙은 이름은 절대 덮지 않는다.** `Team.nameIfAbsent` 가 그걸 강제한다.
덮기 시작하면 결정 1이 무의미해진다. 재적재도 이름을 바꾸지 못한다.

**결정 3 — 이미 지나간 커리어 2개의 이름은 틀릴 수 있다는 것을 받아들인다.**
당시 이름은 어디에도 남아 있지 않아 복구할 방법이 없다. 화면에 "현재 프로필 기준" 을
표시하는 선에서 감수한다. **앞으로 적재되는 커리어부터는 정확하다.**

**결정 4 — 이름표가 없거나 깨져도 적재를 멈추지 않는다.** 이름은 표시용이고 경기가 본체다.
게임이 그 파일 형식을 바꿨다고 공식 경기 수백 건을 못 넣게 되는 쪽이 훨씬 나쁘다.
**대신 조용히 넘어가지 않는다** — 경로와 원인을 WARN 으로 남기고, 결과는 화면에 이름 없는
팀으로 그대로 드러난다. 삼키는 것과 다른 점이 그것이다.

**빈 문자열을 이름으로 두지 않는다.** `nameOf` 가 `Optional` 을 돌려주고 공백은 비어 있는
것으로 친다. 빈 이름을 붙이면 화면에서 "이름 없는 팀" 과 "이름이 있는 팀" 이 같아 보인다.

**검증.** DB 없이 도는 테스트 8개(`CommonDataParserTest` 5 · `TeamRegistryTest` 3 추가)를
변조 2종으로 확인했다 — 이름 무조건 덮어쓰기, 0번을 딕셔너리에서 찾기. 둘 다 잡혔다.
픽스처가 필요한 1개는 메인에서 돌려야 한다.

**뒤집힐 조건.** 게임이 세이브 안에 팀 이름을 넣기 시작하면 결정 1~3 이 전부 필요 없어진다.
`common.data` 에 프로필 변경 이력이 있다면 결정 3 도 다시 본다.


## D56. 팀 이름의 1순위는 세이브의 `TeamInfo` 다 — D55 를 고친다 (2026-08-29)

**D55 의 결론 하나가 틀렸다.** 거기서 "지나간 커리어의 팀 이름은 어디에도 없어 복구
불가" 라고 썼는데, **세이브 안에 있었다.** `common.data` 를 붙이고 나서 세이브의 다른
클래스를 훑다가 `TeamInfo` 53개를 찾았다.

```
0        Ember scale               [UseKey=true]    ← 이 커리어의 플레이어 팀 이름
1–7      team.name.amateur.team1–7        (7)
8–17     team.name.semi_pro.team1–10     (10)
18–27    team.name.pro2.team1–10         (10)
28–37    team.name.pro.team1–10          (10)
38–52    team.name.worlds.team1–15       (15)
```

**`ID` 가 곧 `game_team_id` 이고, `NameKey` 가 이름 또는 로컬라이제이션 키다.**
`UseKey` 가 둘을 가른다. 커리어 안에 있으므로 **그 커리어를 플레이할 때의 값**이다.

**관측이 전부 맞아떨어졌다.** 커리어의 정규리그 10팀은 `0`(플레이어) + `27`(pro2.team10,
승격) + `29·31~37`(프로1부)이고, 3~18세트짜리 꼬리는 **전부 38~52 = 월드 챔피언십**이다.
D54 노트에 "하위 리그" 라고 적었던 꼬리표는 틀렸다 — 월드가 맞다.
AI 팀 합계 `7+10+10+10+15 = 52` 로 `common.data` 의 52개와도 일치한다.

**그리고 D55 가 근거로 든 사례가 오히려 반대를 증명한다.** 66세트를 뛴 35번이 지금
프로필에서 `dd` 인 것은 맞지만, 세이브가 말하는 그 팀은 `team.name.pro.team8` 이다.
`dd` 는 사용자가 **방금** 만든 커스텀이고 **앞으로 만들 커리어**에 붙는다.
즉 `common.data` 는 커리어의 이름이 아니라 **다음 커리어의 예고**다.

**결정 1 — 출처 우선순위를 못 박는다.**

| | 출처 | 언제 |
|---|---|---|
| 1 | 세이브 `TeamInfo` | 항상. 커리어 시점의 사실이다 |
| 2 | 시드 (`team_name_seed`) | 1이 로컬라이제이션 키일 때 이름으로 해석 |
| 3 | `common.data` | 세이브가 그 번호를 모를 때만. 폴백으로 내렸다 |

**결정 2 — 키를 저장한다 (`team.name_key`).** `name` 은 해석 결과이고 `name_key` 는
세이브가 말한 사실이다. 둘을 나눠야 **시드가 틀렸을 때 재적재 없이 고칠 수 있다.**
이름은 표시용인데 적재를 오염시킬 이유가 없다.

**결정 3 — 시드에 없는 키는 이름을 비운다.** 키를 이름 자리에 넣으면 화면에
`team.name.pro.team8` 이 팀 이름처럼 뜬다. 모르는 것은 비워 두는 쪽이 D51(티어 컷라인)과
같은 원칙이다.

**시드는 게임 에셋에서 못 뽑았다.** `sharedassets0.assets` 에 키는 있는데 표시 문자열이
없다(키마다 정확히 1회 출현 — 팀 정의표뿐이다). 사용자가 게임 화면에서 읽어 준 목록을
`V6` 시드로 넣었다. **검증은 한 점으로 했다** — `pro.team8 = KT Rolster Bullets`,
그 팀이 실측 커리어에서 66세트를 뛴 35번이다.

**남은 불확실성 하나.** `worlds` 는 15팀인데 받은 목록이 14개였다. 빠진 하나를
`Afreeca Freecs` 로 지목받아 끝(`team15`)에 뒀지만 **목록 안에서의 위치는 확인되지 않았다.**
어긋나 있으면 `team_name_seed` 의 행 하나만 고치면 된다. `worlds` 는 표본이 3~18세트라
화면 영향도 가장 작다.

**결정 4 — `ParsedSave` 에 팀을 넣지 않는다. 파서를 따로 둔다 (`TeamInfoParser`).**
`ParsedSave` 는 골든 파일과 <b>바이트 단위로</b> 비교되는 언어 무관 계약이다. 거기에 필드를
더하면 파이썬 레퍼런스와 베이스라인 세 벌을 같이 갈아야 하고, **"구조 변경" 과 "스냅샷 갱신"
이 한 커밋에 섞인다.** 지금 스냅샷은 이미 DB 보다 뒤처져 있어(패치 19 vs 20) 그 갱신은
독립된 작업이어야 한다. 팀 신원은 경기 기록이 아니라 식별자라 관심사도 다르다.

비용은 경기 스트림을 한 번 더 읽는 것이다. 적재는 세이브가 바뀔 때만 돌고 그 간격이
분 단위라 문제되지 않는다 — 실제 비용은 `IngestCostTest` 가 지킨다.

**검증.** DB 없이 도는 테스트 6개(`TeamNamingTest`)를 변조 3종으로 확인했다 —
`common.data` 를 먼저 보기 · 시드에 없는 키를 이름으로 쓰기 · `UseKey` 무시하기. 셋 다 잡혔다.

**뒤집힐 조건.** 게임 로컬라이제이션에서 키→이름을 뽑아내면 시드가 그것으로 대체된다.
`worlds` 순서가 어긋난 것으로 밝혀지면 시드 행을 고친다 — 설계는 그대로다.


## 아직 안 정한 것

- **티어 등급 컷라인** — 기저 승률 표준편차가 3.6%p 로 작아서 고정 컷(55% 이상 S)을
  쓰면 대부분 한 등급에 몰린다. 백분위 기반이 맞아 보이지만 분포를 보고 정해야 한다.
  **실측했다 (D51).** 40명 평균 49.71% · 표준편차 3.32%p · 55% 이상 2명 — 고정 컷은
  확실히 못 쓴다. 그런데 백분위도 아직 못 정한다. 표본 오차만으로 ±3.4%p 가 흩어져서
  관측된 흩어짐의 상당 부분이 노이즈일 수 있다. `tier_report.sql` 질의 9·10 이
  노이즈 비중과 통계적 최적 `k0` 를 낸다 — 그걸 보고 정한다.
  그때까지 `tier_grade` 는 NULL 이고 화면은 추정 승률로 정렬한다 (D50)
- **감쇠 반감기 2 / 12** — **측정했고, 지금은 정할 수 없다는 것이 결론이다 (D52).**
  경기가 한 시즌에 몰려 있어 경과 패치가 최대 3이다. 반감기를 어떻게 잡아도 티어 순위가
  거의 같다(감쇠 없음 대비 순위 상관 0.976). 재측정 조건을 D52 에 수치로 박아뒀다
- **`prior_strength_k0` 가 티어에도 맞는 값인가** — 24 는 카운터를 위해 정해졌고
  티어에 대해서는 확인된 적이 없다. `tier_report.sql` 질의 9 가 통계적 최적값을 낸다 (D51)
- **경기력 → 승률 예측 모델** — D19 의 축소 목표값을 만드는 회귀식. 어떤 지표를
  어떤 가중으로 넣을지 정하지 않았다
- **여러 사용자 데이터 합산** — `GLOBAL` 스코프가 그 자리지만, 커리어마다 리그 레벨과
  선수 능력치가 달라 그대로 합쳐도 되는지 검증하지 않았다
- **집계를 언제 돌릴지** (`AggregationService.run()`) — 적재 직후마다 돌릴지, 화면 요청 때 돌릴지, 수동으로 돌릴지.
  화면이 생겨야 판단할 수 있다.

  **실측으로 하나가 분명해졌다 (2026-08-27).** `tfm.aggregate-on-start=true` 로 기동하면
  집계가 **따라잡기 적재보다 항상 먼저 끝난다.**

  ```
  00:30:43.440  StartupCatchUp   따라잡기 시작
  00:30:43.441  AggregationRunner 집계 시작      ← 1ms 뒤
  00:30:44.358  집계 완료 run=4
  00:30:45.333  적재 ...                         ← 집계보다 1초 늦다
  ```

  우연이 아니다. `StartupCatchUp` 은 `rescan()` 으로 1.5초 디바운스를 걸고 즉시 반환하는데
  (D39·D12), `AggregationRunner` 는 그 뒤에 동기로 돈다. 게다가 `ApplicationRunner` 두 개의
  실행 순서 자체가 정의되어 있지 않다.

  게임을 하고 나서 앱을 켜면 따라잡기가 새 경기를 가져오는데 집계는 그 이전 데이터로 돈다.
  예외도 경고도 없이 **최신 경기가 빠진 표**가 나오고 숫자는 여전히 그럴듯하다.
  지금은 기본이 꺼져 있어 피해가 없지만, 켜는 순간 이 순서가 기본 동작이 된다.

  그래서 이 미결은 "언제" 만이 아니라 **"무엇이 끝난 뒤에"** 를 같이 정해야 한다.
  적재 완료를 신호로 삼는 쪽이 자연스러워 보이지만, 그러면 워처가 저장을 감지할 때마다
  집계가 도는 것이라 비용을 먼저 재야 한다 (D37)
- **이벤트전의 다른 타입** — D16 참조. 위험은 낮다

---

[[teamFighterManger.README]]
